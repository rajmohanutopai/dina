# Dev overrides — mount source code, enable hot reload, verbose logging.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  core:
    build:
      context: ./legacy/go-core
      dockerfile: Dockerfile
    volumes:
      - ./legacy/go-core:/src
      - ./data:/data
    environment:
      - DINA_LOG_LEVEL=debug

  brain:
    build:
      context: ./legacy/python-brain
      dockerfile: Dockerfile
    volumes:
      - ./legacy/python-brain/src:/app/src
    environment:
      - DINA_LOG_LEVEL=debug
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8200", "--reload"]

  llm:
    ports:
      - "8080:8080"
