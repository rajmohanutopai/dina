/**
 * Mobile compatibility export. The wire builder is shared with Home Node Lite
 * so both owner surfaces emit exactly the same `dina1:` contract.
 */
export { buildAgentSetupCode, SETUP_CODE_PREFIX, type AgentSetupPayload } from '@dina/core';
