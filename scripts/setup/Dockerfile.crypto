# Dockerfile.crypto — lightweight image for install-time crypto operations.
# Used by install.sh to avoid requiring Python on the host machine.
# Scripts are bind-mounted at runtime (not copied into the image).
FROM python:3.12-slim
RUN pip install --no-cache-dir argon2-cffi cryptography mnemonic \
    && rm -rf /root/.cache
