# Dockerfile.crypto — lightweight image for install-time crypto + installer core.
# Used by install.sh to avoid requiring Python on the host machine.
# Scripts and installer module are bind-mounted at runtime (not copied).
FROM python:3.12-slim
RUN pip install --no-cache-dir argon2-cffi cryptography mnemonic pydantic \
    && rm -rf /root/.cache
