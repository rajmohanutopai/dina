# Trust Network V2 — Actionability Layer

Trust answers "should I believe this?" — V2 closes the gap to "can I act on this?". A trusted recommendation is unactionable when the reviewed thing is unavailable in your region, priced out of reach, incompatible with your devices, stale, or just doesn't fit your use case. V1 ships the trust score; V2 makes the recommendation *useful*.

**V1 reference:** `docs/TRUST_NETWORK_V1_PLAN.md` + `docs/TRUST_NETWORK_V1_BACKLOG.md`

## Why this exists

V1's promise — "ranked by trust, not ad spend" — is necessary but not sufficient. A recommendation is only USEFUL if you can act on it. The 12 gaps below are what an ad-driven platform handles invisibly (regional inventory, price filters, device targeting, freshness), and what a decentralised trust network must surface explicitly to compete.

The trust score itself stays region-agnostic and viewer-agnostic — that's what makes scores cacheable + comparable + auditable. Actionability is layered ON TOP as filters, badges, and re-ranking, all keyed by the viewer's locally-held profile that NEVER leaves the device (Loyalty Law).

## Status legend

- `[ ]` todo
- `[-]` in progress
- `[x]` done
- `[!]` blocked / needs decision

## The 12 gaps (motivation, not tasks)

| # | Gap | Concrete failure mode |
|---|---|---|
| 1 | Access / availability | Trusted pen sold only in Uganda — useless to a US viewer. |
| 2 | Recency / freshness | 2018 phone review — model EOL, security stale. 5-year-old restaurant — chef left, ownership changed. |
| 3 | Affordability | Trusted $400 chair — irrelevant to $80 budget. |
| 4 | Compatibility | "Best phone case" — useless if reviewer has iPhone and you have Samsung. |
| 5 | Comparative context | "Is X trusted?" answered. "Is X the BEST trusted option?" not. |
| 6 | Use-case fit | Camera reviewed for wildlife vs weddings vs vlogging — same product, different verdict per purpose. |
| 7 | Negative space | Trust surfaces good things; doesn't proactively warn ("this brand is flagged by 3 of your contacts"). |
| 8 | Language | Mandarin-only review of a movie — useless to non-Mandarin speaker. (Server has filter; mobile UI doesn't surface.) |
| 9 | Schedule / hours | Restaurant only Tue–Sat; 3-week booking lead; seasonal product. |
| 10 | Compliance | Halal/kosher/vegan/gluten-free; FDA/CE marked; age-restricted. |
| 11 | Accessibility (disability) | Wheelchair access; screen-reader compat; captioned content. |
| 12 | Personalisation | Trust score is global, not viewer-specific. A reviewer who matches your taste should weigh more than a stranger with the same trust score. |

## Cross-cutting principles

- **Loyalty Law (privacy):** the viewer's profile (region, budget, devices, dietary, accessibility, language) is keystore-resident and NEVER sent to AppView. All viewer-context filtering is mobile-side after the network query.
- **Trust score stays viewer-agnostic.** Cacheability + comparability + auditability depend on this. Actionability is layered as filters/boosts on top — the underlying score doesn't change.
- **Reviewer-stated metadata is optional.** Existing V1 attestations stay valid. New V2 fields are additive on the lexicon.
- **Subject metadata splits server-extracted vs reviewer-stated.** The enricher fills what it can derive (host TLD → region, OpenGraph → price, JSON-LD → hours). Reviewers fill the rest.
- **Negative space stays opt-in surfacing.** Auto-warnings about flagged brands need careful UX — they can read as "Dina is shaming X" rather than "your trusted contacts have concerns". Phase 4 work, not phase 1.

## Suggested phased path

| Phase | Lift | Delivers | Sections |
|---|---|---|---|
| **P1** | small | Surface what already exists — host TLD, language, lat/lng on cards. | §1 only |
| **P2** | medium | Viewer profile (Cluster A) + subject availability metadata (Cluster B). Closes gaps 1, 3, 4, 8. | §2 + §3 |
| **P3** | medium | Lexicon additions + Write form fields (Cluster C). Closes gaps 2, 6. | §4 |
| **P4** | medium-large | Ranking changes (Cluster D). Closes gaps 5, 7, 9, 10, 11, 12. | §5 |

---

## 1. Phase 1 — Surface existing metadata (`apps/mobile/src/trust/`)

Already extracted server-side, just not visible on cards. Quick win, no schema or backend changes.

- [x] **TN-V2-P1-001** — `subject_card.ts` — extend `SubjectCardDisplay` with optional `host` field; surface as a small chip on the card (e.g., `amazon.co.uk`, `jumia.ug`). Pure data layer + view update.
- [x] **TN-V2-P1-002** — `subject_card.ts` — extend with optional `language` field; render as a chip (`EN`, `ES-MX`) when known.
- [x] **TN-V2-P1-003** — `subject_card.ts` — for `place` subjects, render coordinates as a city/region label (reverse-geocode is a fallback later; for now just lat/lng truncated).
- [x] **TN-V2-P1-004** — `subject_detail.ts` — extend the header to show host + language + (for places) location. Same chips, larger.
- [x] **TN-V2-P1-005** — Tests: render coverage for the new chips (3 fixtures: host-only, language-only, both, place-with-coords). Satisfied by `subject_card.test.ts` (44 specs), `subject_card_view.test.tsx` (chip-row matrix + a11y), `subject_detail_data.test.ts` (header chip parity), `subject_detail.render.test.tsx` (chip render matrix on the detail surface).

## 2. Phase 2A — Viewer profile (Cluster A)

Local-only preferences. Keystore-resident. Never sent to AppView.

- [x] **TN-V2-CTX-001** — `apps/mobile/src/services/user_preferences.ts` — keystore schema for the local profile. Discriminated-union shape so future fields can be added without schema migrations: `{ region, budget, devices, languages, dietary, accessibility }`. Defaults derived from device locale on first read.
- [x] **TN-V2-CTX-002** — Settings → "Region" — picker with country list + "auto" (device locale). Stored as ISO 3166-1 alpha-2.
- [x] **TN-V2-CTX-003** — Settings → "Budget" — per-category optional. Three tiers: `$` / `$$` / `$$$`. Skipped categories impose no filter.
- [x] **TN-V2-CTX-004** — Settings → "Languages" — multi-select from BCP-47 list. Defaults to `[device-locale]`.
- [x] **TN-V2-CTX-005** — Settings → "Devices" — multi-select compatibility profile (iOS / Android / macOS / Windows / Linux / iPad / web). Affects `compat_tags` filtering.
- [x] **TN-V2-CTX-006** — Settings → "Dietary" — multi-select (vegan, vegetarian, halal, kosher, gluten-free, dairy-free, nut-free, …). Optional.
- [x] **TN-V2-CTX-007** — Settings → "Accessibility" — multi-select (wheelchair, captions, screen-reader, color-blind-safe, …). Optional.
- [x] **TN-V2-CTX-008** — `useViewerPreferences()` hook — subscribes to keystore, exposes `{ profile, isHydrated }`. Powers the filter/badge logic on every screen.

## 3. Phase 2B — Subject metadata enrichment (Cluster B)

Server-side: schema additions + enricher upgrades. New columns on `subjects.metadata` (a single JSONB bag per Plan §3.6.2 — NOT new top-level columns; we already enrich into that bag).

- [x] **TN-V2-META-001** — `subjects.metadata.availability` — `{ regions: ISO[], shipsTo: ISO[], soldAt: host[] }`. Optional reviewer-declared in the attestation; auto-filled by enricher from URL TLD when missing. **DONE (wire side).** Wire object `availability?: { regions?: string[]; shipsTo?: string[]; soldAt?: string[] }` added to `Attestation` lexicon type + Zod validator (each sub-field independently optional within the object; ISO 3166-1 alpha-2 regex on `regions` / `shipsTo`, RFC 1035 ≤253 char hostname bound on `soldAt`; cap 30 for region arrays, cap 20 for soldAt) + DB schema (3 separate `text[]` columns: `availability_regions`, `availability_ships_to`, `availability_sold_at` — denormalised because each sub-field has a distinct GIN array-overlap search shape; JSONB would force every "ships to X" / "sold at Y" search to traverse a path expression and skip the index) + ingester handler (each sub-field empty-array → NULL independently to keep the GIN indexes sparse). Migration `0009_attestation_availability_schedule.sql` is additive + idempotent (paired with META-004 since both land together). Validator covered by 11 specs (accept full / partial / empty-object; reject lowercase + 3-letter + digit region codes; reject > 30 regions / > 20 soldAt / > 253 char hostname / empty-string entries). Persistence + idempotent overwrite pinned by integration tests IT-ATT-002t/u/v/z. The `host_to_region` enricher (META-007) auto-fills `regions` from URL TLD on the SUBJECT side; the wire-side reviewer-declared override here is the second leg. Subject-level merge (reviewer-declared regions ∪ enricher-declared regions; consensus shipsTo across reviewers) is deferred to META-001's unified pipeline — that's the right place to design the merge rule for ALL META-* fields together rather than re-deciding per field.
- [x] **TN-V2-META-002** — `subjects.metadata.price` — `{ low_e7, high_e7, currency, lastSeenMs }`. CBOR-int convention same as `serviceArea.latE7/lngE7`. **DONE (wire side).** Wire object `price?: { low_e7, high_e7, currency, lastSeenMs }` added to `Attestation` lexicon type + Zod validator (cross-field refine `low_e7 ≤ high_e7`, ISO 4217 alpha-3 currency regex, `lastSeenMs` ≤ now + 5min skew, integer-only e7 columns to honour the AT Protocol no-floats CBOR rule) + DB schema (4 nullable columns: `price_low_e7 bigint`, `price_high_e7 bigint`, `price_currency text`, `price_last_seen_at timestamp` — denormalised so RANK-002's range-overlap predicate runs as integer comparisons over a partial composite b-tree `attestations_price_range_idx WHERE price_low_e7 IS NOT NULL`, not JSONB path traversal) + ingester handler (all 4 columns set/cleared as a tied group on insert + `onConflictDoUpdate`). `bigint` mode `'number'` is safe — at e7 scale, JS_MAX_SAFE_INTEGER covers prices up to ~$900T, far above any realistic item. Migration `0008_attestation_price.sql` is additive + idempotent. Validator covered by 12 specs (accept full + point-price + zero / reject reversed-range + negative + non-int low_e7 + lowercase/4-letter/digit-only currency + future/negative lastSeenMs + missing required fields). Persistence + idempotent overwrite (including currency change EUR→USD) pinned by integration tests IT-ATT-002q/r/s. Subject-level merge into `subjects.metadata.price` (most-recent wins? min/max consensus across reviewers?) is deferred to META-001's unified reviewer-declared metadata pipeline — that's the right place to design the merge rule for ALL META-* fields together rather than re-deciding per field.
- [x] **TN-V2-META-003** — `subjects.metadata.compat` — `tags: string[]` from a curated closed list (`ios`, `android`, `macos`, `windows`, `usb-c`, `lightning`, `110v`, `240v`, …). Open-ended list expanding by deliberate enrichment. **DONE (wire side).** Wire field `compat?: string[]` (≤15 entries × ≤50 chars per entry) added to `Attestation` lexicon type + Zod validator + DB schema (`compat text[]` with GIN index `attestations_compat_idx`) + ingester handler (collapses `[]` → NULL). Cap of 15 (vs 10 for compliance/accessibility) because devices legitimately check many compatibility boxes simultaneously — a modern laptop checks OS-family + bus + connector + wireless + power + display + audio standards easily into double digits. Migration `0007_attestation_compat.sql` is additive + idempotent. Closed-vocab list lives on the writer side; AppView indexes opaque tags so the vocabulary expands without redeploying the ingester. Validator covered by 6 specs (accept up-to-15 / single / empty; reject >15 / oversized / empty-string entries). Subject-level merge into `subjects.metadata.compat.tags` deferred to META-001's unified pipeline (same as META-005/006).
- [x] **TN-V2-META-004** — `subjects.metadata.schedule` — `{ hours: 7-day map, leadDays, seasonal: month[] }`. Optional. Reviewer-declared; auto-filled by enricher from JSON-LD `OpeningHours` markup where present. **DONE (wire side).** Wire object `schedule?: { hours?, leadDays?, seasonal? }` added to `Attestation` lexicon type + Zod validator (`hours` keyed by `z.enum(['mon','tue','wed','thu','fri','sat','sun'])` so a typo like `'monday'` fails at the gate, not silently on render; per-day value is `{ open, close }` each `HH:MM` 24-hour regex; `leadDays` integer 0–365; `seasonal` array of months 1–12 cap 12) + DB schema (single `schedule_json` JSONB column — heterogeneous shape per-day map + scalar + array gets no benefit from denormalisation; no current search predicate over the field). Migration `0009_attestation_availability_schedule.sql` (paired with META-001). Ingester handler collapses an empty `{}` (no sub-fields declared) to NULL so a future "schedule declared?" query works, but preserves partial schedules (e.g. `{ leadDays: 14 }` for a service that needs 2-week advance booking). Validator covered by 11 specs (accept full / partial-leadDays / partial-seasonal / subset-of-days; reject 25:00 hour / single-digit hour / unknown day code "monday" / negative leadDays / leadDays > 365 / month 0 / month 13). Persistence (full + partial preserved + empty-collapse + idempotent overwrite) pinned by integration tests IT-ATT-002w/x/y/z. The META-010 JSON-LD `OpeningHours` parser will auto-fill `hours` on the SUBJECT side; the wire-side reviewer-declared override here is the second leg. Subject-level merge deferred to META-001's unified pipeline.
- [x] **TN-V2-META-005** — `subjects.metadata.compliance` — `tags: string[]` (`halal`, `kosher`, `vegan`, `gluten-free`, `fda-approved`, `ce-marked`, `age-18+`, …). Reviewer-declared. **DONE (wire side).** Wire field `compliance?: string[]` (≤10 entries × ≤50 chars per entry) added to `Attestation` lexicon type + Zod validator + DB schema (`compliance text[]` with GIN array-overlap index `attestations_compliance_idx`) + ingester handler (collapses `[]` → NULL so the GIN stays sparse). Cap of 10 (vs 3 for `useCases`) because compliance is additive — one product is legitimately halal AND vegan AND gluten-free AND CE-marked AND fair-trade simultaneously, and the surfaces that render these (filter chips, detail-page badges) want all the badges. Migration `0006_attestation_compliance_accessibility.sql` is additive + idempotent. Validator covered by 7 specs (accept up-to-10 / single / empty; reject >10 / oversized / empty-string / non-string entries). Persistence + idempotent overwrite (including reviewer dropping a tag on amendment) pinned by integration tests IT-ATT-002n/o/p. **Subject-level merge into `subjects.metadata.compliance.tags` is deferred to META-001's unified reviewer-declared metadata pipeline** — that's the right place to design union/most-recent/consensus semantics for ALL META-* fields together rather than re-deciding per field.
- [x] **TN-V2-META-006** — `subjects.metadata.accessibility` — `tags: string[]` (`wheelchair`, `captions`, `screen-reader`, `color-blind-safe`, `audio-described`, …). **DONE alongside META-005 (parallel architecture).** Wire field `accessibility?: string[]` added to lexicon + validator + DB schema (`accessibility text[]` with GIN index `attestations_accessibility_idx`) + ingester handler. Same cap (10) and per-string bound (50) rationale as META-005 — accessibility is additive across dimensions (a venue can be wheelchair-accessible AND have captions AND audio description AND quiet hours). Validator covered by 6 specs (mirrors META-005 reject paths); persistence + idempotent overwrite covered by the same IT-ATT-002n/o/p tests that verify both fields together. Same META-001 deferral applies for the subject-level union step.
- [x] **TN-V2-META-007** — Enricher: `host_to_region.ts` curated map (TLD heuristic — `.uk` → `GB`, `.in` → `IN`, plus per-host overrides for amazon-* / etsy / shopify-* etc.). Sets `metadata.availability.regions` when not already set by reviewer. **DONE.** New pure module `appview/src/util/host_to_region.ts` with `hostToRegion(hostOrUrl) → string | null`. Two curated maps: 25 host overrides (Amazon storefronts by TLD, US-primary commerce hosts on `.com` like etsy/ebay/bestbuy/target/walmart, region-primary like flipkart.com → IN, rakuten.co.jp → JP, mercadolivre.com.br → BR) and 51 ccTLD entries covering Europe, Asia/Oceania, Americas, MEA. Override-first lookup so future ccTLD additions can never silently change a curated host's storefront answer. Returns `null` for unknown TLDs (no greedy `'US'` default — caller distinguishes "no signal" from "signal is US"). Wired into `subject_enrichment.ts` for `product` and `content` subjects via private helper `applyAvailabilityRegion(metadata, uri)` that sets `metadata.availability = { regions: [code] }` only when `availability` doesn't already exist (forward-compat with reviewer-declared override path landing in META-001). 25 unit specs in `tests/unit/host_to_region.test.ts` cover override priority, ccTLD coverage (`.uk` → GB pinned in its own test), edge inputs (null/empty/case/port/path/scheme/mobile-subdomain/trailing-dot), purity, and a soft budget pin so the maps don't bloat. Existing 63 `subject_enrichment.test.ts` specs still pass; 8 new specs there cover the wired-in availability output for product + content. Note: this also satisfies the test side of TN-V2-TEST-003 in full.
- [x] **TN-V2-META-008** — Enricher: ASIN region detection — `amazon.com` host implies US ASIN, `amazon.co.uk` implies UK ASIN, etc. Sets `metadata.availability` automatically. **DONE alongside META-007.** Audit verified the contract is fully satisfied by the META-007 chain: `host_to_region.ts` carries the complete Amazon storefront map (`amazon.com → US`, `amazon.co.uk → GB`, `amazon.de → DE`, `amazon.in → IN`, `amazon.co.jp → JP`, plus 11 more), and `subject_enrichment.ts:enrichProduct` calls `applyAvailabilityRegion(metadata, ref.uri)` after the identifier-parse step — so an ASIN-bearing product with an amazon URL gets BOTH `identifier_kind=asin` AND `availability.regions=[<region>]` set in a single pass. Three new specs in `subject_enrichment.test.ts` pin the combined ASIN+amazon.com → US, ASIN+amazon.co.uk → GB, and ASIN-without-URL → no-availability cases (the third deliberately conservative — same ASIN can exist on multiple storefronts, so we never default to US without a host signal). No new code needed; META-008 is structurally subsumed.
- [x] **TN-V2-META-009** — Enricher: OpenGraph price extraction — fetch `<meta property="product:price:amount">` from the subject's URL when reviewer hasn't declared price. Best-effort; rate-limited; fallback graceful. **DONE (parser side).** New pure module `appview/src/util/parse_open_graph_price.ts` exposes `parseOpenGraphPrice(html, { now })` which extracts a META-002-shaped `{ low_e7, high_e7, currency, lastSeenMs }` object from HTML or returns `null` when no usable signal is present. Two-priority extraction: (1) OpenGraph product meta tags `<meta property="product:price:amount">` + `product:price:currency` — Facebook's product-extension Open Graph spec, the convention Amazon/Walmart/Etsy/most retail platforms emit; (2) Schema.org JSON-LD `Product.offers.{price,priceCurrency}` and `AggregateOffer.{lowPrice,highPrice,priceCurrency}`. OG wins when both are present (deterministic). Currency normalised to ISO 4217 alpha-3 uppercase; symbol-only currencies (`$`, `€`) return null because they're ambiguous (`$` = USD/CAD/AUD/MXN/…). E7 conversion rounds via `Math.round(n * 1e7)` to prevent IEEE-754 imprecision (`0.30000000000000004 → 3_000_000`). European decimal-comma (`29,99`) handled. AggregateOffer `lowPrice > highPrice` (malformed range) gracefully falls through to point-price `price` when present. Pinned by 21 specs covering: input shape (4) / OG meta variations including quote style, attr order, decimal comma, symbol rejection, negative rejection, float-imprecision rounding (8) / JSON-LD fallback Offer + AggregateOffer + @graph + malformed-range + missing-currency + malformed-JSON (6) / OG-vs-JSON-LD priority + injectable `now` determinism + META-002 validator round-trip (3). Output round-trips cleanly through the META-002 Zod validator (the hard pin against schema drift between enricher and reviewer paths). Shared `extractJsonLdBlocks` extracted to `appview/src/util/json_ld_extract.ts` so META-009 + META-010 don't fork the regex. **The HTTP-fetch + integration into the subject-enrichment pipeline is downstream + deferred** — the parser is the load-bearing logic; the fetcher is a thin best-effort wrapper (rate limit, timeout, fallback graceful) that drops in without the parser changing.
- [x] **TN-V2-META-010** — Enricher: JSON-LD `OpeningHours` parser for `place` subjects. Falls through to reviewer-declared schedule if no markup. **DONE (parser side).** New pure module `appview/src/util/parse_json_ld_schedule.ts` exposes `parseJsonLdSchedule(html)` which extracts a META-004-shaped `{ hours: 7-day map }` from HTML's `<script type="application/ld+json">…</script>` blocks containing schema.org `openingHoursSpecification` markup. Day codes accept all three schema.org forms — URI (`http://schema.org/Monday`), long English (`Monday`), short (`Mon`) — and `dayOfWeek` accepts both string and array (range form). Time normalisation handles ISO 8601 variants: `HH:MM` (canonical), `HH:MM:SS` truncated, `HH:MM:SS.sss` truncated, compact `HHMM`. 12-hour AM/PM rejected because the META-004 wire contract is 24-hour only — the parser's null-fallthrough must match the validator's reject path so the round-trip is closed. Conflict policy: when the same day appears in multiple specs, the FIRST valid entry wins (deterministic + documented). Multi-block pages merged across all `<script>` blocks; one malformed block doesn't poison siblings. Pinned by 23 specs covering: input shape (4) / single-day variations including URI form + long form + short form + HH:MM:SS truncation + HHMM compact + 12-hour rejection + unrecognised day + 25:00 rejection (9) / multi-day variations including merge + array-day-form + first-wins conflict + missing-time-skips-entry + all-unrecognised-returns-null (5) / JSON-LD envelope variations including @graph + multi-block-merge + invalid-block-graceful + case-insensitive script type + META-004 validator round-trip (5). Output round-trips cleanly through the META-004 Zod validator. Shared `extractJsonLdBlocks` extracted to `appview/src/util/json_ld_extract.ts`. **The HTTP-fetch + integration into the subject-enrichment pipeline is downstream + deferred.**
- [x] **TN-V2-META-011** — Subject `lastActiveMs` — server-derived freshness signal. Updated when any new attestation lands on the subject. Powers the "stale" badge in §5. **DONE.** New nullable `last_active_at timestamp` column on `subjects` (DB schema + partial b-tree index `subjects_last_active_idx WHERE NOT NULL`). Migration `0005_subject_last_active.sql` is additive + idempotent. Attestation handler bumps the column via `GREATEST(last_active_at, recordCreatedAt)` — Postgres `GREATEST` skips NULL inputs so a fresh subject (NULL) gets the new value while a backdated replay leaves the column unchanged. Update fires only on `handleCreate`; `handleDelete` deliberately doesn't reset the freshness clock (retraction is the source-revocation system's concern, not this signal — the stale-review badge is "any attestation that ever landed", not "attestations currently live"). Pinned by 4 integration specs: IT-ATT-002j (first attestation populates), IT-ATT-002k (newer attestation bumps forward), IT-ATT-002l (backdated replay does NOT roll backward — the GREATEST contract), IT-ATT-002m (delete does NOT reset).

## 4. Phase 3 — Review-structure additions (Cluster C)

Lexicon additions + Write form. Existing V1 attestations remain valid (all new fields optional). Schema-validated by AppView record-validator.

- [x] **TN-V2-REV-001** — Lexicon: `com.dina.trust.attestation.useCases` — optional free-text from a per-category curated list (`everyday`, `professional`, `travel`, `kids`, …). Up to 3 tags. Enables use-case-aware ranking. **DONE.** Wire field `useCases: string[]` (≤3 entries × ≤50 chars; min-length-1 per entry to reject empty strings) added to `Attestation` lexicon type + Zod validator (`record-validator.ts`) + DB schema (`use_cases text[]` with GIN array-overlap index `attestations_use_cases_idx` matching `tags`' shape) + ingester handler (collapses `[]` → NULL so the GIN index doesn't carry zero-length rows). Migration `0002_attestation_use_case_last_used.sql` is additive + idempotent. Validator covered by 6 specs (accept up-to-3 / single / empty array; reject >3 / oversized / empty-string / non-string entries). Persistence pinned by integration test IT-ATT-002b (round-trip), IT-ATT-002c (empty + absent → NULL), IT-ATT-002d (idempotent overwrite on amendment). Closed-vocabulary discipline lives on writers (mobile data layer enforces per-category tables); AppView indexes opaque tags so the vocabulary can evolve without redeploying the ingester.
- [x] **TN-V2-REV-002** — Lexicon: `attestation.reviewerExperience` — optional enum `novice|intermediate|expert`. Self-declared. Future: scorer can weight expert reviews higher in technical categories. **DONE.** Wire field added to `Attestation` lexicon type + Zod validator (closed-enum) + DB schema (`reviewer_experience text` with partial b-tree index `attestations_reviewer_experience_idx WHERE NOT NULL` since "any tier" queries don't hit the index) + ingester handler. Stored as text rather than Postgres ENUM so future tier additions don't need an `ALTER TYPE`. Migration `0003_attestation_reviewer_experience_recommend_for.sql` is additive + idempotent. Validator covered by 4 specs (accept each tier, reject unknown / empty / non-string); persistence + idempotent overwrite pinned by integration tests IT-ATT-002e/f/g.
- [x] **TN-V2-REV-003** — Lexicon: `attestation.lastUsedMs` — optional ms-since-epoch when reviewer last interacted with the subject. Distinct from `createdAt` (review write time). Powers freshness even when the review itself is recent. **DONE.** Wire field `lastUsedMs: number` (integer ms — AT Protocol forbids floats in CBOR records) added to `Attestation` lexicon type + Zod validator (clock-skew guard mirroring `boundedIsoDate`: ≤ now+5min, ≥0). DB stores as Postgres `timestamp last_used_at` (handler converts integer ms → `Date` once at write time so search/scorer queries can use ordinary `WHERE last_used_at > NOW() - INTERVAL ...` instead of re-deriving on every read). Partial b-tree index `attestations_last_used_idx WHERE last_used_at IS NOT NULL` keeps the index small since most rows leave it absent. Validator covered by 7 specs (accept past / 0 / now; reject future / negative / non-integer / string). Persistence + idempotent overwrite pinned by integration tests IT-ATT-002b/c/d.
- [x] **TN-V2-REV-004** — Lexicon: `attestation.recommendFor` / `notRecommendFor` — optional string[] use-case tags. "Recommend for everyday writing; not for calligraphy." **DONE.** Both wire fields added to `Attestation` lexicon type + Zod validator (≤5 entries × ≤50 chars; min-length-1 per entry) + DB schema (`recommend_for text[]` + `not_recommend_for text[]` with separate GIN array-overlap indexes mirroring `attestations_use_cases_idx`) + ingester handler (collapses `[]` → NULL on both fields). The two lists are intentionally disjoint-of-each-other in spirit but the schema doesn't enforce it — overlap is semantically odd but a writer's call (the validator stays out of the writer's intent). Validator covered by 10 specs (accept up-to-5 / both lists / empty arrays; reject >5 / oversized / empty-string / non-string for each list). Persistence + idempotent overwrite (including reviewer dropping a list to `[]`) pinned by integration tests IT-ATT-002e/f/g. Cap of 5 chosen over 3 (used for `useCases`) because the recommendation use-case is read-time enumeration, not author-time tagging — readers want to know *all* the niches the reviewer endorses / warns against.
- [x] **TN-V2-REV-005** — Lexicon: `attestation.alternatives` — optional `SubjectRef[]` of other subjects the reviewer also tried. Enables "the reviewer also looked at X, Y" surfaces. Plan §6.3 conflict_chooser dovetails here. **DONE.** Wire field `alternatives?: SubjectRef[]` (≤5 entries; same shared `subjectRefSchema` bounds as the primary `subject` so readers navigating an alternative don't hit a SubjectRef the rest of the system can't render) added to `Attestation` lexicon type + Zod validator + DB schema (`alternatives_json jsonb` mirroring `evidence_json` / `mentions_json` / `related_attestations_json` shape). Cap of 5 matches mobile `MAX_REVIEW_ALTERNATIVES`. Not GIN-indexed: the access pattern is "hydrate on detail-page read", and reverse lookups would need a dedicated edge table (cf. `mention_edges`) not a JSONB GIN. Migration `0004_attestation_alternatives.sql` is additive + idempotent. Validator covered by 6 specs (accept up-to-5 / empty; reject >5 / unknown subject.type / malformed DID / oversized name). Persistence + NULL-collapse pinned by integration tests IT-ATT-002h/i. Full V2 envelope round-trip pinned by extended `accepts the full V2 envelope (all REV-001..005 fields)` test.
- [x] **TN-V2-REV-006** — Mobile: Write form — use-case picker. Per-category vocabulary table (`USE_CASE_BY_CATEGORY`: tech → everyday/professional/travel/gaming/creative; books → fiction/nonfiction/reference/kids/travel; restaurant → date_night/family/business/casual/late_night; etc.) with `USE_CASES_DEFAULT` fallback. Closed-vocabulary discipline: free-form tags + tags from other vocabularies are silently rejected by the `toggleUseCase(current, tag, vocabulary)` helper. Cap at `MAX_USE_CASES = 3`; unselected tags grey out when at cap (re-enable when user deselects). State: `useCases: readonly string[]` on `WriteFormState`. UI: chip row in Advanced section (collapsed by default alongside last-used picker). Tests: 21 data-layer specs (vocabulary lookup head-segment + lowercase + fallback, every tag has a label, toggle add/remove preserves order, cap rejection, closed-vocab rejection, immutability, idempotence) + 6 render specs (vocabulary visibility, tap-toggle, multi-select up to 3, cap-greyed-out, removal re-enables, a11y).
- [x] **TN-V2-REV-007** — Mobile: Write form — last-used bucket picker behind a collapsible "Advanced" section. Implemented as 6 coarse buckets (`today` / `past_week` / `past_month` / `past_6_months` / `past_year` / `over_a_year`) rather than a free-form date picker — answers the freshness question users actually have without demanding date precision they don't remember, and avoids a native date-picker dependency. Bucket → ms via `lastUsedMsForBucket(bucket, nowMs)` (midpoint resolution; `today` → nowMs verbatim). State: `lastUsedBucket: LastUsedBucket | null` on `WriteFormState`; null = "user didn't pick" → wire record omits `lastUsedMs`. UI: collapsed-by-default Advanced section, tap-to-toggle radio-like bucket pills, tap-twice clears. Tests: 15 data-layer specs (closed-enum sanity, monotonic ordering, midpoint resolution, purity, exhaustiveness) + 6 render specs (collapsed default, expand/collapse, tap-toggle, radio replacement, accessibility role + expanded state).
- [x] **TN-V2-REV-008** — Mobile: Write form — alternatives selector. State: `alternatives: readonly ReviewAlternative[]` on `WriteFormState`, capped at `MAX_REVIEW_ALTERNATIVES = 5`. Pure helpers: `addReviewAlternative` (trims name, drops empties, dedups by `subjectId` when present else `kind+name` case-insensitively, no-op at cap) + `removeReviewAlternative` (by index, no-op out-of-range). UI: chip list of currently-added entries with X-to-remove + search input + result rows. Search wired via optional `searchAlternatives?: (q: string) => Promise<readonly ReviewAlternative[]>` prop on `WriteScreenProps` — runner-side wiring of the actual search xRPC is a separate concern; the screen handles loading / error / already-added-disabled states. At cap the search input hides + a hint copy surfaces. Tests: 22 data-layer specs (add/remove + dedup + cap + immutability) + 9 render specs (input visibility, search trigger, result tap-to-add, chip remove, cap-hide-input, already-added disabled, search-error surface, no-prop graceful path).
- [x] **TN-V2-REV-009** — AppView: ingester handler accepts the new fields; record-validator schema covers them; enricher passes them through unchanged. **DONE alongside REV-001..005.** Audit verified: (1) Zod validator covers all 5 V2 fields with min/max/enum bounds; (2) jetstream-consumer.ts:540 hands `validation.data` (Zod-parsed object preserving all known fields) to the handler — no upstream transformer drops fields; (3) attestation handler reads + persists all 5 fields with empty-array → NULL collapse on tag fields and integer-ms → timestamp conversion on lastUsedMs; (4) enricher (`subject_enrichment.ts`) takes only `SubjectRef` not the full attestation, so attestation-level fields can't be touched by enrichment — pass-through is structural, not coincidental.
- [x] **TN-V2-REV-010** — Tests: write_form_data tests cover the new optional fields; record-validator tests pin the new schema; ingester unit tests cover both paths (with + without new fields). **DONE alongside REV-001..005 + REV-006..008.** Coverage: (1) mobile `write_form_data.test.ts` covers REV-006 useCases + REV-007 lastUsedBucket + REV-008 alternatives data-layer (76+ specs); (2) appview `record_validator.test.ts` covers all 5 wire fields with accept-with + reject-on-malformed (33 V2-specific specs across 5 describe blocks + 2 cross-field hardening specs from TEST-005); (3) integration `01-ingester-handlers.test.ts` covers persistence with-and-without via IT-ATT-002b/c (useCases + lastUsedMs round-trip + NULL collapse), 002d (idempotent overwrite), 002e/f/g (reviewerExperience + recommend lists round-trip + NULL collapse + idempotent overwrite), 002h/i (alternatives JSONB round-trip + NULL collapse), 002j/k/l/m (META-011 last_active_at — bonus coverage for the freshness signal that depends on this contract).

## 5. Phase 4 — Ranking, alternatives, and warnings (Cluster D)

The biggest lift. Closes the remaining gaps (5, 7, 9, 10, 11, 12).

### Search xRPC filters (small)

- [x] **TN-V2-RANK-001** — `search` xRPC: `viewerRegion` filter — bounded by `metadata.availability.regions` containment. Excludes subjects unambiguously unavailable in the viewer's region. **DONE.** New `viewerRegion` parameter on `SearchParams` validated as ISO 3166-1 alpha-2 (regex `/^[A-Z]{2}$/`). WHERE-clause predicate: `COALESCE(jsonb_array_length(metadata->'availability'->'regions'), 0) = 0 OR metadata @> {availability: {regions: [region]}}::jsonb`. Two-half logic for "missing-pass": rows with NULL availability path, missing regions key, or empty regions array all pass through (we don't penalise unknown). Rows with non-empty regions must contain the viewer's region or get excluded. Positive `@>` half can ride the existing `subjects_metadata_idx` GIN (jsonb_path_ops opclass); the COALESCE-jsonb_array_length half can't but the assembled WHERE narrows by category/sentiment/etc first. Schema-validation pinned by 5 unit specs (accept all alpha-2, reject lowercase / 3-letter / single-letter / digit). SQL-assembly pinned by 2 stub specs (predicate present when set, silent skip when undefined). Real-DB end-to-end semantics pinned by IT-API-020a (3-subject GB/US/missing case proves missing-pass + correct exclusion of GB-only for US viewer + correct exclusion of US+GB for FR viewer who only sees missing-availability subjects) and IT-API-020b (empty `regions: []` array treated as missing-pass — defensive against writers who supply availability scaffolding but no regions).
- [x] **TN-V2-RANK-002** — `search` xRPC: `priceRange` filter — `low <= max AND high >= min`. NULL price = include (we don't penalise unknown). **DONE.** Two new query parameters `priceMinE7` + `priceMaxE7` on `SearchParams` (`z.coerce.number().int().min(0)` to tolerate URL-string inputs, same convention as `limit` / `minReviewCount`) plus a cross-field `.refine` rejecting reversed ranges (`min > max` would silently produce an empty set after SQL translation — a 400 is more informative). Predicate is range-OVERLAP with missing-pass: `(price_low_e7 IS NULL OR (price_low_e7 <= max AND price_high_e7 >= min))`. Half-open ranges supported: only `priceMinE7` set → `price_high_e7 >= min`; only `priceMaxE7` set → `price_low_e7 <= max`. Missing-pass clause aligns with viewerRegion/minReviewCount precedent — we don't penalise unknown. The partial composite b-tree `attestations_price_range_idx (price_low_e7, price_high_e7) WHERE price_low_e7 IS NOT NULL` covers the non-NULL branch via leading-column range scan. Pinned by 7 schema-validation specs (accepts both bounds / coerces strings / accepts half-open / rejects negative + reversed / accepts point-query) + 4 stub WHERE-assembly specs (range with missing-pass, half-open lower, half-open upper, absent-no-predicate) + 2 real-DB integration specs (IT-API-020k: 5-row case proving inside/straddle-low/straddle-high/NULL all surface and below excludes; IT-API-020l: half-open + reversed-range schema rejection).
- [x] **TN-V2-RANK-003** — `search` xRPC: `compatTags` filter — array overlap with `metadata.compat.tags`. **DONE.** New `compatTags` query parameter on `SearchParams` (≤1000-char outer bound, ≤50 chars per parsed entry to match META-003 validator). Predicate: `attestations.compat && ARRAY[...]::text[]` — **OVERLAP** semantics via Postgres `&&`, intentionally NOT containment. The "either connector" intent is the only useful discovery query for compat ("show me devices supporting usb-c OR lightning"); containment would silently shrink the result set to "devices supporting both" — almost always empty. New internal helper `parseTagFilter(raw, label)` factored out of the existing `buildTagContainment` since RANK-003 + RANK-004 share the parse + bound-check but use different SQL operators. New `buildTagOverlap` reads through to the same parsed list with `&&` instead of `@>`. Pinned by 3 unit-stub specs (overlap operator present, NOT containment; empty silent-skip; > 50-char throws) and 2 integration specs: IT-API-020i (3-row case proving usb-c-or-lightning surfaces both single-tag rows and excludes neither, plus single-tag and no-match controls), IT-API-020j (single-row composition test pinning the OVERLAP-vs-CONTAINMENT semantic divergence between compatTags and dietaryTags on the same row).
- [x] **TN-V2-RANK-004** — `search` xRPC: `dietaryTags` / `accessibilityTags` filters — array containment. **DONE.** Two new comma-separated query parameters on `SearchParams` (≤1000-char outer bound for DOS guard, ≤50 chars per parsed entry to match the META-005/006 validator). Predicate: `attestations.compliance @> ARRAY[...]::text[]` (and same shape against `attestations.accessibility`) — **containment** semantics (must have ALL the listed tags), not overlap. Containment is the right call because users compose multiple requirements ("halal AND vegan AND gluten-free") and overlap would surface unintended results. Filters AND together when both set (composes naturally with the existing `AND ...` chain). Empty / whitespace-only entries silently dropped (`?dietaryTags=halal,,vegan` → `[halal, vegan]`); empty filter list skips the predicate entirely so the user gets unfiltered results rather than an empty set. Per-entry > 50 chars throws as a 400-shaped ZodError (matching the `tags` filter's bound-violation pattern). Internal helper `buildTagContainment(raw, column, label)` parameterises both filter paths with a single implementation; type widened to `AnyArrayColumn = typeof attestations.compliance | typeof attestations.accessibility` because Drizzle's strongly typed `PgColumn<...>` encodes the column name in the type and a concrete `typeof X` won't accept `Y`. Pinned by 4 new schema-validation specs + 5 new WHERE-assembly stub specs in `search_filters.test.ts`, plus 4 real-DB integration specs (IT-API-020e/f/g/h covering: containment-not-overlap on compliance, containment on accessibility, AND composition across both filters, no-match-returns-empty pinning the no-false-positives contract).
- [x] **TN-V2-RANK-005** — Mobile: filter chips on `/trust/search` driven by viewer profile from `useViewerPreferences()`. Off by default; user opts in per session. (Includes the chip-naming spec from RANK-016.)

### Score / ranking changes (medium)

- [x] **TN-V2-RANK-006** — Scorer: per-category recency-decay tuning. Tech moves fast (6mo half-life), books slow (5yr half-life), restaurants medium (1yr). Configurable in `appview_config`. **DONE.** New pure module `appview/src/scorer/algorithms/category_halflife.ts` exposes `halflifeForCategory(category, override?)` which returns a positive integer half-life (days) for any attestation `category`. Built-in `DEFAULT_CATEGORY_HALFLIFE_DAYS` map: tech 180 (phone/laptop/tech/software/ide), books 1825 (5yr — book is immutable), restaurants/cafes/bars 365 (1yr — chefs/menus rotate), hotels 730 (chains drift slower than indie), services 540 (1.5yr — personnel changes). Lookup priority: exact-match-first, then progressively shorter colon-separated prefixes (override beats default at SAME depth, more-specific beats less-specific across both sources). Defensive guards on override values (0 / -1 → ignored — would otherwise produce `exp(-x/0) = NaN` or `exp(-x/-1) = ∞`). Wired into `computeSentiment` via the new `category?: string | null` field on `TrustScoreInput.attestationsAbout` (optional for back-compat — legacy callers keep V1 180-day decay); the data loader in `refresh-profiles.ts` now pulls `attestations.category` and passes it through. Default fallback to `DEFAULT_HALFLIFE_DAYS` (180, the V1 baseline) for unmatched categories — no behaviour change for any caller until they start populating `category`. Pinned by 19 lookup specs (built-in map / fallback / prefix walk / override priority / defensive non-positive guard / casing / map invariants) + 11 property specs in TEST-009 (monotonicity in age / equivalence at equal age / cross-category ordering / canonical anchors / numerical safety). **`appview_config` DB surface is deferred** — the existing `trust_v1_params` table is (key, scalar number) and can't cleanly carry a per-category map; the override-map hook is forward-compatible so when a config surface lands, it's a one-line merge.
- [x] **TN-V2-RANK-007** — Scorer: viewer-region boost (NOT a filter — a sort tweak). Subjects available in viewer's region rank slightly higher; no penalty for unavailable so the user can still discover. **DONE.** When `viewerRegion` is set, search.ts prepends a `CASE WHEN subjects.metadata @> '{"availability":{"regions":[V]}}'::jsonb THEN 1 ELSE 0 END DESC` to the ORDER BY chain, so explicit region matches sort above subjects with no availability signal AND above subjects matching other regions (the latter only relevant when paired with viewerRegion-without-filter, e.g. an admin debugging surface). Implementation chooses primary-key bucketing rather than a numeric score bump because the existing search has no real ranking pipeline (sort=relevant uses ts_rank, sort=recent uses recordCreatedAt only). Two-branch `runQuery`: when `viewerRegion` is set we LEFT JOIN subjects (needed for the metadata reference); otherwise the V1 query plan is unchanged — keeps the no-boost hot path identical to baseline. Pinned by 1 unit-stub spec (CASE WHEN + region payload + DESC keyword present) and 2 integration specs: IT-API-020c (GB-match ranks above unknown when both share the same recordCreatedAt — isolates the boost), IT-API-020d (the documented "boost overrides recency" contract — old GB-match outranks fresh no-availability for a GB viewer; without `viewerRegion`, recency wins). Caveat: cursor encoding remains recordCreatedAt-based and pagination past the first page with `viewerRegion` set has the same latent inconsistency as `sort=relevant` already does — first-page UX is correct.
- [ ] **TN-V2-RANK-008** — Scorer: personalisation hook — taste-profile match between viewer's prior reviews and the candidate reviewer's history boosts that reviewer's weight. Defer to a later phase if the privacy story isn't crisp (computing taste-profile match without leaking either party's reviews to AppView is non-trivial — likely needs ZK proofs which are V3+).

### New surfaces (medium)

- [x] **TN-V2-RANK-009** — New xRPC: `getAlternatives(subjectId, count, viewerCtx?)` — top-N trusted alternatives in the same category. Powers the "3 trusted alternatives" strip on subject detail. **DONE.** New handler `appview/src/api/xrpc/get-alternatives.ts` registered in `server.ts` as `com.dina.trust.getAlternatives` with rate-limit tier 120 RPM (mirrors `subjectGet` since it's rendered alongside on the same surface). Two-phase query: (1) PK lookup of focus subject's category, (2) LEFT JOIN `subjects ⨝ subject_scores` filtered by `category = focus.category AND id != focus.id`, ordered by `subject_scores.weighted_score DESC NULLS LAST` with `subjects.id DESC` as a deterministic tiebreaker. LEFT JOIN preserves unscored subjects so fresh categories return alternatives even before the scorer ticks. Defaults: `count=3`, max `25`. `viewerDid` accepted on the schema for forward-compat (viewer-aware ranking later) but not yet consumed. Empty-result edge cases — unknown subjectId, subject without category, focus in unique category — all return `{alternatives: []}` rather than 404, so the mobile UI suppresses the strip uniformly. Self-exclusion always wins over trust score (an even-higher-scored focus subject must NOT appear in its own alternatives).
- [x] **TN-V2-RANK-010** — New xRPC: `getNegativeSpace(viewerDid, category)` — flagged-by-1-hop subjects in a category. Powers the proactive warning surface. **DONE.** New handler `appview/src/api/xrpc/get-negative-space.ts` registered in `server.ts` as `com.dina.trust.getNegativeSpace` with rate-limit tier 60 RPM (lower than `getAlternatives` since this surface is rendered once per category browse, not per detail view). Two-phase pipeline: (1) `computeGraphContext(viewer, depth=1)` materialises the 1-hop set; if empty, return `{subjects: []}` immediately. (2) `flags ⨝ subjects` filtered by `authorDid IN <1-hop>`, `category = ?`, `isActive = true`. JS-side bucketing collapses by `subjectId`, tracks distinct flagger DIDs (the `flaggerCount`), rolls up severity via a closed-rank table (critical=0..informational=3 with min-wins so a single critical flag dominates over softer flags on the same subject), and tracks the most-recent flag timestamp. Rank: `flaggerCount DESC` then `lastFlaggedAt DESC` — consensus matters most, recency tiebreaks. Self-flag exclusion is structural (the viewer is never in `oneHopDids`). 2-hop flaggers excluded by depth=1 traversal. Defaults: `limit=10`, max `50`. **viewerDid is mandatory** because the whole notion of "negative space" is viewer-relative; without it there's no meaningful 1-hop set.
- [x] **TN-V2-RANK-011** — Mobile: subject card + detail — recency badge ("3 years old") when category-tuned half-life threshold is exceeded. Implemented as `deriveRecencyBadge(category, lastActiveMs, nowMs)` in `subject_card.ts` with per-category threshold table (`RECENCY_THRESHOLDS_MS`: tech 1y, books 5y, restaurant 1y, office_furniture 3y, default 2y). Wired into `SubjectCardDisplay.recency` + `SubjectDetailHeader.recency` via the new `SubjectCardContext` / `SubjectDetailContext` arg, rendered as a "warning" chip first in the chip row, included in the a11y label. Tests: 11 data-layer specs (thresholds + formatting + future-dated guards), 2 view specs, 3 a11y specs, 4 detail-data specs, 3 detail-render specs.
- [x] **TN-V2-RANK-012** — Mobile: subject card + detail — region pill ("📍 UK only") when `availability.regions` doesn't include viewer region. Implemented as `deriveRegionPill(availabilityRegions, viewerRegion)` in `subject_card.ts`. ISO 3166-1 alpha-2 case-insensitive match; "missing field = pass" preserved when availability OR viewer region is absent. Multi-region formatting: 1-3 listed verbatim, 4+ compressed to "+N". Detail screen wires `viewerProfile?.region` from `useViewerPreferences()` (keystore-resident; never reaches the wire — Loyalty Law). Tests: 14 data-layer specs (null/inclusion/multi-region), 2 view specs, 2 a11y specs, 4 detail-data specs, parity tests pinning card↔detail contract.
- [x] **TN-V2-RANK-013** — Mobile: subject card + detail — price-range chip (`$`/`$$`/`$$$`) when `metadata.price` is known. Wired through `subject_card.ts::normalisePriceTier`, rendered as the fourth context chip on `SubjectCardView` + the detail header chip row, included in the a11y label after location, with parity tests pinning card↔detail contract.
- [x] **TN-V2-RANK-014** — Mobile: subject detail — "3 trusted alternatives" strip below the review list. Implemented as `deriveAlternatives(raw, currentId)` in `subject_detail_data.ts` plus `<AlternativesStrip>` component in the detail screen. Strip renders compact band-stripe cards (band derived from `subjectTrustScore` via shared `trustBandFor`); tap → router-pushes the alternative's detail page. Defensive guards: drops null/empty entries, drops self (matches `subjectDid`/`subjectIdentifier`/`subjectUri`), de-duplicates by subjectId, caps at `MAX_ALTERNATIVES = 3`. Tests: 22 data-layer specs + 5 detail-render specs covering hide-when-empty, singular vs plural header, band stripe per rated alternative, cap enforcement.
- [x] **TN-V2-RANK-015** — Mobile: subject detail — flag-warning banner ("2 of your contacts flagged this brand"). Implemented as `deriveFlagWarning(summary)` in `subject_detail_data.ts` with scope→noun mapping (`subject`→"product", `brand`→"brand", `category`→"category"). Banner renders ABOVE the chip row (highest-priority safety signal), warning-tinted background with hairline warning border + alert icon. Banner-or-nothing posture: silent for count=0 to avoid reassurance theatre. Defensive guards: non-finite/negative count → null, non-integer count floors, runaway count clamps to 1M ceiling, unknown scope falls back to generic "subject" noun. Always-plural prepositional phrasing ("1 of your contacts flagged this..."). Tests: 18 data-layer specs + 4 detail-render specs covering shown/hidden/count=1/count>1/all three scope nouns.
- [x] **TN-V2-RANK-016** — Mobile: search filter chips — "in my region" / "in my budget" / "compatible with my devices". Off by default. (Implemented alongside RANK-005 in `viewer_filters.ts`; chips for region/budget/devices/dietary/accessibility are stubbed with `isApplicable: false` until corresponding META-* server tasks land — language-only is functional today.)

## 6. Tests

- [x] **TN-V2-TEST-001** — `apps/mobile/__tests__/services/user_preferences.test.ts` (31 specs across `defaultPreferences — device-locale derivation`, `loadUserPreferences — first read`, `saveUserPreferences — round-trip`, `forward-compat: missing fields take per-field defaults`) + `apps/mobile/__tests__/hooks/useViewerPreferences.test.tsx` (8 specs covering hydration, save, mutate, race-safety). Pins: locale-derived defaults, locale-case canonicalisation, bare-language fallback, save/load round-trip, forward-compat per-field defaults, hook-level isolation between renderers.
- [x] **TN-V2-TEST-002** — `apps/mobile/__tests__/trust/subject_card.test.ts` — extended end-to-end with all V2 chip variants. 105 specs covering: host chip (P1-001), language chip (P1-002), location chip (P1-003), price tier (RANK-013), recency badge (RANK-011) including per-category thresholds + future-dated guards + threshold-table sanity, region pill (RANK-012) including null/inclusion/multi-region/non-string-entries defensive guards, plus chip-independence + parity tests pinning the card↔detail contract.
- [x] **TN-V2-TEST-003** — `appview/tests/unit/host_to_region.test.ts` — TLD heuristic + per-host override map. **DONE alongside META-007.** 25 specs covering override-priority semantics (amazon.com → US locks in independently of TLD), all curated ccTLDs (`.uk` → GB pinned in its own test for the country/TLD-mismatch bug class), edge inputs (null / undefined / empty / case-insensitive / scheme-stripping / port-stripping / path-stripping / mobile subdomain / trailing dot), purity, and a soft map-size budget pin so the maps don't quietly bloat. The deliberate "no greedy default to US for unknown `.com`" contract is pinned so future drift surfaces immediately.
- [x] **TN-V2-TEST-004** — `appview/tests/unit/subject_enrichment.test.ts` — extend with availability/price/schedule paths. **DONE.** Two phases of work: (1) wired the META-009 + META-010 parsers into `enrichSubject` via a new `EnrichmentOptions` bag (`{ html?: string }`) — pre-fetched HTML is the integration point future orchestration layers (rate-limited fetcher, cache, fallback) plug into; the enricher itself stays pure (no I/O). Type-segregated wiring: `applyPrice` invoked only for product subjects, `applySchedule` only for place subjects (products don't have hours, places aren't priced like products). Don't-overwrite-existing guard on both fields so a future caller-supplied or reviewer-merged value isn't clobbered by an enricher fallback. (2) Extended `subject_enrichment.test.ts` with 15 new specs across 3 describe blocks: META-009 product price (OG meta + JSON-LD AggregateOffer + no-html-no-price + html-no-markup-no-price + cross-type-no-leak + cohabitation with availability.regions); META-010 place schedule (JSON-LD OpeningHours + no-html-no-schedule + html-no-markup-no-schedule + cross-type-no-leak + cohabitation with place_type segment); html-option determinism (structural equality across consecutive calls, modulo `lastSeenMs` drift acknowledged in test comment — a future orchestration layer pinning fetch time once will let this tighten to strict equality) + frozen-output preservation with html-derived fields. Total file goes from 73 specs (V2 baseline) to 88. Note: the existing 8 availability-path specs (IT-EXT-* covering `metadata.availability.regions` from host_to_region for product + content) were already in place from META-007 / earlier iterations; this iteration adds the price + schedule legs and the "options bag" plumbing they ride.
- [x] **TN-V2-TEST-005** — `appview/tests/unit/record_validator.test.ts` — extend with the new optional attestation fields (accept-with + reject-on-malformed). **DONE.** All 5 V2 attestation fields have dedicated `describe` blocks with full accept-with + reject-on-malformed coverage: useCases (6 specs), lastUsedMs (7 specs), reviewerExperience (4 specs), recommendFor / notRecommendFor (10 specs symmetrically), alternatives (6 specs reusing shared `subjectRefSchema` bounds). Plus 2 hardening specs added in this iteration: a regression guard pinning that the V1 minimal record still round-trips (proves every V2 field is genuinely optional), and a multi-field-malformed reject test that pins Zod's error-aggregation contract — flips a bug class where a future refactor swapping `.extend()` for `.merge()` (or vice versa) would silently drop error paths. Plus a full V2 envelope round-trip combining all fields. 101 specs total in the file, all green.
- [x] **TN-V2-TEST-006** — `appview/tests/integration/` — search filter tests for `viewerRegion`, `priceRange`, `compatTags`. **DONE.** `viewerRegion` covered by IT-API-020a/b/c/d (4 specs from RANK-001/007 work). `dietaryTags` + `accessibilityTags` covered by IT-API-020e/f/g/h (4 specs from RANK-004 work). `compatTags` covered by IT-API-020i/j (2 specs from RANK-003 work — overlap-vs-containment semantic divergence pinned). `priceRange` covered by IT-API-020k/l (2 specs from RANK-002 work — full overlap + NULL missing-pass + half-open + reversed-range schema rejection). All four V2 filter families now have integration coverage against a real Postgres.
- [x] **TN-V2-TEST-007** — `appview/tests/integration/` — `getAlternatives` xRPC. **DONE alongside RANK-009.** 12 specs in `tests/integration/10-api-endpoints.test.ts` under `§10.X getAlternatives Endpoint`: IT-ALT-001 (trust-desc ranking on same category), IT-ALT-002 (self-exclusion even when focus has the highest score), IT-ALT-003 (cross-category exclusion — a higher-scored subject in a different category must NOT leak through), IT-ALT-004 (count parameter respected at 3 and 5), IT-ALT-005 (NULL trust scores sort below scored — pins the NULLS LAST contract), IT-ALT-006 (unknown subjectId → empty), IT-ALT-007 (subject without category → empty), IT-ALT-008 (focus in unique category → empty), IT-ALT-009/a..d (schema: count default of 3, accepts optional viewerDid, rejects count>25, rejects count=0, coerces string-to-int from URL params). Each case isolated with unique subject IDs derived from `makeSubjectId(label)` so concurrent / rerun isolation is preserved.
- [x] **TN-V2-TEST-008** — `appview/tests/integration/` — `getNegativeSpace` xRPC. **DONE alongside RANK-010.** 12 specs in `tests/integration/10-api-endpoints.test.ts` under `§10.Y getNegativeSpace Endpoint`: IT-NEG-001 (1-hop flagger surfaces; 2-hop / stranger excluded; cross-category excluded), IT-NEG-002 (consensus ranking — 3-flagger subject above 1-flagger), IT-NEG-003 (highest severity wins per subject — `critical` dominates `informational` even when both are present), IT-NEG-004 (self-flag exclusion — viewer's own flags don't appear in their own warning surface), IT-NEG-005 (revoked / `isActive=false` flags don't contribute), IT-NEG-006 (lonely viewer with empty 1-hop graph → empty response), IT-NEG-007 (2-hop flagger excluded — pinning the 1-hop-only contract that makes this a high-trust signal), IT-NEG-008 (limit parameter caps result size), IT-NEG-009/a..d (schema: defaults limit=10, rejects non-DID viewer, rejects empty category, rejects limit>50). New helper `insertFlagBy(uri, authorDid, subjectId, opts)` lets each test control the flagger DID per row — the existing `insertFlag` hardcodes `did:plc:flagger`, which is wrong for negative-space tests where the flagger DID drives the 1-hop graph membership.
- [x] **TN-V2-TEST-009** — Property test: per-category recency-decay monotonicity (older = lower decay weight; same age = same weight). **DONE.** New file `appview/tests/unit/recency_decay_monotonicity.test.ts` (11 specs, deterministic mulberry32 seed, 200 iterations per property) pins four algebraic invariants of the recency-decay model: (1) strict monotonicity in age — for any fixed category, age_old > age_young ⇒ recency(old) < recency(young), no ties at boundary, no inversions, no NaN/Infinity; canonical anchors (age=0 → recency=1.0; age=halflife → recency=exp(-1) ≈ 0.368) pinned; negative ages clamp to 0 (no future-dated boost); (2) equivalence at equal age — same age + same halflife ⇒ byte-identical recency; categories sharing the same halflife (e.g. phone+laptop both at 180) produce strictly equal output for any age; (3) cross-category ordering — books decay slower than tech at every positive age; restaurants between tech and books; full-map sweep verifies halflife monotonicity is preserved for every pair of distinct halflife entries; (4) numerical safety — recency stays finite for very old ages (10,000 years) and very long halflifes (10,000-year override). The tests isolate the `recencyFactor(age, halflife) = exp(-max(0,age)/halflife)` math so a future refactor of `computeSentiment` plumbing won't bury the property check; if the scorer's formula changes, these tests break intentionally.

## 7. Documentation

- [x] **TN-V2-DOCS-001** — `docs/trust-network/V2-actionability.md` — public-facing explainer of the actionability layer (the 12 gaps, the 4 clusters, what users see in the UI).
- [x] **TN-V2-DOCS-002** — `docs/trust-network/threat-model.md` — extend §7 ("V1 does NOT promise") with V2 promises and the new privacy surface (viewer profile is keystore-resident; never sent to AppView; filtering is mobile-side).
- [x] **TN-V2-DOCS-003** — `docs/trust-network-walkthrough.md` — extend with §11 "V2 actionability layer" pointing at the new cluster A/B/C/D shape. Companion-doc list updated to point at `V2-actionability.md` + the V2 backlog; new "Where to go next" row points at the V2 layer; §11 itself sketches the four clusters, shipped-vs-open status, and the data-layer / view-layer / screen-wiring pattern that the V1 chips followed and V2 surfaces inherit.

---

## Total: 56 tasks across 7 sections

| Section | Count |
|---|---|
| 1 Phase 1 (surface existing) | 5 |
| 2 Phase 2A — Viewer profile | 8 |
| 3 Phase 2B — Subject metadata | 11 |
| 4 Phase 3 — Review structure | 10 |
| 5 Phase 4 — Ranking + surfaces | 16 |
| 6 Tests | 9 |
| 7 Docs | 3 |
| **Total** | **62** |

## Working agreement

- Same as V1: pick one task at a time. `[-]` when starting, `[x]` when merged. Block? `[!]` + one-line reason.
- Phase 1 first — surface existing metadata buys credibility for the V2 framing before the larger schema work lands.
- Phase 2A and 2B can land in parallel (one is mobile-only, one is server-only).
- Phase 3 lexicon additions need a `@dina/protocol` minor bump and a conformance-vector regen — coordinate with the protocol changelog.
- Phase 4 personalisation (TN-V2-RANK-008) has a privacy gate — defer if the ZK story isn't ready.
- New tasks discovered mid-flight: append to the relevant section with the next sequential ID.
