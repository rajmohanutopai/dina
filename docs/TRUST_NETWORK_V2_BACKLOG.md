# Trust Network V2 — Backlog

Nothing to do. V2 work closed; the only remaining item (TN-V2-RANK-008, taste-profile personalisation) is deferred to V3+ behind a ZK-proof gate (computing taste-profile match without leaking either party's reviews to AppView violates Loyalty Law without crypto support that doesn't exist yet).

If a `/loop` cron is iterating against this file, **delete the cron**.

Historic backlog content is in git history.
