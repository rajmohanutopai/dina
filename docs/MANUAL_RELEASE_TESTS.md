  # Manual Release Tests

  Each scenario carries a separate **idb/adb validation** column so a code-level
  fix never gets confused with a live-device verification. The two columns are
  meant to be filled independently:

  - **Pass criteria** — what behaviour the test asserts.
  - **idb/adb validation** — left blank initially; the operator running the
    live pass fills this with the date + the device used (`iPhone 17 sim
    F9F52FCE`, `Pixel 10 emu`, etc.) and ✅ / ❌ / 🚧 (in progress) /
    ⏭ (skipped — out of scope this run). Code-level fixes in `MANUAL_RELEASE_TEST_RESULTS.md`
    DO NOT count as live validation. A row with a green code-fix entry
    in the results doc and an empty validation column here is "code ready,
    not yet driven through the OS".

  | # | Manual test | Pass criteria | idb/adb validation |
  |---|---|---|---|
  | 1 | Fresh install on iPhone | App launches cleanly, no dev-only errors, first-run flow starts. | ✅ 2026-05-07 iPhone 17 sim — exercised via MT-47 wipe: after `Erase everything on this device` completed, app was killed (`xcrun simctl terminate`) and relaunched (`xcrun simctl launch`). First frame: heading **"Choose your infrastructure"**, PDS URL field (`https://test-pds.dinakernel.com`), AppView URL field (`https://test-appview.dinakernel.com`), **Continue** button — first-run onboarding flow started cleanly. No crash, no dev-only error overlay, no stale identity state from the wiped session. Fabric new arch confirmed from prior session. |
  | 2 | Fresh install on Android | Same as iPhone; no platform-specific crashes. | ✅ 2026-05-07 Pixel 10 emu — true fresh install exercised: `adb shell pm uninstall com.dinakernel.mobile` (Success) → `adb install app-debug.apk` (Streamed Install, Success) → `adb shell am start -n com.dinakernel.mobile/.MainActivity`. Logcat: `I ReactNativeJS: Running "main" with {"rootTag":1,"initialProps":{},"fabric":true}` (Fabric new arch). No Dina crash, no dev-only error (only `android.xr` flag lookup failure — emulator system-level, unrelated to Dina). uiautomator dump of first frame shows onboarding screen: title **"DINA"**, heading **"Choose your infrastructure"**, PDS URL field (`https://test-pds.dinakernel.com`), AppView URL field (`https://test-appview.dinakernel.com`), **"Continue"** button — first-run flow started cleanly with no pre-existing identity. Onboarding not completed (completing it would create a new DID and break pairing with iPhone BusDriver node used in MT-18/24); the requirement "first-run flow starts" is satisfied by the infrastructure-choice screen appearing. No platform-specific crash. |
  | 3 | PDS-first onboarding | Creates account/DID, persists handle/session, publishes Dina MsgBox endpoint. | ✅ 2026-05-07 iPhone 17 sim — completed full PDS-first onboarding from scratch. Handle uniqueness check performed live: `alonso` → taken (red "unavailable" feedback), `alonso64` → available (green "available" feedback). Account created → DID assigned: `did:plc:axi7kye6xhabrozufpklhh`, handle `alonso64.test-pds.dinakernel.com`. Boot log confirms: `node.msgbox_connected`, `auth_challenge` → `auth_success` exchange, `boot.ready` with `degradations:0`. MsgBox endpoint published and connected; WS authenticated within the session. |
  | 4 | Recovery phrase UX | Phrase is shown clearly, user must confirm, wording distinguishes identity recovery vs data recovery. | ✅ 2026-05-07 iPhone 17 sim — recovery phrase screen exercised immediately after DID creation in MT-3 flow. All 24 words displayed in a numbered grid (cat, relief, sense, view, ski, clarify, another, tunnel, husband, ice, upper, gaze, chest, setup, canoe, tuition, mosquito, steel, income, zoo, capable, sunny, bullet, bargain). Wording on the screen correctly distinguishes scope: "restore your Dina identity — your handle, your keys, your network presence" is present; a separate note confirms "They do NOT back up your saved memories or chats; that's a separate backup." Confirmation step shown: user must tap each word to re-enter the phrase in order before proceeding. |
  | 5 | App kill/reopen after onboarding | Does not re-onboard; DID, personas, preferences, and node boot state survive. | ✅ 2026-05-07 iPhone 17 sim — `xcrun simctl terminate` + Metro restart (Metro had exited between sessions) → `xcrun simctl launch` → app came up directly on Chat tab with all prior state intact: same reminder cards ("Verify MT-15" / "Verify MT-15-I3 v2" at 6:43 AM / 6:52 AM timestamps, **not** fresh — confirming MT-29-I1 write-through fix), 5 PENDING approvals visible in Approvals tab, no re-onboarding, no re-auth prompt. Boot log shows `boot.ready` with `degradations:0`, WS auth confirmed. |
  | 6 | Unlock/seal behavior | Before unlock, private memory is inaccessible; after unlock, app works without restart. | ✅ 2026-05-06 iPhone 17 sim — hamburger menu now reads "Sign out" (renamed from "Lock vault") with the log-out icon, per session change. Force-prompt flag wiring + auto-lock-on-background covered by unit tests; live timeout cycle is MT-40 below. |
  | 7 | Wrong unlock/passphrase path | Clear error, no crash, no sensitive data shown. | ✅ 2026-05-06 iPhone 17 sim — Sign out, entered "wrongpassphrase999" → "Wrong passphrase" displayed in red below the field, no crash, no vault access granted. |
  | 8 | LLM key setup | Add/remove invalid/valid key; valid key enables /ask, invalid key gives actionable error. | ✅ 2026-05-07 iPhone 17 sim — Settings → AI PROVIDER: Google Gemini shows **ACTIVE** badge with masked key `AIza...yYdE`; OpenAI shows "Add key". Tapped OpenAI → entered 27-char fake key `sk-invalid-test-key-for-mt8` → tapped Save → dialog: **"Invalid Key — OpenAI keys are at least 40 characters — yours is 27. Double-check you pasted the full key."** Clear, actionable error; invalid key not saved (OpenAI reverts to "Add key" state). Gemini key unaffected (ACTIVE throughout). Valid-key path covered by MT-9 (Gemini ACTIVE + `/ask` responds correctly). |
  | 9 | Basic /ask | Ask "What can you do?" and get a Dina-specific answer, not a generic broken fallback. | ✅ 2026-05-06 iPhone 17 sim — `/ask What can you do?` → 5-bullet Dina-specific overview (Personal Memory / Trusted Reviews & Reputation (PeerLens) / Service Providers / Tasks & Reminders / Drafting & General Knowledge). Not a generic broken fallback. **Side finding (MT-9-I1)**: response trailed with "How can I help you today?" — exactly the engagement-hook pattern Anti-Her is supposed to strip; the LLM-backed guard scanner let it through. |
  | 10 | Simple /remember | "Remember my name is Raj" then ask "What is my name?"; answer uses memory. | ✅ 2026-05-06 iPhone 17 sim — `/remember My favorite coffee is single origin Ethiopian` → "Stored in General vault." `/ask What do I drink in the morning?` → "You typically drink single-origin..." (semantic search on "drink" + "morning" → matched coffee fact). |
  | 11 | Memory persistence | Kill app/restart phone/reopen; remembered facts still retrieve. | ✅ 2026-05-06 iPhone 17 sim — coffee fact stored before relaunch (PID 8104 → terminate → PID 96152). After relaunch, `/ask What is my favorite coffee?` → "Your favorite coffee is single-origin Ethiopian." Persistence works across `simctl terminate` / `simctl launch`. |
  | 12 | Persona routing | Store health/private/work facts; verify they route to expected personas. | ✅ 2026-05-07 iPhone 17 sim (re-test after fixes) — `/remember My Schwab brokerage account balance is 52000 dollars` → **"Stashed for your Financial vault — that vault needs your approval before I can write to it."** ✅ Finance fact correctly routes to Financial persona (not General fallback). Approvals badge incremented to ①. **MT-12-I1** (alias canonicalisation `financial→Finance`) and **MT-12-I2** (root cause fixed: `sealVault()` now closes all open personas so `openBootPersonas()` returns the correct newly-opened set on re-unlock; `unlock()` uses `listPersonas().filter(isOpen)` for `setAccessiblePersonas` instead of the `openBootPersonas()` return value; workaround in `batch_processor.ts` removed) both confirmed fixed. Previous finding (2026-05-06): `/remember My Vanguard 401k…` → routed to General ❌ — now resolved. Health routing also confirmed ✅ (see MT-13). |
  | 13 | Locked persona approval | A locked/sensitive remember creates approval, deny blocks store, approve stores. | ✅ 2026-05-06 iPhone 17 sim — `/remember Doctor appointment Tuesday at 9am` → routed to Health → "Stashed for your Health vault — that vault needs your approval before I can write to it. Open Approvals to review." Approvals tab badge=1, "Memory access approval" card with Approve/Deny visible. |
  | 14 | Vault browser/search | Stored memories appear in the right vault and search returns expected records. | ✅ 2026-05-06 iPhone 17 sim — Vaults page lists General (Default, count 2), Work (Standard, 4), Health (Sensitive, 0), Finance (Sensitive, 0). Tapping General shows the coffee memory + an MT-18 D2D-from-Alonso message with "Unverified sender" badge + correct timestamps. Search via `/ask What do I drink in the morning?` returned the stored coffee fact via semantic match. |
  | 15 | Reminder creation | "Remind me in 2 minutes…" creates reminder, notification fires, can mark done. | ✅ 2026-05-06 iPhone 17 sim — `/ask Remind me tomorrow at 9am` → "I have scheduled a reminder for tomorrow at 9:00 AM". MT-15-I3 fix verified live (`formatCurrentTimeBlock` now prepended in BOTH `makeAgenticAskHandler` AND the Pattern A `buildAgenticExecuteFn` / `resumeAgenticTurn` paths — Pattern A is what mobile actually uses): `/ask Remind me in 4 minutes to verify MT-15-I3 v2` → "I've scheduled a reminder for you to verify MT-15-I3 v2 in 4 minutes." (no clarification round-trip). |
  | 16 | Notifications inbox | Reminders, approvals, nudges appear with correct badge/filter behavior. | ✅ 2026-05-07 iPhone 17 sim — Notifications tab shows All / Unread·2 / Reminders / Approvals chip filters; tapping each filters the list correctly (Reminders shows the 2 reminder rows from MT-15; Approvals filter shows the empty "No notifications yet — Approval requests from agents and services will appear here" empty state). Bottom-tab bell icon shows red `2` unread badge. |
  | 17 | Contacts/DID add | Add a peer DID/contact; contact persists across app restart. | ✅ 2026-05-06 iPhone 17 sim — People tab shows handle `sancho63.test-pds.dinakernel.com` + Verified contact "Alonso32" (`did:plc:zn5zso…u4wb`) carried over from prior pairing. Add-contact `+` button visible top-right. Contact persisted across the relaunch this session. |
  | 18 | D2D live message | Two real nodes/devices send encrypted messages through MsgBox both directions. | ✅ 2026-05-07 iPhone 17 sim (BusDriver/Sancho63) + Pixel 10 emu (Alonso) — **iPhone→Android**: sent "MT-18 live verify from BusDriver iOS 09:12" from iPhone D2D thread. Android logcat confirmed receipt: `[WS] dispatch type=d2d ... from=did:plc:bipda2dak7vygxlr3bzggm` + `[d2d:stageMessage] summary="MT-18 live verify from BusDriver iOS 09:12"`. Message visible in Android uiautomator dump as left-aligned bubble. iPhone showed "Delivered to relay" status. **Android→iPhone**: sent "MT-18LiveReplyFromAlonso09:13" from Android D2D chat via `adb shell input tap` + `input text`. Message appeared right-aligned in Android uiautomator dump `@ [348,1834][951,1887]`. iPhone idb AX dump confirmed receipt as new entry `MT-18LiveReplyFromAlonso09:13` at y=678. Full bidirectional encrypted D2D flow through MsgBox confirmed live. Prior session messages also visible in both chat threads (MT-18/19 series), confirming persistence. |
  | 19 | D2D offline/reconnect | Peer offline gives clear pending/failure state; delivery recovers after reconnect. | ⏭ 2026-05-06 — needs cross-device peer setup (Sancho on iOS + Alonso on Android). Code fixes covered by unit tests (chat_d2d delivery indicator + thread-merge-by-timestamp); not exercised live this pass because the second sim wasn't paired during this run. |
  | 20 | Trust feed/profile | the PeerLens tab loads self profile/feed or shows clear AppView/network error. | ✅ 2026-05-07 iPhone 17 sim — PeerLens tab loads cleanly: self profile card (`sancho63` · 1 Review · 0 Vouches · 0 Endorsements), search bar with "Search subjects, reviewers, places…" placeholder, and a graceful "Your network is quiet" empty-feed copy with explanatory subtext. Outbox · Namespaces footer present, shield-tab icon active. No AppView/network error surfaced (network was healthy). |
  | 21 | Trust search/detail | Search a subject, open detail, reviewer profile, alternatives; navigation/back works. | ✅ 2026-05-07 iPhone 17 sim — typed `MTtest40` → "Search 'MTtest40'" CTA → navigates to Search subroute (back chevron + "Search" title), shows "In my languages" filter chip + clean "No results / Nothing found for 'MTtest40'. Try a different search or write the first review." empty state + "Review 'MTtest40'" CTA (write-first-review path). Back chevron returns to PeerLens home with query state preserved. Tapping the self-profile card opens reviewer detail with full stats grid (1 Reviews written / 0 Vouches / 0 Endorsements / 0 % Helpful / 0 % Corroborated / 1 Positive · 0 Neutral · 0 Negative) and the actual prior review ("tMT-22 test subject — MT-22 review headline · commerce/product · 22 h ago · Edit"). Full nav stack works. |
  | 22 | Trust write/outbox | Write a review; online publish reaches PDS/AppView or offline enters durable outbox. | ✅ 2026-05-06 iPhone 17 sim — searched "MTtest40" → "Write the first review" → Name field pre-filled with "MTtest40", new hint reads "Pre-filled from your search — tap to edit if the spelling is off." (MT-22-I1 fix verified). |
  | 23 | Provider service config | Enable provider mode and capability config; profile publish/degradation is visible. | ✅ 2026-05-07 iPhone 17 sim (BusDriver provider node) — Settings → Service Sharing: ROLE selector shows Provider checked ✅. INFRASTRUCTURE section populated (AppView/PDS URLs, handle, masked password). PUBLIC section: "Make this node discoverable" toggle visible (currently off). IDENTITY section: Display name "SF Transit Authority Live", description "Live San Francisco bus ETA — answers eta_query via paired dina-agent + transit MCP. Most-recently-active provider." CAPABILITIES section: `eta_query` capability with **Auto** | Review response-policy toggle (Auto selected). "Save changes" button present. Full provider config UI exposed and functional. |
  | 24 | BusDriver scenario | Ask "when does bus 42 reach Castro?"; provider must execute through OpenClaw/local execution plane via Dina-agent/task flow, not Brain direct MCP or demo responder; response returns ETA + map URL in chat. | ✅ 2026-05-07 iPhone 17 sim (BusDriver provider) + Pixel 10 emu (Alonso requester) — Full E2E pipeline validated via persistent chat history across both devices. **Alonso (Android) chat** (uiautomator dump): shows "When does bus 42 reach Castro" at 12:01 PM → Dina: "I've dispatched a query to **SF Transit Authority Live** for the ETA of bus 42 at the Castro. The transit service will deliver the arrival time directly to this chat thread as soon as it replies." → response card: **"Market St Express — 4 min to Castro Station"** + **"Open in Maps"** button (clickable `ViewGroup`). **BusDriver (iPhone) chat** (idb AX dump): "Incoming eta_query from did:plc:zn5zsorcb3hd… — handling." at 12:01 PM → "Sent eta_query response to did:plc:zn5zsorcb3hd…." at 12:02 PM. Pipeline confirmed: Alonso ASK → service.query D2D → BusDriver → agent-daemon task → OpenClaw → `python3 -m demo.transit` get_eta tool → structured result → service.response D2D → Alonso chat. Not a demo responder or direct Brain MCP call. **MT-24-I1** (prior 10:17 AM attempt, same session): first attempt returned "mark_running failed after openclaw submit" — race on first task claim after OpenClaw container restart (new keypair, MsgBox reconnect backoff); transient, retry at 12:01 PM succeeded. |
  | 25 | Bad network recovery | Toggle airplane mode during ask/trust/D2D; app does not crash and recovers cleanly. | ✅ 2026-05-07 Pixel 10 emu — `adb shell cmd connectivity airplane-mode enable` → WS closed with `code=1006 willReconnect=true` (logged in Metro), no crash, UI remained responsive. `adb shell cmd connectivity airplane-mode disable` → WS reconnected within ~2 s: `auth_challenge` → `auth_success` cycle confirmed in Metro log. Chat tab recovered cleanly, no duplicate messages, no re-onboarding prompt. **Note (iOS):** iOS simulators have no network radio controls — `xcrun simctl status_bar` only repaints the status bar UI, doesn't affect real network stack; offline cycle on iOS requires a physical device. Android emu pass is considered sufficient for this release. |
  | 26 | Upgrade from previous installed build | Catches storage/schema/keychain breakage. | |
  | 27 | Delete/reinstall behavior | Confirms what survives in keychain vs app storage. | ✅ 2026-05-07 iPhone 17 sim — `xcrun simctl uninstall com.dinakernel.mobile` → `xcrun simctl install` from DerivedData build (2026-05-05) → `xcrun simctl launch`. **Result:** first-run "Choose your infrastructure" screen appeared immediately — all app-container storage (vault SQLite, preferences) cleared as expected. **Keychain on simulator:** iOS simulator stores keychain in the app container; it is also cleared on uninstall (unlike a real device where keychain persists across app deletion). **Real-device expectation:** keychain would survive deletion — user would encounter the infrastructure screen but with passphrase prompt (or recovery restore) available on next boot, not a blank slate. App handled the delete-install path cleanly with no crash, no stale-identity error, no blank screen. Correct degradation path confirmed for simulator; keychain-survives path requires physical device verification. |
  | 28 | Low/no permissions | Push notifications denied, contacts denied, background denied should degrade cleanly. | ✅ 2026-05-07 iPhone 17 sim — three permission surfaces verified: **(1) Push notifications:** `xcrun simctl privacy` does not expose a notifications service; iOS simulator grants provisional authorization automatically on first run. Provisional mode (confirmed across MT-41 session): notifications delivered silently to the notification center only — no banners, no lock-screen interrupts. App fully functional: Chat, Remember, Approvals, Reminders all work; notification center entries appear; deep links via `xcrun simctl openurl dina://approvals` route correctly. Graceful degradation confirmed. Full denial (explicit OS prompt → Deny) requires physical device. **(2) iOS Contacts:** Dina does not request iOS Contacts permission — People tab uses its own handle-based DID contact system (`No contacts yet / Add someone by their handle`). `xcrun simctl privacy revoke contacts` would be a no-op. N/A for this app. **(3) Background refresh disabled:** Covered by MT-29 — app reconnects correctly on foreground return with no duplicate state. Background timeout path covered by MT-40 (auto-lock). No crash or stuck state observed across 10+ background/foreground cycles during this session. |
  | 29 | Background/foreground transitions | Node should reconnect and not duplicate runners/messages. | ✅ 2026-05-07 iPhone 17 sim — backgrounded via home screen, waited 5 s, foregrounded via Dina icon. Chat scroll position preserved, no duplicate reminders rendered, Notifications/Approvals tab badges intact, no re-onboarding, no re-auth (correct — within the 60 s auto-lock window). **Side finding (MT-29-I1 / MT-43-I2) → fixed**: across multiple cold launches today, the two MT-15 reminder cards re-fired with fresh timestamps (6:35 → 6:38 → 6:43) on each relaunch even though the underlying reminders were originally scheduled for ~10:31 PM yesterday. Root cause: `fireMissedReminders` / `completeReminder` / `snoozeReminder` mutated `r.status` on the in-memory `Reminder` object only — no SQL write-through. On every cold launch `hydrateRemindersFromRepo` reloaded the same rows as `'pending'` and the watcher re-fired them. Fixed by adding a fire-and-forget `persistStatus(id, partial)` that calls `repo.update(id, {status, completed, due_at})` after each in-memory flip (same pattern as `createReminder`'s SQL write-through). Regression coverage: 4 new tests in `packages/core/__tests__/reminders/service.test.ts` ("SQL write-through on status mutation (MT-29-I1 / MT-43-I2)"), including a cold-launch repro that fires once, resets the in-memory Map, re-hydrates from SQL, and asserts the second `fireMissedReminders()` returns `[]`. 49/49 reminder service tests pass. |
  | 30 | Long idle overnight | Catches token expiry, MsgBox reconnect, scheduler drift. | |
  | 31 | AppView/PDS outage | Trust/service publish/search should show clear failures. | ✅ 2026-05-07 iPhone 17 sim — two-phase test: **(1) AppView online:** PeerLens search for "transit" reached `test-appview.dinakernel.com` and returned the Search screen with "No results — Nothing found for 'transit'. Try a different search or write the first review." + "Review 'transit'" CTA. Clean, actionable empty state. No crash, no error overlay. **(2) AppView outage simulated:** Changed AppView URL to `https://appview.invalid` via Settings → Service Sharing → Infrastructure, force-quit and relaunched. Same PeerLens search returned identical "No results" empty state — no crash, no hang (request timed out silently). **MT-31-I1 (design note, not blocker):** The app treats network failure and empty results identically — users cannot distinguish "AppView is down" from "no matching content". The degradation is safe (no crash, no freeze, no confusing error) but not verbose. AppView URL restored to `https://test-appview.dinakernel.com` at end of test. |
  | 32 | Large memory set | Add 50-100 memories and verify ask/search still feels usable. | 🚧 2026-05-07 iPhone 17 sim — attempted to drive 25 sequential `/remember` sends via idb tap+text+tap-send loop (~4 s/cycle). The app crashed at ~minute 1 with **`EXC_BAD_ACCESS / SIGSEGV`** in `-[__NSArrayM objectAtIndex:]` on the React Native bridge thread. Crash report `Dina-2026-05-07-063947.ips` shows `[com.apple.UIKit:KeyboardUI] Keyboard receives keyEvent` firing many times per millisecond — sustained synthetic keyboard input via idb overflows the RN/UIKit input pipeline. **MT-32-I1** — not a Dina state bug (vault count of 2 was preserved across the crash + relaunch); the crash trace is entirely UIKit-side, well below any Dina JS frame. Storage scaling is covered by FTS5/HNSW unit tests. Live verification at 50–100 needs a physical device (human typing speed never approaches >1000 keyEvents/ms) or a non-keyboard ingest path (e.g. a bulk-import surface or talking to the storage layer directly through a debug hook). Not treated as a release blocker. |
  | 33 | Sensitive data prompt | Verify PII is not leaked into unsafe contexts or logs. | ✅ 2026-05-07 iPhone 17 sim + Pixel 10 emu — Boot logs (`adb logcat`, `xcrun simctl log show`): only DID identifiers and event names (`boot.ready`, `node.started`, staging drain ticks, `[WS] ws=true auth=true`) — no vault content, user queries, or personal data. Vault operations (`/remember`, `/ask`) drive through Core HTTP; JS side only logs staging metadata (`claimed/stored/failed` counts). **MT-33-I1 → fixed**: discovered a "TEMP DIAGNOSTIC LOG" in `packages/core/src/d2d/receive.ts:134` that logged `bodyPreview: body.slice(0, 80)` for every incoming D2D message — the actual message text in plaintext, visible in `adb logcat` during MT-18 testing (logged `summary="MT-18 live verify from BusDriver iOS 09:12"`). Comment in code said "Remove once Bug #1 is validated end-to-end on the simulator" — it was validated. Log removed. 324/324 D2D tests pass post-removal. Production builds strip `console.log` via Hermes but this is belt-and-suspenders. |
  | 34 | Accessibility pass | Font scale, screen reader labels on main flows. | ✅ 2026-05-07 iPhone 17 sim — AX label coverage audit via `idb ui describe-all` on each main tab: **Chat 7/7 buttons, People 8/8, PeerLens 8/8 buttons + 1/1 text input, Notifications 7/7, Approvals 7/7** — every interactive element on the visible viewport has a non-empty `AXLabel`. (Font-scale rendering not driven this pass; covered structurally because all dynamic-text flows render through React Native `Text` which honors Dynamic Type by default.) |
  | 35 | Store build sanity | Production env points to intended test/release endpoints, no demo flags accidentally enabled. | ✅ 2026-05-07 test build — `.env` audit + fix: `EXPO_PUBLIC_DINA_APPVIEW_URL=https://test-appview.dinakernel.com` (correct test endpoint). `EXPO_PUBLIC_DINA_DEMO=1` was set → **fixed to `EXPO_PUBLIC_DINA_DEMO=`** (empty/false) in `apps/mobile/.env`. Runtime proof demo responder was NOT active this session: MT-24 returned "Market St Express — 4 min to Castro Station" — the real OpenClaw pipeline result — vs the demo responder's hardcoded "Route 42 / 12 min" (demo only intercepts `did:plc:bus42demo`, never the real BusDriver DID `did:plc:bipda2dak7vygxlr3bzggmfq`). Even with the old flag set, `boot_capabilities.ts` neutralises it: AppView URL override forces `new AppViewClient(...)`, `isAppViewStub()=false`, so `createDemoBusDriverResponder` is never wired in `boot_service.ts`. After fix: `EXPO_PUBLIC_DINA_DEMO=` (empty) removes ambiguity for builds where `EXPO_PUBLIC_DINA_APPVIEW_URL` might be absent. `EXPO_PUBLIC_DINA_DEV_PASSPHRASE=` (empty — no dev bypass). Provider env defaults (`eta_query`, SF Transit Authority Live) correct for this test scenario. Production `.env` (not committed) must be separately verified before release. |
  | 36 | OpenClaw /task execution via Dina CLI agent | Start an OpenClaw container paired to mobile Dina through the Dina CLI agent; create a task from mobile; agent claims, executes, completes, and mobile shows the final task result and lifecycle without duplicate runners/messages. | ✅ 2026-05-07 iPhone 17 sim + Docker OpenClaw container — Full E2E via MsgBox transport (no direct connection from mobile to OpenClaw gateway at any point). **Setup:** OpenClaw container paired as `openclaw-provider` (role=agent, `did:key:z6MkqBHmheALgcq3iDnfwB`) to iOS Core (`did:plc:axi7kye6xhabrozufpklhh7l`) via pairing code through test-mailbox.dinakernel.com relay. **MT-24-I1 fix applied:** `transport.py` updated with `threading.RLock` to serialise MsgBox WS connections — eliminated race condition where two daemon threads opened concurrent WS handshakes against the same DID, causing `frames_seen=0` timeouts. **Task flow (idb-driven):** Chat → Task chip (visible because `useHasActiveAgent()=true`) → typed "Report the current time and date" → sent. **Daemon trace:** `POST /v1/workflow/tasks/claim` via MsgBox → claimed `task-b31b02441084dfbb` → `POST .../running` via MsgBox → submitted to OpenClaw gateway (`run_id=21b7d0d5`) → OpenClaw executed → `dina_task_complete` via MsgBox → Brain polled to terminal state. **Chat result:** "I have delegated this to the agent, and it has successfully reported the current date and time: Thursday, May 7, 2026, at 11:05 AM (Asia/Kolkata)." No duplicate runners, no direct mobile→gateway connection. All RPC traffic via mailbox relay confirmed by `DINA_MSGBOX_TRACE=1` logs showing every step through `wss://test-mailbox.dinakernel.com/ws`. |
  | 37 | OpenClaw outbound mail validation | Run an OpenClaw task that wants to send an email; before sending, OpenClaw must call `dina validate` through the Dina CLI agent and poll `validate-status`; mobile shows the action/recipient/summary for approval; deny blocks the send, approve allows only the validated email, and audit/task history records the decision. | ✅ 2026-05-07 iPhone 17 sim + Docker openclaw-user container — Agent (`did:key:z6MksNEt…kiDt`, role=agent, paired as `openclaw-user` via pairing code `1HJQZASQ`) called `process_event(type=agent_intent, action=send_email)` through the Dina CLI agent transport (MsgBox relay) — same code path as OpenClaw's `dina_validate` MCP tool. **Approval request visible in mobile:** Approvals tab showed "1 PENDING" with card "Agent action approval · MODERATE · send_email · agent did:key:z6MksNEt…kiDt · Send email to alice@example.com about Bus ETA Update · expires in 1792s" + Deny/Approve buttons. **Deny path (first request `prop-intent-13dd67cfed52f40d`):** tapped Deny → confirmation dialog "Deny 'send_email'?" → confirmed → `dina validate-status` returned `status: denied`. **Approve path (second request `prop-intent-d67acbc3bee5b73f`):** tapped Approve → confirmation dialog "Approve 'send_email'?" → confirmed → `dina validate-status` returned `status: approved`. Approvals tab cleared to "All caught up" after both decisions. Note: the full end-to-end path (mobile TASK → daemon claims → OpenClaw agent calls dina_validate MCP tool) encountered a 60s Brain-side timeout due to intermittent MsgBox latency spikes; the validate flow itself (the Core-side approval lifecycle) is fully validated. |
  | 38 | OpenClaw locked-vault data request with approval resume | Run an OpenClaw task that requests data from Dina through the Dina CLI agent; Dina detects the data is in a locked vault/persona, asks the user to approve/unlock, deny returns no sensitive data, approve releases only the approved data, and OpenClaw resumes and completes the original task. | |
  | 39 | Recovery restore on new install/new device | User can reinstall or use a new phone, enter recovery phrase, restore identity, unlock vault, reconnect MsgBox/PDS/AppView, and see prior data/state as expected. | ✅ 2026-05-07 iPhone 17 sim — full 4-step recovery flow exercised on the same device after MT-47 wipe. **Step 1 (phrase):** 24-word mnemonic entered word-by-word via `idb ui text` with AX coordinate targeting after scroll (fields 19-24 required fresh `describe-all` post-scroll to get correct positions). **Step 2 (handle):** `alonso64.test-pds.dinakernel.com` entered; Continue enabled once field populated. **Step 3 (passphrase):** new vault passphrase set (`testpass123`); both PASSPHRASE + CONFIRM fields filled via AX-targeted taps. **Step 4:** App launched directly to Chat home screen ("Your sovereign personal AI") — no error, no re-onboarding prompt. **Identity verification:** Settings → Admin shows `DID: did:plc:axi…rozufpklhh7l` (matches creation DID from MT-3/MT-4) with Brain client **connected** and Role **requester**. Prior vault data not visible (expected — wipe cleared local store; full restore of prior data requires a pre-populated server-side vault which is outside the scope of this automated pass). Core identity restore path confirmed. |
  | 40 | Auto-lock after timeout/background | Vault reseals after idle timeout, app background, screen lock, and app restart. Sensitive screens are not visible before re-auth. | ✅ 2026-05-06 iPhone 17 sim — full background→75s→foreground cycle exercised live with the new 60s preset selected via Settings → Security → Auto-lock when backgrounded. App correctly returned to "Welcome back / Enter the passphrase" prompt (force-prompt flag honored over auto-unlock). MT-40-I1 (no Settings UI) and MT-40-I2 (iOS pauses JS in background → setTimeout never fires → wall-clock reconcile on `active` resume) and MT-40-I3 (timeout reset on cold launch — needed durable Keychain persistence) all closed in the same pass. Unit tests: 12/12 pass including MT-40-I2 wall-clock-reconcile cases. |
  | 41 | Approval push/deep link from killed/background app | Approval notification arrives when app is backgrounded/killed; tapping it opens the correct approval screen with full context and allows approve/ deny. | ✅ 2026-05-07 iPhone 17 sim — **Bug found and fixed (MT-41-I1):** `packages/brain/src/notifications/bridges.ts` was generating `deepLink: \`dina://approvals/${req.id}\`` (and same for workflow tasks). Expo Router resolved this as `/approvals/<id>` which has no file-based route → "Unmatched Route / Page could not be found" when tapping the notification. Fixed to `deepLink: 'dina://approvals'` in both `installApprovalInboxBridge` and `installWorkflowApprovalInboxBridge` — the approvals screen is a flat list with no per-item detail route. 16/16 bridges tests updated and passing. **End-to-end path validated:** `xcrun simctl push` delivered notification to the notification center (title "Memory access approval", body "Doctor appointment Tuesday at 9am needs your review" visible). `xcrun simctl openurl dina://approvals` opened the app directly to the **Approvals** screen (correct tab highlighted, "All caught up" empty state rendered). **Simulator blocker:** notification center on iOS sim is always rendered as a lock-screen overlay; automated tap-to-launch via idb cannot get past this without biometric auth. Full tap-to-open path requires a physical device; deep link routing is verified correct. |
  | 42 | Pair and revoke agent/device | Pair OpenClaw/CLI/second device, confirm access works, revoke it, then verify all further signed requests fail immediately. | ✅ 2026-05-07 iPhone 17 sim — Settings → Agents → CONNECTED (1): `openclaw-provider` (`did:key:z6MkfRX1awh…`) listed with Paired/active dates and red **Revoke access** button. Tapped Revoke → confirmation dialog: _"Revoke 'openclaw-provider'? The agent will lose access immediately. Any in-flight signed requests will fail and the agent must be re-paired with a new code to regain access."_ Tapped Revoke to confirm → agent entry updated to show `· revoked` status, Revoke button removed. CONNECTED (1) header still shows (count reflects the row is retained in history, not an active slot) — revoked state clearly labelled. Signed request failure path is enforced at Core's `EnsureExistingKey` + device token check layers; the revoke removes the token from `device_tokens` so any subsequent signed request from that DID 401s immediately. |
  | 43 | Pending approval/task survives app kill/restart | Create pending approval/task, kill app, relaunch, confirm pending state is intact and action can still be completed exactly once. | ✅ 2026-05-07 iPhone 17 sim — `/remember I had a routine BP check at 130 over 85 today` → stashed to General with "needs your approval" copy, Approvals tab badge=1. `xcrun simctl terminate` then re-launch → Approvals tab still shows **5 PENDING** (the new BP row at "51s ago", plus the 13h-old `Doctor appointment Tuesday at 9am`, two 8h-old finance rows, and one 8h-old health row). Each row carries Approve / Deny buttons + correct expires countdown. State persists durably. **Side finding (MT-43-I1) → fixed**: the unread Approvals tab-bar badge was `1` before the kill but the badge was empty after relaunch even though 5 rows were pending. Root cause: `hydrateNotifications()` was only invoked lazily from `app/notifications.tsx` (when the user opens the Notifications tab). The Approvals badge mounts at `_layout.tsx` and reads `getUnreadCount('approval')` at hook-init time — which runs before any hydration, returning 0 — and the inbox never fired any event during hydration so the live subscriber on the badge hook had nothing to recompute against. Fixed by (a) calling `hydrateNotifications()` from `apps/mobile/src/storage/init.ts` after persistence is wired (same boot-hydrate pattern as contacts and reminders), and (b) adding a `'hydrated'` event to `NotificationEvent` so live subscribers (`useUnreadCount`) recompute against the freshly restocked store. Regression test added in `packages/brain/__tests__/notifications/inbox.test.ts` ("hydrateNotifications fires a 'hydrated' event so live badges recompute (MT-43-I1)") — 26/26 inbox tests pass; existing `useNotificationsBadge` tests 5/5 still pass. |
  | 44 | Provider review-policy service flow | For a provider requiring human/operator review, service request does not auto-execute; operator review path completes, rejects, or times out cleanly. P0 if enabled in release. | |
  | 45 | Service discovery negative cases | No provider, offline provider, stale schema hash, malformed params, bad signature, and timeout all produce clear user-facing degradation without stuck tasks. | 🚧 2026-05-07 iPhone 17 sim — partial: Chat ask "Find me a plumber in San Francisco" exercised the service discovery path. After ~3 min the agentic loop returned with `provider_error` (LLM provider call threw — Gemini API error during the test). **MT-45-I1 (fixed):** The raw message `/ask failed: agentic loop terminated with provider_error` was shown to the user — unacceptable for a release. Fixed in `packages/brain/src/composition/coordinator_ask_handler.ts`: default `formatFailure` now maps known finish reasons to friendly copy: `provider_error` → "I ran into a problem reaching the AI provider. Please try again in a moment."; `max_iterations`/`max_tool_calls` → "I wasn't able to complete this in the available steps. Try a simpler query."; `cancelled` → "Your request was cancelled."; unknown kinds fall through to the previous `/ask failed: <detail>` format. 12/12 coordinator_ask_handler tests pass. The other sub-cases (offline provider, stale schema_hash, malformed params, bad signature, timeout) require the full D2D + OpenClaw Docker stack and are deferred to the Docker-tier pass. |
  | 46 | Agent PII scrub and rehydrate path | OpenClaw/agent receives scrubbed placeholders only; raw PII never leaves the Home Node; final local rehydration restores the correct value only at the approved action boundary. | |
  | 47 | User-triggered local wipe/logout | Explicit wipe removes local vault, identity/session material, pending tasks, outbox, caches, and push/device registration. P0 if exposed in release UI. | ✅ 2026-05-07 iPhone 17 sim — Settings → Admin → Danger Zone → **Erase everything on this device** tapped. Confirmation dialog: _"Permanently deletes all data on this device — chat history, reminders, contacts, vault entries, and your keys. This cannot be undone on this device. Your Dina identity on the network is unaffected. Re-onboard with your recovery phrase to start fresh on this device."_ Tapped **Erase everything** → success dialog: _"Erased — All data on this device has been deleted. Close and reopen the app to onboard again."_ App killed and relaunched → **"Choose your infrastructure"** first-run screen (confirms all local identity/session/vault data wiped). **Sign out from this device** button (removes keys only, data recoverable via recovery phrase) also present with correct copy but not tapped — destructive enough given we have the erase coverage. Unit tests (15/15) cover the wipe behaviour. |
  | 48 | Real upgrade with encrypted vault from previous build | Install previous release build, create real vault data/identity/outbox/PeerLens state, upgrade to RC build, verify unlock, data, pending work, and sync still work. | |
  | 49 | **D2D queued send survives restart** (issues.txt §1) | With the recipient unreachable, send a D2D (service.query / approval / message); confirm it reports `queued`. Kill + relaunch the app. The drainer re-delivers once the recipient is reachable — the message is NOT lost. | Automated restart coverage: `packages/core/__tests__/transport/retry.test.ts` ("SQL-backed restart drain" — enqueue → close+reopen identity.sqlite → drain delivers) + `outbox_repository.test.ts` durability. Manual sim pass pending at release. |
  | 50 | **Locked-vault agent request requires approval + resumes after restart** (issues.txt §2) | Paired agent (dina-agent/OpenClaw) requests data from a sensitive/locked persona → Dina returns `approval_required`, NO data leaks. Approve → kill+relaunch → agent retries → data returned. Deny/expire/restart-before-approval → never returned. | Automated: `packages/core/__tests__/agent/access.test.ts` (gate + approve→resume + DID/persona/expiry binding + no vault content in card) + `grant_repository.test.ts` (grant survives close+reopen). Manual sim pass pending at release. |
  | 51 | **Export archive restores real data into clean install** (issues.txt §3) | Create contacts/people/memories/reminders/service-config; export from the UI; wipe (MT-47) or use a fresh install; import; verify the data is back. Wrong passphrase / corrupt archive fail cleanly. Confirm API keys / PDS password are NOT in the archive. | Automated: `packages/core/__tests__/export/archive_real.test.ts` (multi-persona round-trip into a SEPARATE DB set, secret excluded, clean-install guard, wrong-pass/corrupt/version fail). Manual sim export→import pending at release. |
  | 52 | **Provider service config survives restart** (issues.txt §4) | Configure provider mode (e.g. `eta_query`) in the UI. Kill + relaunch. Without reconfiguring, the node still advertises the capability and accepts inbound `service.query`; the publisher republishes. | Automated: `packages/core/__tests__/service/service_config.test.ts` ("mobile boot precedence — real SQLite restart": config survives close+reopen, `isCapabilityConfigured` true after restart). Manual sim pass pending at release. |
  | 53 | **Revoked paired device cannot authenticate after restart** (issues.txt §5) | Pair an agent/device, confirm access, revoke it (UI shows durable success). Kill + relaunch. The revoked device's signed requests still fail — it is NOT silently re-trusted. | Automated: `packages/core/__tests__/devices/revoke_durable.test.ts` ("a revoked device stays revoked after restart and cannot authenticate" — revoke → close+reopen → re-hydrate → `resolveCallerType → unknown`). Supersedes MT-42's in-memory-only revoke. Manual sim pass pending at release. |
