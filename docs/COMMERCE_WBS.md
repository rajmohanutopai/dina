# Commerce Pack — Work Breakdown Structure

Derived from `docs/COMMERCE_PROCUREMENT_PLUGIN_ARCHITECTURE.md` (3,340 lines,
§1–§29) and reconciled against the code at commit `4e02c2c8`.

Spec section references are given as `§n`. Every leaf task carries a status, a
dependency, and a Definition of Done that can be checked rather than asserted.

## Status legend

| Mark | Meaning |
|------|---------|
| **DONE** | Built, tested, and mutation-verified where it encodes a rule |
| **PART** | Built but with a recorded defect or gap against the spec |
| **TODO** | Not started |
| **BLOCKED** | Cannot start until a named dependency lands |

## Owner decisions folded in (2026-08-07)

| # | Decision | Effect on this WBS |
|---|----------|--------------------|
| 1 | X-10: Core never synthesizes a replacement quote after restore | Supplier seam owns the re-offer; `quote_voided` when unwired |
| 2 | Broad §24 scope — build the full vertical | All workstreams in scope |
| 3 | `1.x` unfrozen until Phase 0 exit, then freeze at `1.0` | 1.10 becomes a documentation gate, not a major bump |
| 4 | Quotes do not reserve stock (§27 Q6 closed) | `max_uses` is admission capacity; inventory stays supplier authority |
| 5 | Unit vocabulary v1 closed, custom rejected (§27 Q4 closed) | 1.1 confirmed; widening later is additive only |
| 6 | Archive carries operational tables as evidence; restore never adopts counters | **4.2 shrinks** — preflight proves authenticity, not counter trust; capacity always re-derived |

Decision 6 removes a failure mode rather than mitigating one: there is no
"validated counters" path to get wrong, because counters are never trusted.

## How to read this

The spec's own delivery phases (§24) are the top-level ordering, with one
exception: **WS-0** is a cross-cutting remediation stream that did not exist in
the spec. It came out of three dual-review rounds and has to interleave, because
several §24 items are symptoms of it rather than independent work.

A phase exit gate is met only when every task in it is DONE *and* the §24 exit
criterion has been demonstrated end to end. Passing unit tests is not a gate —
§23 says so explicitly: "The commerce pack must not claim production readiness
merely because workflow and plugin repository tests pass."

---

# WS-0 — Structural remediation (cross-cutting)

Not in §24. Added because the review evidence showed the same rule escaping at
new call sites three times in one day. Each escape was individually correct and
individually tested; the shape was what failed.

| ID | Deliverable | Spec | Status | Depends | Definition of Done |
|----|-------------|------|--------|---------|--------------------|
| 0.1 | `QuoteFamily` aggregate; quote ledger unreachable from callers | §9.8 §16.2 | **DONE** | — | Aggregate owns epoch/expiry/capacity/voiding/revision; grep shows no production caller naming ledger mutators |
| 0.2 | `CommerceOrder` aggregate + §12.8 decision/genesis atomicity | §9.9 §15.5 §12.8 | **DONE** | 0.1 | Order owns its lifecycle; uniqueness stays a DB invariant; decision + hold settlement + genesis commit together via an injected seam; genesis failure rolls the acceptance back. 2 mutations caught |
| 0.2b | Extract `AdmissionService` as a separate transactional coordinator | §9.9 | **DONE** | 0.2 | `CommerceTransaction` owns the boundary; `CommerceAdmissionService` decides where one begins. The engine holds NO runner and exposes only `…InTx` methods. Nesting is now refused by name on every platform — it previously failed on mobile only (op-sqlite cannot nest a raw BEGIN) while the reentrant server runner passed, a rule two hand-written comments used to police. Two static guards keep it: `TxRunner`/`.tx(` appears only in `transaction.ts` + `runtime.ts`, and every public engine method is named `…InTx`/`…Record`. 9 coordinator tests. |
| 0.3 | `StatusChain` aggregate | §9.11 §16.2 | **DONE** | 0.1 | Creation, advancement and fencing all go through the aggregate; raw `CommerceStatusHeadRepository` unreachable from production; genesis fork closed via `admitted_epoch` + `reconciliation_required`; 5 mutations all caught |
| 0.3b | `ReconciliationService` — evidence verified before the chain moves | §12.7 §16.2 | **DONE** | 0.3 | `CommerceReconciliationService` owns the seven lifecycle boundaries (genesis, status update, restore fence, cancellation race, reconcile, restored-order ceremony, pending-cancellation finalisation). Held evidence is verified INSIDE the transaction that advances the chain, so a verifier that passed and a chain that did not move cannot be separated by a crash. §12.8's genesis seam is deliberately NOT wrapped — admission calls the engine from inside its own transaction, and a service wrapper would offer a second, non-atomic way to do the one thing that must be atomic. The epoch service moved to the coordinator too, found by the guard rather than by hand. |
| 0.4 | `register(quote, expectedBuyerDid)` — audience binding as a required argument | §9.8 §14.2 | **DONE** | 0.1 | Both inline buyer comparisons deleted; a new registration path cannot compile without naming its expected audience. Mutation: deleting the guard fails 4 tests across both paths |
| 0.5 | Ledger boundary made real: composition root + static guard | §16.2 | **DONE** | 0.2 0.3 | `commerce/runtime.ts` composes once; all five per-repository globals removed; engines depend on aggregate stores; `boundary.test.ts` asserts all four rules over the source |
| 2.2b | Retained-request / projection-digest binding | §9.9 §9.12 | **DONE** | 0.7 | WS-0.7 closed five `JSON.parse(…) as …` sites and MISSED the one that matters most: admission read the retained quote REQUEST that way, and that record carries the PRICED DELIVERY PROJECTION an incoming order's delivery is checked against. A projection edited in the store after writing became the standard the order had to match, so a mismatched order would pass — and the symptom would be a wrong commercial outcome, not an error. Now through `rehydrateQuoteRequest`, whose `validateQuoteRequest` ends in `verifyCommerceRecordDigest` and so re-derives the request digest from the content. Also removes a `JSON.parse` throw from inside `admitInTx` — in a transaction, on the inbound path, where everything else returns a typed refusal. 2 mutations caught; tests tamper via a harness-side hook rather than a production affordance, because `put` is first-writer-wins for the body on purpose (2.8) |
| 5.9 / 7.1 | **Buyer decision surface wired** (Core half) — fan-out, filters, ranking, evidence | §13.2-§13.6 | **DONE** | 0.8 | Six of the seven buyer-lane orphans closed. `procurement_service` composes the ORDER those pure functions run in — which is where the interesting mistakes live and was previously written only inside a scenario test — behind owner-only `POST /v1/commerce/procurement/plan` and `/choose`. TWO calls rather than one, because a network round trip sits between them: collapsing them would force the route to send, putting a second egress path beside the one the four gates guard. It adds no rule of its own; every decision still belongs to the module that owned it. `composeProductEvidence` stays on the ledger — building evidence needs a PeerLens read Core does not have, so the service takes composed evidence as an argument rather than learning to fetch. 4 mutations caught; one was a finding (an EMPTY-string `buyer_did` passed the guard, which silently disables self-exclusion while every other check passes) |
| 2.11 | **Probing resistance wired** — §20.10 spent before dispatch | §14.3 §20.10 | **DONE** | 0.8 | The orphan ledger's first real finding, closed the same day it was named. `admitQuoteRequest` was built, gated, and called by NOTHING: a competitor could draw a supplier's price curve by asking a hundred times at varying quantities, every individual answer legitimate. Now spent at the ingress BEFORE any runner is asked, for `request_quote` AND `availability` (which reveals what a supplier can source — half the curve, and leaving it out would make it the cheaper way to ask). The window lives in `QuoteAttemptLedger`: in memory on purpose (a durable table is the permanent log §20.10 warns against, and it would survive a restore into refusing a customer whose window has passed), pruning on every read rather than on a timer (a sweeper is a second thing to forget to wire), forgetting the PEER and not merely their attempts, and capped per peer far above any budget so eviction can never turn a refusal into an admission. Fails CLOSED when unwired — an unwired probing defence that silently permits is invisible, because every request succeeding is what success looks like. ONE refusal code for every reason (§14.3): a prober who can tell "budget spent" from "we don't quote you" learns the catalog. Only an ADMITTED request charges, so a peer past their limit can recover. 4 mutations caught |
| 0.8 | **The orphan ledger** — every exported commerce symbol is wired, or listed as not-yet-wired WITH A REASON | ARCH-0 | **DONE** | 0.5 | The defect this codebase produces most is a correct, well-tested rule that nothing calls; it was found by hand five times in this workstream alone (reconcile lane, epoch watermark, census, admission arrival evidence, ingress refusal detail). A static guard now enumerates every exported symbol under `src/commerce` and fails when one has no production caller and no ledger entry — AND when a ledger entry has quietly become wired, because a stale entry hides the next one. Current ledger: 5 test doubles, `composeProductEvidence` (needs a PeerLens read Core does not have), `ingestCatalog` (fetches; Core owns no outbound transport), `refusalsAreUniform` (a property-checker for tests), 2 reference packs, and the single-record watermark form. A THIRD bug in the guard, found later and the worst of them: it read production files with their COMMENTS in, so a symbol whose only non-test mention anywhere in the repo was a sentence in another module's prose counted as wired — `buildCatalogSnapshot`, the function that mints what goes on the wire, sat off the ledger looking called. A guard with a comment-shaped bypass is worse than no guard, because it certifies. Comments are now stripped from both sides, which immediately surfaced that orphan and `commerceAvailability`; both are now wired (WS-5.1). Proved to gate in both directions. Two bugs in the guard itself on first run: it walked only `packages/` and so reported `createCommerceRuntime` as unwired, and it reused a `/g` regex with `.test()`, which is stateful through `lastIndex` and silently skips every other match |
| 2.2c | Static guard: no commerce source parses a stored record outside `rehydrate.ts` | §9.12 | **DONE** | 2.2b | The cast guard (0.7) greps for `as SignedQuote` and friends, which is exactly why it missed the site with teeth: the retained request was read as `JSON.parse(…) as { delivery: … }` — an ANONYMOUS shape with no type name to find. A guard on the READ instead of the cast found three more sites immediately. Two were real and are fixed: the stored ACKNOWLEDGEMENT (read on both the replay and decision paths, where `JSON.parse('null') as OrderAcknowledgement` produced a null typed as a supplier commitment and a corrupt column threw) now goes through `rehydrateAcknowledgement`, which re-derives the acknowledgement digest; the cancellation-result scan no longer throws on one corrupt row, because one unreadable receipt must not make every other cancellation for the order unanswerable. Two are legitimate and are allow-listed WITH REASONS in the guard: `loadHeadStatus` already validates and compares `status_digest` to the head, and `catalog_ingest` parses an HTTP body that has not been stored. 4 mutations caught; the guard proved to gate by reintroducing the defect in a fourth file and watching it name the file |
| 0.6b | Refusals diagnosable to the OPERATOR without widening the wire code | §14.2 | **DONE** | 0.6 | `quote_unknown` covers an expired quote, one this node never held, and a retained request receipt it could not read back. Non-disclosing on the wire is right — telling a stranger which one lets them probe the ledger — and wrong in a log, where an operator debugging a live refusal had exactly as much to go on as an attacker. The rejected outcome now carries an operator-facing `detail` that never reaches the acknowledgement; the ingress error (already written for logs, §14.2) carries it. Found by writing `disaster_recovery_journey`, where a clock sitting on the quote's `valid_until` answered `quote_unknown` and cost an hour. 3 mutations caught, one of which — the ingress dropping the detail again — was the usual unwired-seam finding |
| 0.6 | Separate authorization from existence at the ingress gate | §11.2 §16.2 | **DONE** | — | `order_reconcile` is entitlement-by-evidence (buyer-bound payload) and reaches the handler with no local reference; `order_status`/`cancel_order` stay entitlement-by-possession. 3 mutations caught |
| 0.7 | Type the wire boundary — structural validation → signature/identity/digest → domain construction | §9.12 | **DONE** | 0.2 0.3 | REHYDRATION now uses the ingress validator (`commerce/rehydrate.ts`). Five sites that read a record back from the receipt store as `JSON.parse(…) as PurchaseOrderProposal` — a promise to the compiler and a check of nothing — now re-run `validatePurchaseOrderProposal` / `validateSignedQuote`, which RE-DERIVES the digest and so catches content edited after writing, the corruption a shape check alone misses. Each caller decides what failure means: the decision path errors, `loadRetainedQuote` treats it as not-retained, the recovery sweeper SKIPS that row so one bad receipt cannot stall every other timed-out order. 9 tests, 3 mutations killed. The ingress casts are GONE too: `readPurchaseOrderProposal` / `readSignedQuote` in `@dina/commerce-protocol` validate and TYPE in one step, with the existing `validate…` delegating to them so there is ONE implementation and the two shapes cannot drift. Every `as PurchaseOrderProposal` / `as SignedQuote` in `src/commerce` is gone, and a static guard in `boundary.test.ts` keeps them gone — proven non-vacuous by reintroducing one. The single documented exception is `order_decision.ts`, which hands a counterproposal's replacement quote to `decideOrder`, the one gate that owns audience binding. `decisionOutcome` now has a default arm returning `received_unresolved`. Reviewed honestly: it is NOT reachable today (every caller either builds the acknowledgement locally or runs `validateOrderAcknowledgement` first, which rejects an unknown kind before the switch). It earns its place against the protocol adding a FOURTH acknowledgement kind — the validator would accept it, the switch would not, and without a default it returns `undefined` typed as a result: a silent wrong answer to a buyer, caused by a change in another package that compiles cleanly here. **0.7 DoD met.** |

**Why 0.3 is the critical one:** the restore-fence rule is currently a predicate
each caller must remember to call. It cannot express chain *creation* at all,
because it takes a head epoch and at genesis there is no head. `signGenesis` /
`signGenesisInTx` can therefore re-sign a divergent sequence-0 record after a
restore, forking against the genesis the buyer already holds — and the fence
cannot repair it (unavailable before genesis, blocked afterward by the
same-sequence fork check).

---

# WS-1 — Protocol spine (Phase 0)

Exit (§24): *independent TypeScript fixtures produce byte-identical hashes and
reject the same malformed cases.*

| ID | Deliverable | Spec | Status | Depends | Definition of Done |
|----|-------------|------|--------|---------|--------------------|
| 1.1 | Money, quantity, unit vocabulary — **closed list, custom units rejected** *(owner decision, §27 Q4 closed)* | §9.1 §9.2 | **DONE** | — | Integer minor units; unit + pack conversion; custom units rejected. Widening after freeze is additive only |
| 1.2 | Exact arithmetic contract | §9.1 | **DONE** | 1.1 | Rational subtotal, single round-half-even, order-independent totals; non-negativity owned by `minorUnitsToString`; permutation-equivalence + rejection vectors frozen |
| 1.3 | Canonicalization + 10 domain-separated digests | §9.12 | **DONE** | 1.1 | Byte-exact canonical JSON; domain separation per record type |
| 1.4 | Product reference + exact-variant identity | §9.3 §9.4 | **DONE** | 1.3 | Relationship/object discriminant enforced in both directions; mutation-verified |
| 1.5 | Quote request / signed quote / revision chain | §9.7 §9.8 | **DONE** | 1.3 | Buyer verification binds expiry, request_id, line correspondence (subset legal, invention not), exact product identity, and substitution authority with evidence. 5 mutations caught |
| 1.6 | Order proposal, acknowledgement, status, cancellation, reconcile, epoch | §9.9–§9.11 §12.7 §12.8 §16.2 | **DONE** | 1.3 | The fence path no longer routes around the dispute deadline. `verifyStatusSuccession` refused `delivered → disputed` after the window; `verifyRestoreFence` allowed it and never looked at the window, because it took no clock at all — so a supplier could dispute an order whose window shut months ago by marking the record `restore_fence: true`. It now takes the RECEIVER's `at_iso`, required rather than optional (an omitted clock that skips the check reproduces the bug), and binds the deadline to the predecessor's own window. Recorded as BREAKING in the conformance changelog. A `>=` mutation SURVIVED first: nothing pinned whether the deadline instant is inclusive, though three functions answer that question and a port reading them separately could split — terminal on one side, still disputable on the other, over one millisecond. Pinned in all three. 4 mutations killed |
| 1.7 | Protocol version negotiation | §9.13 | **DONE** | 1.3 | `commerce_order_refs.pinned_version` pins the exact version at reservation; acknowledgements, status updates and receipts are built at the pinned version, not the build's. Major still derived from the pin for drain accounting |
| 1.8 | Catalog declaration + snapshot wire contract, pointer/tombstone, root + proof verification | §10.2 §10.3 | **DONE** | 1.3 | `catalog_publication.ts`: the full §10.2 trust chain, each hop recomputable — pointer → snapshot metadata → payload root → bounded pages. Pointer advance is CAS on the previous sequence: genesis must be 1 with no predecessor, gap and rollback and same-sequence fork all refused as publication faults, and the predecessor digest must link. Withdrawal is an explicit tombstone carrying the next sequence and naming no snapshot. Pages are bounded (500 items / 1000 pages) so a fetcher caps work before trusting. Commitments use their OWN domain prefix, NOT the ten frozen §9.12 record digests — a page digest is a content commitment, not a §9.12 record, and widening that list would break the frozen vectors. 27 tests, 5 mutations run: 3 killed, and 2 SURVIVED and were fixed — page-order in the payload root was untested, and the page-index binding was documented as a reordering mitigation it does not provide. A PUBLISHER now exists (`commerce/catalog_publisher.ts`): deterministic pagination, page + root + snapshot commitments, and a pointer that advances the chain. It runs the CONSUMER's own `verifyCatalogPointerAdvance` on its own output, so a producer cannot drift from the verifier and a fork is refused before anything reaches the wire. Over-large catalogs are REFUSED, never truncated — a silently shortened full-state snapshot would omit products with nothing in the record to say so. Empty catalog publishes zero pages, not one empty page. Withdrawal is TERMINAL for a catalog_id (a tombstone names no snapshot, so nothing can link to it; relaunch means a new id) — a decision the tests forced. 40 tests across the two packages; 8 mutations run, 6 killed, 2 survived and were fixed. §10.3 feed-fetch hardening is now BUILT as pure policy (`commerce/catalog_feed_policy.ts`, no I/O): HTTPS-only allow-list, credentials-in-URL refused, literal private/loopback/link-local/CGNAT/multicast addresses blocked in v4 and v6 including the IPv4-mapped form, redirect targets re-validated as strictly as the original, content-type allow-list, and BOTH a decompression ratio and an absolute cap (neither implies the other). Fails closed on any address form it cannot parse. The address check is explicitly documented as needing to run again at CONNECT time on every hop — a URL check alone does not stop DNS rebinding. 63 tests, 5 mutations killed. An INGESTER now consumes it (`commerce/catalog_ingest.ts`), so both the policy and the verifiers have a caller that cannot be routed around: `fetchUnderPolicy` clears the URL BEFORE the first request, re-checks the CONNECTED address on every hop (the DNS-rebinding defence a URL check cannot give), re-validates every redirect target, and enforces status/content-type/byte/decompression caps. `ingestCatalog` then verifies the pointer advance against what this consumer last accepted, refuses a snapshot the pointer does not name even when internally valid, verifies every page, and refuses when the pages do not add up to the snapshot's own item count. A withdrawal fetches nothing. Tests drive the real PUBLISHER's output through the ingester, so producer and consumer are checked against each other rather than against fixtures either invented. 20 tests, 7 mutations killed. Deep review found and fixed two real gaps: `maxMillis` was a DEAD constant (declared, never read) and there was no aggregate byte budget across pages — both are now ONE budget shared by the snapshot and every page, checked before each request. **GAP WAS STALE, corrected 2026-08-10.** It read "the AppView side (Jetstream ingest, PostgreSQL projection, xRPC search) is unbuilt" long after all three shipped: `appview/src/ingester/handlers/commerce-catalog.ts`, `appview/src/shared/commerce/catalog-projection.ts` and `appview/src/api/xrpc/commerce-catalog-search.ts`, exercised against real Postgres. `commerce_discovery_interop.test.ts` now drives the WHOLE chain with CORE's own published bytes — this row's verification pipeline producing them, AppView's independent verifier accepting them, the projection storing them and discovery answering from them — so the two halves are joined rather than each green about itself. A row that says a thing is unbuilt after it is built is the same defect as one that says a thing is wired when it is not; both were found in this review, in both directions |
| 1.9 | Frozen conformance vectors | §25.1 | **DONE** | 1.1–1.8 | Have arithmetic, digests, malformed, and now CATALOG (`catalog.json`): page digests, payload root, snapshot digest, plus five pointer-chain cases with the refusal STRING pinned, not merely the fact of refusal — two implementations rejecting a rollback for differently-worded reasons diverge the first time an operator reads a log. Proven to gate by perturbing the frozen root and watching CI fail. Building it exposed a real defect: `catalog_publication.ts` had invented a SECOND hash contract `(string) => string` where the package's `Sha256Fn` is `(Uint8Array) => Uint8Array`, so a caller holding the package's own hash could not use it — four passing test files had hidden this because they and the module agreed on the wrong thing together. Now one contract throughout. Two more categories landed: `quantity.json` pins unit and PACK conversion — exact cross-scale comparison, and the REFUSAL when a `case`/`pallet` needs pack evidence, with cross-dimension cases kept separate so a port cannot collapse two different refusal reasons into one check that happens to pass. `relationship.json` pins the DID/product discriminant in BOTH directions (a product `manufactured_by` another PRODUCT would compose manufacturer standing along an edge that means nothing) plus temporal validity: closed-before-open AND zero-length, since `<=` rather than `<` is the whole difference, and UTC rather than merely parseable, since an offset canonicalizes to different bytes. Refusal STRINGS frozen throughout. Proven to gate by perturbing a refusal string, a pack conversion result, and a temporal verdict — each fails CI. `packages/commerce-protocol/docs/conformance.md` updated (a doc test enforces that every vector file is listed). **THE LAST FOUR CATEGORIES ARE NOW FROZEN.** `product.json` grew three sections. `scoped` pins §9.3 NORMALIZATION: an identifier is a signed assertion BY ITS ISSUER, so `manufacturer_sku` and `custom` REQUIRE `issuer_did` — two manufacturers using the same internal SKU is ordinary, and a port that accepted a scoped scheme unattributed would let one supplier's catalog answer for another's part number. Accepted cases are pinned beside the refusals, because a port that refused EVERYTHING would pass a refusal-only vector. `variant` pins §9.4 EXACT-VARIANT AUTHORITY — the same identifier at a different variant is a different line item, which is what stops a 12-pack shipping against a quote for a 6-pack because the GTIN matched — including the asymmetric case, where a variant present on one side and absent on the other is NOT a match, because "unspecified" is not a wildcard. Three new `equality` cases pin the issuer half: the same SKU under two issuers is two products. `schema_evolution.json` is new and carries §9.13's two halves. VERSION ADMISSION: same MAJOR parses (MINOR is strictly additive), anything else is a TYPED refusal naming what is supported — a port comparing the whole string would refuse every minor release, one comparing nothing would act on fields whose meaning changed. UNKNOWN FIELDS is the half that is easy to get backwards, so both directions are pinned together: canonicalization INCLUDES an unknown field, so a receiver may not strip what it does not recognise and still expect the signature to verify; validation TOLERATES it, so a record from a newer minor is not invalid merely because it says more than this build reads. Accept and preserve, never accept and rewrite. The canonical BYTES are pinned rather than merely their equality, because a port canonicalizing both sides the same wrong way would satisfy an equality-only assertion. 30 new cases; proven to gate by perturbing four of them (a variant declared a substitute, a scoped scheme accepted unattributed, a higher MAJOR declared parseable, an unknown field declared digest-neutral) — each turns CI red. `packages/commerce-protocol/docs/conformance.md` updated and its **Known gaps** list is now empty. |
| 1.10 | **Declare `1.x` unfrozen** until Phase 0 exit, then freeze at `1.0` *(owner decision 2026-08-07)* | §9.13 §25.1 | **DONE** | — | `packages/commerce-protocol/docs/conformance.md` (shipped in `files`) carries the declaration, a changelog of the three incompatible changes, checkable Phase 0 exit criteria, and the §25.1 vector gaps. 5 tests keep it from drifting |
| 1.11 | `validateIsoUtc` rejects impossible calendar dates (`2026-02-30`) | §9.12 | **DONE** | — | Round-trips the parsed instant; leap days and month ends still accepted; mutation-verified |

**Phase 0 cannot be declared frozen** while 1.8, 1.9 and 1.10 are open.

---

# WS-2 — Supplier-side Core engines

| ID | Deliverable | Spec | Status | Depends | Definition of Done |
|----|-------------|------|--------|---------|--------------------|
| 2.1 | Commerce stores (order refs, quote/status heads, use counters, receipts, watermarks) | §23 | **DONE** | 1.x | Dual harness (in-memory + SQLCipher) parity |
| 2.2 | Admission engine — §9.9 precedence, atomic holds, settlement, recovery sweeper | §9.9 §15.5 | **DONE** | 2.1 | Recovery sweeper FIXED and WIRED. It was correct and unreachable: nothing in the repository called `recoverAdmissions()` outside its own tests, so no running node ever timed out a reservation — every abandoned order held capacity indefinitely and reconcile answered `received_processing` for ever. Inside it, a second leak with no floor: it loaded and re-validated the order proposal first and skipped the row if either failed, so the row returned on every future sweep and the hold never came back, silently (the sweep reported only successes, so "0 timed out" read the same as "everything stuck"). A `rejected(decision_timeout)` needs nothing from the proposal — its three fields are on the reference and the digest got there from admission — so it now resolves the row from the reference and reports what it genuinely cannot resolve as `stuck`, separately from errors. `buildAcknowledgement` was narrowed from a whole `PurchaseOrderProposal` to a `Pick` of the three fields it reads; the wide type is what taught the sweeper it needed one. New `CommerceAdmissionSweeper` started in `wireWorkflowPlane` (one place, both boots), resolving the runtime per tick so boot order, a fail-closed epoch and identity teardown are all quiet ticks. 3 wiring mutations killed against the REAL plane — a sweeper unit test would have passed while nothing constructed it, which is how this arose. **BOTH OPEN ITEMS CLOSED, and the correction is that they were BUILT and UNPROVEN rather than missing.** The request-receipt / projection-digest binding is in `admitInTx`: the yardstick is the RETAINED REQUEST receipt, rehydrated through `validateQuoteRequest` (which re-derives the request digest — the one corruption a shape check cannot see), never the order's own projection. A mutation swapping the yardstick for `order.delivery_projection` is killed, which is the check that makes the binding mean anything: an order compared against itself always matches. Store-integrity failures on the admission path already went through `recordAdmissionRejection`, so they are durable; the FREEZE across repair was believed and never asserted — the previous test even said so in a comment ("a second would be a replay of the first refusal, which is correct behaviour"). §9.9's sentence is "an admission answer, once given, is frozen … never a retroactive acceptance", and the integrity path is where that is easiest to get wrong precisely because the condition HEALS: an operator restores the receipt from a backup and the order would now bind perfectly. A node that re-evaluated would hand the buyer an acceptance for a proposal it has already refused, after the buyer has moved on. Two new tests on BOTH backends: repair the store byte for byte and the replay still returns the recorded rejection (validated by the same validator the wire uses, because that is what the buyer receives); and the frozen rejection REFUNDS the hold, so a different order against the same quote still reserves — otherwise the supplier's own disk fault silently spends their offer. 5 mutations killed, including "the refusal is not recorded", "the frozen rejection spends capacity", and "an unreadable stored acknowledgement is handed back as the answer" (which is `JSON.parse('null')` returned to a buyer as "here is what we agreed"). **Open:** nothing |
| 2.3 | Status / cancel / reconcile engines | §9.11 §12.7 §12.8 | **DONE** | 2.1 | Acceptance/genesis atomicity closed by 3.6. `finalizePendingCancellation` FIXED: it recorded a terminal `cancelled` and moved nothing — no decision, no refund, no chain — so Core told the buyer the order was cancelled while its own chain stayed live and could still sign `dispatched`, and the held capacity stayed held for good. The digest §12.8 requires a `cancelled` result to carry was an optional CALLER parameter: omitted produced a record the buyer must reject, wrong bound the ruling to a head this engine never ruled on, and either way it was durable, terminal and unreopenable. Parameter deleted; the digest comes from a head this path just wrote. Two parked shapes each get the live path's own outcome — pre-genesis decides/refunds/signs the `cancellation_won` genesis, post-genesis transitions the head — through ONE shared settlement rather than a second copy. The one existing test finalized only to `refused_already_dispatched`, which decides nothing, which is why it survived. 3 mutations killed; a 4th (deleting the new validate-before-write guard) SURVIVED and is documented in code as a backstop no current input reaches. Chasing it found a live defect: `versionMatches` had a `pinnedVersion === ''` hatch for rows that cannot exist, which if one did would DISABLE §9.13 pinning for the row whose version nobody knows — removed, unknown pins now refuse. Re-adoption now answers `received_unresolved`, not `received_accepted`. The decision IS known — it is the buyer's own signed evidence handed back — but this node cannot act on it: re-adoption rebuilds a REFERENCE, not the order's lines, its quote context or its external state, and chain creation stays barred until the owner runs the §16.2 ceremony. `accepted` was true and useless: it echoed the buyer's own document at them and invited them to wait for status updates that could not come. `received_unresolved` is §12.7's own word for "holds the order, cannot report where it is", and its longer retry suits something that needs a human — no NEW outcome, because the set is frozen with vectors and a fourth value would be a wire-major change to say what one already says. The answer is the same on the poll that re-adopts and on every poll after, and it CLEARS: once the ceremony makes the order describable, the same question returns `received_accepted`, which the scenario test now walks. Two existing assertions had to change, and the procurement scenario's own narrative had been contradicting itself — the sentence directly under `expect(...received_accepted)` read "the order is back, but ChairMaker cannot describe it… it is frozen". I also corrected a wrong comment of my own mid-change: I first described re-adopted orders as left `reserved`, and they are decided. **Open:** nothing |
| 2.4 | Epoch service — CAS publication, restore increment, fail-closed signing, bounded revalidation | §16.2 | **DONE** | 2.1 | Service now has a real repo: `PDSPublisher.getRecord` + `putRecord(..., {swapRecord})` over `com.dinakernel.commerce.epoch`, installed only on success. A node with no PDS leaves commerce disabled. BOTH open items are closed. §16.2's "signing nodes also re-verify the live epoch on a bounded interval" is built: the cached epoch now carries a validated-at time, `revalidate()` re-reads the live record, and `currentEpoch()` REFUSES once it has gone unverified past a bound — a revalidation whose only effect is a log line changes nothing about what the node signs. Convergence means STOPPING, never adopting: a node that adopted the higher epoch would sign beside the node that actually restored, holding the pre-restore capacity counters the fence had already voided, and two nodes each incrementing on sight of the other would ping-pong the epoch for ever. Five distinct stop reasons, each a different repo fault: superseded, rolled back below what this node signed at, record deleted (absence is not consent), record replaced in place at the same number, and a record that turns out to belong to another business. An unreachable repo does NOT stop on one failed read — a blip must not take a healthy supplier off the market — but the staleness bound turns a long run of them into a refusal, because "I cannot check" for long enough IS the forgotten node. The repo wiring moved to `@dina/home-node` and MOBILE now has it: `getCommerceEpochService()` was read in two places on the phone and set in none, so `currentEpoch()` threw on every commerce operation there — invisible, because a node refusing everything is what a correctly fail-closed node also looks like. Both ticks (§9.9 admission recovery, §16.2 revalidation) now start through ONE `startCommerceSweepers` per composition root; admission recovery used to live in the shared workflow plane under a comment saying both boots got it from there, and the phone never calls that plane, so no reservation on a phone ever timed out and no capacity was ever refunded. A new boundary guard fails if either root stops starting or stopping them. The orphan ledger also had a hole: a class naming ITSELF in an error message counted as internally consumed, so any such class could sit unwired and off the ledger — string literals are now stripped from the same-file count, interpolations kept. 20 tests, 19 mutations killed and 3 more killed by the compiler (the discriminated union makes the state guards non-optional). **Open:** nothing |
| 2.5 | `loadHeadStatus` bound to the expected order/buyer/supplier | §9.11 | **DONE** | 0.3 | The digest proved the record was the one the head NAMES, never that it belongs here. The receipt store is keyed by digest across every order this node has handled, so a head row pointing at another order's status loaded clean, then drove transition legality and became the cumulative-lines floor for a successor signed on a DIFFERENT order. Bound to buyer, order and supplier. Deleting the `buyer_did` and `supplier_did` comparisons both SURVIVED the first test, which covered only the order id — each is a distinct escape (`purchase_order_id` is buyer-chosen and the stores are keyed by (buyer, order), so two buyers can share an id) and each now has its own case. 3 mutations killed |
| 2.6 | Restore fence re-derives rather than restates | §9.11 §16.2 | **DONE** | 0.3 | The buyer enforced two rules the supplier did not: fulfilment bounded by the order's `accepted_lines`, and state legality from the predecessor. The supplier copied both forward verbatim and never loaded the order at all — so Core would sign a record the buyer must reject, stranding the order the fence exists to rescue. A restore is also the one moment the halves can genuinely disagree, since the order reference and the status receipts are separate tables and can return from different backup vintages. The fence now rehydrates the order through the ingress validator and runs the buyer's own `verifyStatusLines`. The EPOCH BAR had the same shape: both sides require the fence to raise the epoch, but ours against the local head and theirs against the buyer's — which after a restore sits above ours on records we lost. The bar is now the higher of the local head and the highest epoch among verified presented receipts. One arm is documented as INERT (cumulative regression cannot fail while the fence restates verbatim) and passed anyway so the call stays byte-for-byte the buyer's, because §16.2 permits a fence to advance rather than restate. 3 mutations killed |
| 2.7 | Commerce `order_status` capability handler | §11.2 | **DONE** | 0.6 | Delivered by 0.6: the subject gate authorizes by order ownership under the authenticated sender, and unknown / unowned / missing all collapse to one non-disclosing answer so the endpoint is not an existence oracle. **Attempted and REVERTED (2026-08-08):** moving the answer into Core, on the reasoning that only Core can sign a status record. Six §9.13 tests failed and were RIGHT — the published result schema for `order_status` is the SUPPLIER's, declared in the manifest and pinned by the buyer, so a Core-chosen shape violates the contract the buyer pinned and removes the supplier's ability to enrich a status with data only it holds. Reverted in full including the unused reader it introduced. The genuine concern underneath (a runner reporting a state Core never signed) belongs on the completion path as a cross-check against Core's own chain — see 2.10 |
| 2.8 | Receipt evidence capture | §9.12 §16.2 | **DONE** | 0.7 | `evidence_json` existed since CMC-1 with EVERY caller writing `'{}'`, and `put` was `ON CONFLICT DO NOTHING` — so the second observation, the one carrying real authentication, was discarded. Now TWO FIELDS, TWO RULES under one write: the record body stays first-writer-wins (the digest addresses those bytes; a second writer proposing different bytes is a collision or an attack), the evidence ACCUMULATES. Typed observations (`signed_here` / `received` with counterparty DID, envelope id, key id, signature), deduped by identity with `observedAt` deliberately excluded so a retried delivery is the same fact rather than a delivery log, and the first sighting keeps its timestamp. Corrupt stored evidence reads as empty and is dropped rather than merged (merging it would let anyone who could write that column inject observations); a receipt must not become unwritable because a metadata field went bad. Captured at admission (`received`, naming the AUTHENTICATED sender threaded down rather than re-read from the order) and at status/cancellation signing (`signed_here`). 5 mutations, 5 killed; one — the admission capture having no test — was a finding |
| 2.9 | Counterparty epoch watermark wiring | §16.2 | **DONE** | 2.4 | The table and its repository existed since CMC-1 with NOTHING reading them — a fence with the gate left open. `admitSupplierRecords` now runs on every tool-lane completion, straight after the pinned-schema check and before the result becomes the owner's answer: §25.3 delayed-pre-restore-write refused, one stale record refuses the whole result (a filtered result silently gives the buyer fewer quotes than the supplier sent), nothing raised when refusing (so a higher epoch earlier in a list cannot admit a stale one after it), BigInt comparison (`"10" < "9"` in every text collation), non-canonical epochs refused rather than coerced, and one counterparty-facing refusal string that never names a third party's restore history. An EFFECTFUL stale completion parks `outcome_unknown`, matching the schema-mismatch reading. 6 mutations caught, two of which were findings: the wiring test proved the first version read `service_ingress.from_did` — SUPPLIER-side correlation that never appears on a buyer's outbound envelope, so the gate would never have fired; and a record carrying an epoch with no `supplier_did` was passing by default, the exact one-field bypass |
| 2.10 | Cross-check a runner-reported status against Core's own chain | §9.11 §11.2a | **DONE** | 2.7 | `transformInboundOrderResult` now owns `order_status` as well as `submit_order`, with a different treatment: `state` is overwritten with the head Core SIGNED and every other field passes through. The two are not equal in authority — a runner saying `dispatched` while the chain says `accepted` reports something this business has not claimed. Overwriting rather than refusing, because a refusal costs the buyer the carrier reference too and the runner may simply be ahead of the chain. With NO chain the answer is left alone (substituting a state there would be Core making the claim it is stopping the runner from making); an OMITTED state gets Core's added, because omission is not agreement. An existing test asserting `order_status` was untouched was updated rather than deleted. 5 mutations killed |

---

# WS-3 — Platform enablement (Phase 1 blockers)

Nothing in WS-1 or WS-2 is reachable in production until this stream lands.

| ID | Deliverable | Spec | Status | Depends | Definition of Done |
|----|-------------|------|--------|---------|--------------------|
| 3.1 | Provider-kind install support | §11.2a §23 | **DONE** | — | `kind.provider` shipped in `NODE_SUPPORTED_FEATURES`; a provider manifest installs through `beginInstall`, and a manifest also declaring an unshipped kind is still refused. Mutation-verified |
| 3.2 | Provider-ingress bridge | §11.2a | **DONE** | 3.1 | Full lane: `ServiceHandler` recognises the plugin plane and routes through an injected `ProviderIngressSubmitter` (defaulted in `buildHomeNodeServiceRuntime`, so both boots get it); auto and post-approval paths both dispatch; typed refusals reach the requester; completion rides the real bridge sender, validates the result against the PINNED result schema, and answers the buyer. 13 mutations caught |
| 3.3 | Listing bound to exact `(install_id, manifest CID, capability)` | §23 FR-P2 | **DONE** | 3.1 | `validateServiceConfigForSave` resolves the binding against the install registry: unknown install, non-active install, stale CID, undeclared capability, and tool-published-as-service each refuse the SAVE with their own code. Fails closed with no registry. Ingress keeps the same checks at query time (a listing valid at save can go stale later). 7 mutations caught |
| 3.4 | Extension-operation broker | §3.4 | **DONE** | 3.1 | State machine built: durable `plugin_extension_proposals` ledger; propose (deny-before-validation, params checked before the row exists, idempotent by (install, key)); permit / refuse-recorded / cancel-before-decision; begin-execution as its own crash boundary; settle verifying against the schema pinned AT PROPOSAL TIME; `outcome_unknown` terminal and distinct from failed; `listExecuting` for the sweeper. 7 mutations caught. Composed at BOTH boots via `createPluginHostRuntime`, with a static guard that nothing else constructs the parts. The RECOVERY SWEEP is now built and wired: `listExecuting()` had no caller, so a proposal whose process died mid-effect stayed `executing` for ever and the one row recording that an effect MIGHT have happened sat in a table nobody read. It SETTLES and never retries — an effect that may have crossed the wire cannot be run again — and the deadline measures from the PERMIT, not the proposal, so a row that waited on an owner's approval is not swept the instant it starts. Wired through `installPluginHostRuntime`, so both composition roots get it from the call they already make and neither can forget; replacing a runtime stops the outgoing sweeper first. A REAL DEFECT fell out of building it: `settle` read the state and then wrote without a CAS, so two settlers racing — the dispatcher finally hearing back and the sweep giving up on the same row — both wrote and last won, meaning a swept `outcome_unknown` could overwrite a verified `completed` and turn a fact into a question an operator then chases. `finish` now CASes on `executing` and the second settler reports `not_executing`. The RUNNER LANE is built to the shape §3.4 actually specifies rather than the obvious one: the spec says "there is no separate in-process callback surface", so a `POST /v1/plugins/host-operations` route would be a second door into the same authority with none of the claim-token, idempotency or lease discipline — the runner asks by COMPLETING its claim with a typed proposal. `parseHostOperationRequest` recognises one and refuses a params-named `install_id`; `consentedCapability` requires BOTH the pinned manifest and a consented scope hash (a manifest capability with no hash is one the owner declined); `brokerHostOperation` refuses a PAUSED install acting on a claim it held while active, records a refusal rather than dropping it, and never re-decides a retry of the same key. The DECISION is the existing action plane, not a new one: `decideExtensionProposal` runs `evaluatePluginIntent` over the operation's `action_class`, so payment is BLOCKED at every ring, a custom capability's read still cards, and a standing grant permits a write past first-N — §3.4's "approval OR standing-grant check" verbatim. `applyOwnerDecision` is the owner's half, with the permit CAS making a double tap on the card run the effect once. 36 tests; two of them were WRONG on first run and the gatekeeper was right both times — a standing grant in the defaults contradicted the expected card, and a custom capability id never floors below MODERATE. The SEAM is now wired and the chain runs end to end. `WorkflowService.complete()` notices a typed proposal, DETACHED like the response bridge — the completion has already been CAS-confirmed and must never unwind on a network effect — and a failure anywhere in the handler is reported rather than thrown back. The verified result reaches the runner as its NEXT claim on its own lane, in a new `host_operation` envelope block that is deliberately NOT `params`: the consented `params_schema` governs params and an operation's result is not that shape, so putting it there would force the schema wide enough to accept both. The block carries the terminal STATE verbatim, because a runner that saw only a result could not tell a refusal from a failure from an outcome nobody knows, and those lead it to opposite next steps. A refusal is delivered too — a runner left waiting on a task that will never come is worse than a no. `settleOwnerDecision` gives the owner-approved route the SAME follow-up path as the auto-permitted one, so a runner cannot tell whether a human was involved. Wired at BOTH roots through one factory whose policy defaults are the strictest reading (`custom` + `unverified`, so nothing above SAFE runs silent until a root injects resolvers that say otherwise). Wiring mobile surfaced a SECOND divergence of the same class: the phone's `WorkflowService` never had `ingressResultTransformer` either, so a supplier running on a phone answered a buyer with the RUNNER's JSON instead of the acknowledgement Core signed (§9.9) — now passed on both. 13 mutations: 11 killed, 1 killed by the compiler, and 1 survives and is documented in place (every `evaluatePluginIntent` path that sets `mode:'blocked'` also sets `allowed:false`, so the belt-and-braces mode check cannot be reached; it stays because two disagreeing fields must refuse). The survivor hunt also found a REAL test gap — nothing completed a non-plugin task through a service with a handler installed — and a fixture bug: a capability without `action_class` builds an envelope the parser rejects, which is the parser working. **Open:** nothing |
| 3.5 | Typed host operations (bounded AppView search, D2D send, publication candidate, connector broker) | §3.4 FR-P9 | **DONE** | 3.4 | `HostOperationDispatcher` drives permitted proposals: claim `executing` before the effect, run, settle against the pinned result schema. Executor context carries params + install identity and NOTHING credential-shaped, so a runner cannot smuggle authority through a payload. A thrown executor settles `outcome_unknown` (a socket can die after the bytes left); characterised failures return `failed`. Bounded AppView search truncates in Core and the request has no limit field to widen. 8 mutations caught. Composed at both boots. ALL FOUR §3.4 operations now exist (`host_operation_executors.ts`): D2D send (the sender is this node, a params `from_did` is REFUSED not ignored, and the recipient is checked against the install's permitted set), publication candidate (Core validates through the §12.1 leakage gate and does NOT publish — publication advances a chain buyers follow and stays the owner's act), and connector broker (the install comes from the PROPOSAL, which is the one check the credential broker cannot make for itself since it trusts whatever install id it is handed). Pre-network broker refusals map to `failed`, wire failures to `outcome_unknown`. The server boot REGISTERS connector-broker and publication-candidate executors. 18 tests. BOTH BOOTS now compose the plane and register all three — the mobile boot had NO host runtime at all (the §3.4 lane was silently absent on the platform that is the product; found by wiring the server and asking what the other boot does). `d2d_send`'s permitted-recipient set derives from §8's EXISTING grant `resources` allowlist rather than a new policy: a D2D recipient IS a resource the grant names, an unconstrained grant names NOBODY (reading it as 'every DID' would turn one approval into a channel to the network), and a `constraintsCorrupt` row names nobody either. |
| 3.6 | Composition root — production wiring for admission / lifecycle / epoch on both boots | §23 | **PART** | 0.2 0.3 0.5 3.1 | Root builds admission + lifecycle, ties acceptance to genesis (§12.8), shares the Tier-0 runner, and answers a typed `commerceAvailability()`. The recovery sweeper is now started in the shared `wireWorkflowPlane`, so it reaches both boots from one place. **FIXED 2026-08-08:** mobile now COMPOSES the runtime, not just tears it down. The gap was invisible by construction — teardown had always called `installCommerceRuntime(null)`, so the code read as if a runtime existed, and every commerce path then found null and failed closed, which is exactly what a correctly-refusing node looks like. The mobile app IS the product, so "server-only" was a real gap rather than a staging decision. Both roots read identity and epoch through fail-closed THUNKS that throw rather than defaulting: a node signing under a guessed identity or a missing epoch produces commitments it cannot stand behind (§16.2). A static boot-parity guard now compares the two roots, because a behavioural test would need a booted app and an assertion about one root would pass while the other dropped the wiring. It asserts creation on BOTH and teardown on MOBILE ONLY — a phone signs out and back in inside one JS process, the server boots one identity per process — an asymmetry a first version of the test wrongly demanded be symmetric. **Open:** the server installs `CommerceEpochService` only with a provisioned PDS, so a node without one reports `no_epoch` — cleanly disabled, not live |
| 3.7 | **Buyer-side executor — the one place an order leaves this node** | §9.9 §12.7 §15.2 FR-P5 FR-P6 | **DONE** | 3.6 7.5 7.7 | `commerce/buyer_executor.ts` + `POST /v1/commerce/orders/submit`. NOT A NAMED ROW BEFORE: three items each ended at the same edge — the §15.2 binding had nothing to bind at execution time, the §12.7 machine had nothing to create its record, and the procurement routes plan and rank without sending. All three assumed this component and none of them was it. IT DOES NOT OWN TRANSPORT: the sender is INJECTED, because egress belongs to the service-query lane that already carries the four gates, signing and MsgBox. What it owns is the ORDER of operations — refuse a duplicate, verify the binding, RECORD then send, settle through the one state machine. STEP 3 IS THE ARGUMENT: recording first means a crash between write and send leaves a record for an order that never left, which reconcile resolves safely; sending first leaves NO record of an order that may exist, and nothing ever asks about it. The sender's three outcomes are documented as obligations in the TYPE — `not_sent` is a promise nothing crossed the boundary and is the only one that authorizes a resend; a sender reporting a timeout as `not_sent` would authorize a duplicate. An unknown acknowledgement kind PARKS rather than settling: the supplier answered something, so the order may exist. The retailer&rarr;manufacturer journey now runs its order step through this executor into ChairMaker's real ingress, so both shipped surfaces meet instead of one test calling both halves. 15 tests + the journey, 7 mutations killed. **SENDER BOUND IN BOTH ROOTS** via `commerce/buyer_sender.ts`, over the SAME `service.query` egress every other outbound capability uses — so the four gates, signing and MsgBox apply without a second path to keep in step. The mapping lives in tested code rather than as a lambda in two roots, because getting it wrong in the safe direction costs a needless reconcile and getting it wrong the other way authorizes a duplicate order: a THROW is reported AMBIGUOUS (the transport has not told us whether anything left — the outbox may already hold it), and `not_sent` is reached only when a gate names itself. A successful dispatch is also AMBIGUOUS, because the acknowledgement arrives later through the response bridge — that is the honest description of an asynchronous lane and exactly the state §12.7 was written for **THE HEADLINE CLAIM IS NOW ENFORCED, not merely written.** "The one place an order leaves this node" was prose, and nothing stopped a second dispatch being added later — which is the exact break this row was designed against, because two send paths eventually disagree about what a failed send means and one of those readings authorizes a duplicate order for real goods. Three guards in `__tests__/commerce/boundary.test.ts`: (1) ONLY `buyer_sender.ts` may name the `submit_order` capability on the outbound side — the capability name is the chokepoint because any second path has to name it to reach a supplier — with a four-file allow-list carrying a reason each (`buyer_response.ts` reads the answer back, `order_decision.ts` and `provider_ingress.ts` are the SUPPLIER serving it); (2) both composition roots install the sender AND the dispatch it sends over, in `storage/init.ts` rather than the boot because the sender is bound to the identity's transport and must be torn down with it; (3) the pre-existing SQLite-repository guard was WIDENED after this row found its hole — it matched `new SQLiteCommerce\w+Repository`, so `SQLiteBuyerOrderRepository` and `SQLiteCatalogPointerRepository` were exempt by naming convention rather than by rule. All three were mutation-checked to prove they can fire: a repo constructed outside the root, a fifth file naming the capability, and a root that stops installing the sender each turn the suite red.. **DR-3 correction.** The card bound `context.serviceUri` while the routes carried a SEPARATE `service_rkey` defaulting to `'self'` — and that rkey is what reaches the acting-for chain, so a listing-scoped grant could be evaluated against a listing the card never showed. `commerce/service_binding.ts` now derives the rkey out of the bound URI, refuses a stated rkey that disagrees, and refuses a URI whose authority is not the order's supplier. +5 tests, two mutation-checked |
| 3.7 | Update-rebind coordinator | §9.13 §16.5 | **DONE** | 3.3 | `UpdateRebindCoordinator` moves prior-CID authorizations, the install's new CID, and every listing pinned to the old CID in ONE Tier-0 transaction; the in-memory listing cache adopts only after commit. Authorizations carry the PRIOR manifest's params/result schemas + scope hash, so an update cannot retroactively re-judge in-flight work. `drain` expires, `lifecycle_continuity` does not (released explicitly). Stores resolve per use, so neither boot's wiring order can leave it half-built. 11 tests on a real SQLite Tier-0 db; 7 mutations killed (one initially survived and produced the "transaction fails AFTER the rebind" test). **NOW CALLED.** `plugins/update_service.ts` is the owner-facing flow the previous note said was the real owner of this wiring, and both composition roots build the coordinator. TWO STEPS, like install and for the same reason: `prepareUpdate` fetches, authenticates and vets a candidate and returns what the owner must SEE; `confirmUpdate` applies the one they saw. Collapsing them would make the act of LOOKING at an update apply it, which is exactly the "silently" §16.5 forbids. THE PUBLISHER COMES FROM THE INSTALL, never from the caller — one line, and the most important one here: a caller-named publisher would let anyone re-point an install at their own pack and inherit the consent the owner gave to somebody else's. A release for a DIFFERENT `plugin_id` is refused for the same reason one level down. The gates are not re-implemented: `vetReleaseManifest` was EXTRACTED from `finishBegin` so the update door runs exactly the install door's list (normalize, ingest-identical validate, runner-only, feature support, min protocol) and a second copy cannot drift — anything that could never have been installed here cannot arrive by update either, with the same code. §16.5 RE-CONSENT IS CONTENT-BOUND, not a boolean: `confirmUpdate` echoes the findings the owner accepted and the coordinator refuses unless they equal EXACTLY what it detects itself. A boolean says nothing about what the yes was FOR, and the whole risk is a wider escalation riding along on a consent given for a narrower one; four mutations prove the point (accept-any, partial, same-count-wrong-content, right-capability-milder-escalation) and all are killed. The confirm is CAS'd twice — on the reviewed CID and on the install's own CID — so a re-prepare in another tab cannot apply something nobody looked at, and the review is spent on success only, so a `requires_reconsent` refusal leaves it usable when the owner accepts. Routes: `POST /v1/plugins/update/prepare` + `/confirm`, owner-only (a plugin able to update itself is a plugin able to grant itself anything, one version at a time), with a transient failure as 503 and a permanent one as 409 because the two ask the caller for different things. A coordinator refusal is never a 200. 23 tests against a real SQLite registry and the real verifier seam; 17 mutations killed. **Design note kept:** discovery is owner-initiated. A release rkey is content-derived (§5), so the next version's key cannot be computed — finding it needs a repo LISTING, which Core has no transport for. A lister added later feeds this same `prepareUpdate`. The prepared candidate lives in memory with a 15-minute TTL: losing it costs a re-prepare, which re-fetches and re-verifies, and that is the safe direction. Re-entrant tx runner means `apply` must be called top-level (documented). **Open:** nothing |
| 3.8 | Prior-major lifecycle continuity handlers | §9.13 | **DONE** | 1.7 3.7 | Routing is pinned by the ORDER, per spec: `commerce_order_refs.serving_manifest_cid` records which manifest admitted the order, and provider ingress answers a lifecycle query under THAT manifest's contract via `buildContinuityEnvelope` — prior `params_schema`, `result_schema`, scope hash, action class, all read from the continuity authorization, never the live manifest. The current manifest is not consulted at all, so a capability that left it entirely is still served. No retained lane → typed `lifecycle_continuity_unavailable`, never a silent answer under the wrong contract. A `drain` entry alone does NOT admit a new lifecycle request. 6 new tests (28 in file), 6 mutations killed. Release is now GATED: `countReservedByServingManifest(cid)` (both repos) drives `releaseContinuity`, which refuses while any order that manifest served is still open and reports the count — releasing early would strand that buyer mid-order. An end-to-end scenario walks it: Sancho orders under manifest 1, ChairMaker ships manifest 2, the lane refuses to close while an order is open, then closes once it settles. 2 further mutations killed. The `submit_order` wire path now exists: provider ingress admits an inbound order in compiled Core BEFORE any runner is asked (§9.9), stamping `servingManifestCid` with the install's current CID — which is what makes §9.13 routing reachable in production rather than test-only. Core keeps idempotency/capacity/reservation; the plugin decides only whether to take the business. `replay`, `rejected`, `processing` and `conflict` all answer WITHOUT dispatch, so a retry storm cannot become a dispatch storm; no commerce runtime fails closed (`commerce_unavailable`). 4 mutations killed (two initially crashed the suite rather than surviving and were redone shape-preserving). The round trip is now CLOSED: `transformInboundOrderResult` (wired at `workflow_plane.ts`, so both boots get it) intercepts a completed `submit_order` and replaces the runner's answer with the acknowledgement Core signed via `decideOrder`. A runner answer Core cannot read leaves the order UNDECIDED for the sweeper rather than guessing at intent; a throw in the seam sends the runner's own answer rather than losing a completion that already landed; the buyer is always the authenticated sender, never a runner-chosen field. 20 new tests, 4 more mutations killed. All three §9.9 outcomes are carried, including `counterproposal`: the seam checks shape only and `decideOrder` owns lineage (§9.9) plus registration through the quote family (§9.8 audience binding). 2 further mutations killed **STATUS CORRECTED 2026-08-10 (PART to DONE).** The row named no outstanding gap — it read as a complete implementation and was still marked PART, which is the FOURTH row this review has caught lying, and the third lying in this direction. Re-verified against the code rather than against the row: `serving_manifest_cid`, `buildContinuityEnvelope`, `lifecycle_continuity_unavailable`, `countReservedByServingManifest` (both repos) and `transformInboundOrderResult` all exist; the transformer is wired in `packages/home-node/src/workflow_plane.ts`, the shared composition helper, so both boots get it; `admission.ts` carries a real `counterproposal` branch. `continuity_release.test.ts` + `provider_ingress.test.ts` = 80 tests green. A stale PART is not harmless: it is indistinguishable from work someone still owes, and it sends the next reader to re-derive a conclusion that was already reached. |
| 3.9 | Buyer + Supplier plugin manifests and runner SDK | §8.1 §8.2 §6 | **DONE** | 3.1 3.4 | `commerce/reference_manifests.ts` ships BOTH packs as product artefacts, not test fixtures — a manifest is a published contract and a pair living only in a `.test.ts` is one nothing can install or review. Supplier declares the four §9.9–§9.11 capabilities as `provider`; buyer declares three as `tool` (a buyer advertising `provider` would put its purchasing on a lane any peer can drive). The REAL validator found four inventions: `action_class: 'commit'` and `privacy_class: 'business'` are not §5 vocabulary, capability ids must be reverse-DNS with hyphens not underscores, and runner capabilities must declare `effects.idempotency`. Buyer `place_order` declares `unsupported` deliberately: it is an effect on ANOTHER node and §15.5 asks THEM to dedup, which this buyer cannot verify — `supported` would authorise auto-retry on a promise nobody here can keep, so an interruption surfaces as `outcome_unknown` instead. `plugins/runner_sdk.ts` gives a runner claim → typed job → answer, explicitly NOT a security boundary (Core re-validates at the bridge). Writing it reproduced the exact bug it exists to prevent: the first version validated params against `schema_snapshot.params`, a field that does not exist (the snapshot IS the result schema), so the check read `undefined` and looked like a guard while being nothing — the real claim guard caught it, and params turn out to be validated twice inside the boundary already. The SDK also swallowed the guard's `terminalized` list, making a lane quiet-because-rejected look like a lane with no work; now reported. 11 tests against the REAL claim guard, envelope parser and workflow service; 4 mutations killed. **§25.6 STEP 1 NOW EXERCISED.** `reference_pack_install.test.ts` publishes each pack as a `plugin.release` at the content-derived rkey §5 requires — the CID computed over the NORMALIZED manifest's canonical bytes, because the normalized form is the stored form and a CID over the raw one would name a document the install never keeps — then drives `beginInstall` → pairing bind → `confirmConsent` through the real repo-proof verifier seam. The argument for shipping these as product artefacts rather than fixtures was that a manifest is a published contract; that argument is only worth something if the packs pass OUR OWN door, and a reference pack is precisely what a third-party author copies. Every gate a stranger's pack meets, ours meets: the ingest-identical validator, the content-address check (a pack served at another release's rkey is refused `integrity_failed`), the §20 unsigned refusal (being ours is not an exemption — the door does not know whose manifest it holds), and §18.1's two-installs-two-consents (each pairs its OWN runner device; a shared runner is a shared authority). The consent card is asserted to be computed LOCALLY, never repeating a publisher's claim about itself. WRITING IT FOUND A GAP IN THE TEST, not the code: the first version skipped the pairing ceremony and read as if consent alone activated a runner. It now asserts the refusal BEFORE the bind, and that a DIFFERENT device cannot activate the one that was bound. 8 tests, 4 mutations killed (content-address dropped, unsigned accepted, consent accepts any device, device verifier skipped, expired pending activates). **A NOTE ON TWO UNKILLABLE MUTATIONS, recorded rather than papered over:** the device-binding guard's clauses are mutually redundant at runtime — on an unbound pending, `install.deviceDid === undefined` and `deviceDid !== install.deviceDid` each refuse alone — so no single shape-preserving mutation is observable. Removing the whole disjunction is COMPILER-killed: the `deviceDid === undefined` clause is what narrows the type for the device-verifier call below it. Defence in depth plus a type-level dependency, not a coverage hole. **Open:** nothing in-repo; publishing to a live PDS is WS-8's control plane |

---

# WS-4 — Restore, continuity and durability

| ID | Deliverable | Spec | Status | Depends | Definition of Done |
|----|-------------|------|--------|---------|--------------------|
| 4.1 | Restore-epoch fence + pre-backup quote voiding | §16.2 | **DONE** | 2.4 | Epoch monotonicity inside the aggregate; capacity cannot be resurrected through the quote path |
| 4.2 | **Validated commerce archive restore** — operational tables restored as EVIDENCE, counters never adopted *(owner decision 2026-08-07)* | §16.2 | **DONE** | 0.2 0.3 2.4 | The fence is armed and discharged: the import writes a durable marker in its OWN transaction; boot reads it and takes `establishAfterRestore` (epoch increment + capacity void + fence receipt) instead of adopting the live epoch; the marker clears only after publication succeeds and is left set on failure so the next boot retries. Unreadable kv reads as "fence owed". Not exported. 5 mutations caught. Preflight now proves the archive's STRUCTURE before a row is written: all-six-or-none (a pre-commerce backup still restores; a torn set never does), row shapes, and the three cross-table references — order→quote, use→quote, chain→order and chain→receipt. All-or-nothing, every finding reported, no row contents in the refusal. 5 mutations caught, one of which exposed a test that passed for the wrong reason. **Open:** nothing. (Status was stale at PART after the fence, preflight and boot wiring all landed; the note already recorded nothing open. Corrected during WS-2.4, which is 4.2's epoch dependency and is now itself DONE.) |
| 4.3 | Per-order restore reconciliation ceremony | §16.2 | **DONE** | 4.2 0.3 | `reconcileRestoredOrder` clears `reconciliation_required` against the buyer-presented order proposal, digest-bound to the supplier's own acknowledgement, and stores it so the order can describe itself again. Layered aggregate check + repository CAS. The owner surface and the bulk operation landed: `listAwaitingReconciliation` + a pure census (oldest first, distinct-buyer count, decided orders included because a frozen decided order is the worse case) behind owner-only `GET /v1/commerce/reconciliation`, registered at the composition root with a `/v1/commerce` authz prefix. The bulk operation REPORTS and cannot repair — the ceremony checks the buyer's held proposal against the digest this supplier signed, and a re-adopted order has nothing local to check against, so a sweep would have to invent the terms (the thing X-10 forbids one layer up); the response says so in `clearedBy`. `makeOwnerGuard` was duplicated in run.ts and watch.ts and is now lifted into owner_guard.ts rather than copied a third time. 4 + 6 mutations caught. **Open:** nothing. (Status was stale at PART after the owner census route and the bulk REPORT-only operation landed; verified against `GET /v1/commerce/reconciliation` in the route table.) |
| 4.4 | Genesis-side restore prerequisite | §16.2 | **DONE** | 0.3 | Genesis half landed with 0.3 (`admitted_epoch` + `reconciliation_required`). Cancellation half closed here: `resolveCancellation` refuses a pre-restore or awaiting-reconciliation order BEFORE writing its receipt, so no durable evidence of an undecided decision survives and a retry after reconciliation still gets a real answer. 2 mutations caught |
| 4.6 | **Core answers the reconcile lane** — §12.7 answered in compiled Core, never dispatched to a runner | §12.7 §16.2 | **DONE** | 4.2 4.3 | `reconcile` and `reconcileRestoredOrder` were reachable only from tests; ingress authorized `order_reconcile` and then handed it to a plugin, which cannot know the answer and whose one plausible guess — `never_received` — is the outcome that authorizes resubmission and which §16.2 makes illegal against a held signed acknowledgement. Core now answers it before the binding checks (so a paused/updated/uninstalled plugin cannot take the disaster-recovery lane dark), a second `ok` variant carries the answer, and the responder widens to `status:'ok'` + `result`. The two hand-built `service.response` body builders collapse into one, because they had already drifted and a field missed by either drops an answer silently. 5 mutations: 3 killed by tests, 1 by the compiler (a consumer cannot skip the new branch and still read `taskId`), 1 survives and is documented — the gate forces the payload buyer and authenticated sender equal, so no test can tell them apart; mitigated structurally by having the gate hand over the DID it authorized |
| 4.5 | Pause / uninstall / update semantics | §16.3 §16.4 §16.5 | **DONE** | 3.7 | Uninstall REFUSES while commerce obligations are open (undecided orders + non-terminal chains), with a typed error carrying the count; `delivered` inside its dispute window counts as open. 4 mutations caught. **UPDATE (§16.5) closed:** "an update cannot silently widen from catalog read to order submission" had no implementation — the rebind coordinator authorized drain + continuity and adopted the new CID without ever comparing what the owner consented to. `detectUpdateWidening` is a pure one-directional comparison (narrowing needs no permission) over five axes: new capability, raised action class, raised privacy class, grown data scope (context ceiling, category, persona — including DROPPING the persona list, since absent means no restriction), and weakened idempotency, the quietest of the five. Unknown and absent class values rank at the CEILING, so `action_class: "totally_safe"` cannot sail past. Wired into `UpdateRebindCoordinator.apply` BEFORE the transaction and refused as `requires_reconsent` carrying every finding; nothing moves. 5 mutations caught. **PAUSE (§16.3):** already correct and now safe to leave permissive — pause stops dispatch and provider answers but WS-4.6 makes reconcile Core-answered, so a buyer can still resolve an order against a paused plugin; pause is reversible and must not refuse. The count is now SCOPED per install. `commerce_order_refs` gained `serving_install_id` alongside `serving_manifest_cid`, and the two are kept because they answer different questions and diverge exactly when it matters: a plugin update moves the CID and keeps the install, so "is this INSTALL still on the hook" is a question the CID cannot answer. The admission seam now carries both as ONE object rather than two positional strings — same type, opposite meanings, and the compiler would have had nothing to say about a transposition. `inFlightCount(installId?)` stays node-wide when unscoped; scoped, it counts reserved orders by install and joins the non-terminal status chains to their order refs AT THE ROOT, which is where the existing comment already said a cross-store question belongs. A chain whose order reference is gone counts as OPEN — an obligation whose provenance we cannot establish is not one to dismiss on a teardown. Orders admitted before the column carry '' and block nobody, which is the right trade pre-release: the alternative is every legacy row blocking every teardown for ever with nothing an operator can do. TWO BUGS OF MINE, both caught by the suite rather than by reading: the INSERT gained the value but not the column name (every reservation then failed as a "concurrent race"), and the SELECT list did not gain the column at all — so `String(row.serving_install_id)` produced the literal text "undefined", a value that compares equal to nothing and looks like data. The read is now `?? ''` for exactly that reason. **Open:** nothing |

**End-to-end scenarios (§24, §25.6).** Four in-process journeys now exist:
`procurement_scenario` (the spine, engines called directly),
`procurement_lane_scenario` (one order through the real plugin lane),
`procurement_journey` (the buyer's whole discovery-to-delivery sequence), and
`disaster_recovery_journey` (§16.2/§12.7/§25.3 — an order carried through a
supplier restore: arrival evidence, re-adoption, the frozen order and its
census, the ceremony that clears it, the watermark refusing a delayed
pre-restore write, and the archive preflight refusing a torn backup). The
§25.6 two-live-node journey stays manual and is not claimed by any of them.

**4.2 was rated critical** because commerce tables fell through the generic
archive import path with nothing invoking `establishAfterRestore` afterwards —
capacity resurrection by the front door, making 4.1 moot in the one scenario
4.1 exists for. Both halves are now closed: the fence answers the counter
question, the preflight answers the structural one.

---

# WS-5 — Catalog and discovery (Phase 2)

Exit (§24): *a buyer with no prior supplier reference discovers and successfully
quotes a live supplier from proof-bound catalog data.*

| ID | Deliverable | Spec | Status | Depends | DoD |
|----|-------------|------|--------|---------|-----|
| 5.1 | Catalog pointer + immutable snapshot publication | §10.2 | **PART** | 1.8 3.5 | The BUILDER (1.8) now has an owner surface: `POST /v1/commerce/catalog/{import,publish,withdraw}` in `server/routes/commerce.ts`, all three behind the same boot-minted owner capability as `/v1/run` — a catalog is what this business sells and at what terms, so publishing one is the owner's commercial act and no plugin's. Publication alone carries the §16.2 fence (`commerceAvailability()`): a snapshot advances a chain buyers follow and a restored node's memory of what it last published is exactly the state a backup carries stale, whereas reading a spreadsheet cannot fork anything and a withdrawal ENDS a chain rather than extending it — fencing the tombstone would remove the owner's ability to stop selling at the moment they most want to. Three refusals to be helpful: `default_scheme` is required (it decides how every bare identifier is READ); the supplier DID comes from this node and is never a body field (a `manufacturer_sku` is only unambiguous scoped to its issuer, so honouring a caller-supplied one publishes under someone else's scope); an empty `items` array publishes but a MISSING one is a 400, so a dropped field cannot silently retire a live catalog. The previous pointer is validated by the CONSUMER's own `validateCatalogPointer` and its digest DERIVED rather than repeated — a second field only creates a way to disagree with the pointer just supplied — and a malformed `previous` is refused rather than treated as absent, which would publish a genesis at sequence 1 and fork the live chain. 33 route tests, 7 mutations killed. **CONSUMER HALF NOW WIRED** — `POST /v1/commerce/catalog/ingest`, owner-only. The old ledger reason was right and the resolution KEPT the boundary rather than crossing it: `ingestCatalog` fetches, Core makes no outbound HTTP, and a route that CONSTRUCTED a transport would put egress behind an owner endpoint where the gates cannot see it. So the composition root — the half that owns transport — calls `installCatalogFeedTransport`, the route only asks whether one exists, and Core stays the half that VERIFIES: pointer advance, snapshot digest, per-page proof, page order, one shared byte/time budget across the whole ingest. The server installs `nodeAuthedTransport` with NO headers, and the empty object is the decision rather than an oversight — a catalog is a public publication, so there is nothing to authenticate with and a credential sent to a supplier's feed URL is a credential given away. A node with no transport refuses `no_catalog_feed_transport` rather than reaching for a global `fetch`; that is the whole reason the registry exists, so it is the first test. Pages are addressed by a `{index}` TEMPLATE because a wire cannot carry the `pageUrl(index)` function, and a template with no slot is REFUSED — the ingest would catch it (each page carries its own index) but would report an operator's typo as "the supplier served the wrong page". `previous_pointer` absent and null both mean "this consumer has accepted nothing", and a mutation forcing null is killed, because that is what turns a replayed old catalog into an acceptable first sighting. 15 tests, 6 mutations killed. The fixtures are built by the REAL `buildCatalogSnapshot`/`buildCatalogWithdrawal` after hand-rolled ones were refused by every validator — a fixture that guesses at digest domains tests the guess. **PUBLISH HALF NOW WIRED TOO.** `commerce/catalog_record_writer.ts` + `installCatalogRecordWriter`, same split as the ingest: Core owns the ORDER because the order is the rule, the composition root owns the WRITE because writing a repo is I/O. THE ORDER IS THE WHOLE POINT — snapshot first (immutable, content-addressed, so a retry writes the same bytes to the same place and a crash after it costs nothing), pointer LAST and only if the snapshot landed. A pointer naming a snapshot that is not there is the one failure a consumer cannot work around: it fetches, gets nothing, and cannot tell a supplier mid-publish from a broken one. The reverse order is what an implementation written from the DATA model rather than the FAILURE model produces, because the pointer is the smaller record and the obvious first thing to update. The snapshot is keyed by its own digest and NOT CAS'd (a CAS would make a retry fail on success); the pointer is keyed by the catalog — one per catalog, because it IS the head and a second key would be a second head — and CAS'd on the expected head, which is what stops a publisher racing itself into a fork. A lost pointer write is reported honestly and leaves the snapshot durable, which is the SAFE half: consumers still see the previous publication, and a retry only rewrites the head. A tombstone that also carried a snapshot is refused — it would say two things at once. The route now BUILDS and PUBLISHES, and the two answers are tellable apart: a node with no repo still gets the records (the useful half for publishing by hand) and is never told they went anywhere. The server's writer re-checks repo identity before every write, because writing a catalog into somebody else's repo would publish this business's terms under another supplier's name. 8 tests, 7 mutations killed; one survived first because the test published against a NULL expected head, which makes "the snapshot is not CAS'd" and "the snapshot is CAS'd on the same value" the same bytes. **Open:** page DOCUMENTS are served over HTTP rather than as repo records, so hosting them is WS-8's control plane; mobile installs no feed transport, for the same §10.3 reason its connector executors refuse — a phone cannot report the address it actually connected to, so it cannot make the re-check the policy depends on |
| 5.2 | Supplier catalog import (CSV first) + pre-publication validation | §12.1 FR-S2 FR-S3 | **DONE** | 1.4 | `commerce/catalog_import.ts`. ALL-OR-NOTHING is the design, not a policy: a snapshot is full state (§10.2), so a partial import publishes a catalog that silently omits products and the omission is invisible in the record — buyers see a supplier who does not stock the thing. Unknown COLUMNS are findings too (a column silently ignored is one the supplier believes they published). Validates against the REAL vocabularies rather than its own: `unitDef` for the closed §9.2 units, per-unit fraction scale on quantities, importable §9.3 schemes, duplicate identity refused rather than merged (§9.4), variant parents resolved after all rows so file order does not matter. `defaultScheme` is REQUIRED, because guessing between `gtin` and `sku` picks the product's identity. Whitespace trimmed, case NOT folded — a trailing space is a spreadsheet artefact; two spellings for one unit is how implementations disagree about quantity. Hand-written CSV splitter (quotes, doubled quotes, embedded commas) rather than a dependency in an auditable package. Deliberately does NOT check leakage — `buildCatalogSnapshot` owns that gate alone, and a test pins the division by passing a secret in a known column through import and watching publication refuse it. 17 tests, 6 mutations killed |
| 5.3 | Publication leakage gate — secret-shaped-token detector + closed public-field vocabularies | §12.1 §23 | **DONE** | 5.2 | `commerce/catalog_leakage.ts`, wired INSIDE `buildCatalogSnapshot` before pagination and before any digest — a gate beside the publisher is a gate the next caller misses, and this is the one place in the pack where a miss is unrecoverable (a snapshot is published, indexed and content-addressed; it does not un-publish). Two independent rules, neither subsuming the other: a CLOSED public-field vocabulary that refuses `internal_cost` / `supplier_notes` / `erp_connection` by not knowing them, and a secret-shaped-value detector for the residual case a legal field carries a credential (vendor keys, GitHub/Slack tokens, AWS key ids, JWTs, PEM blocks, credentials-in-URL, labelled secrets). Findings name the FIELD, never the value — echoing a secret into a log turns one leak into two. Refused subtrees report once, not once per leaf; the report cap is stated rather than silent. Adding it failed 12 existing tests whose fixtures use a bare `sku`, which was a real gap in the vocabulary (a SKU is printed on the packaging) — admitted along with `mpn`, with the §9.3 duplication tension recorded rather than hidden. 26 tests, 6 mutations killed including value-echo and silent truncation. **Open question for the owner:** `list_price` is publishable on the reading that an advertised price is the supplier's own disclosure and a real price still comes from a signed quote; if §14.3 competitor-resistance forbids it, drop the three price fields. **DR-4 correction.** §12.1 step 10 asks for the structured-identifier PII patterns "PLUS a secret-shaped-token detector" and only the second half was built; the first half was already in Core running on the egress path, so nothing looked absent. `detectPII` is now wired in behind a distinct `personal_identifier_value` refusal (different code from a credential: one means rotate the key, the other means a person's details are in the export). **This sentence has been rewritten twice because the rule was wrong twice, and that history is the entry.** It first said product-number fields are scanned "only for classes with non-alphanumeric structure" — a principle, which excluded more than it justified and let a valid card number in a `sku` column publish clean. Correcting it by admitting the numeric classes back over-corrected the other way: `CREDIT_CARD` accepts 13-19 digits and a GTIN-13 is thirteen, so about a tenth of honest catalogs became unpublishable. The rule is now a LIST OF MEASURED COLLISIONS with a stated boundary each, and no principle: `PHONE` always in these fields (the US pattern is not start-anchored, so it finds a ten-digit window inside any longer run); `CREDIT_CARD` below fifteen digits (a GTIN maxes at fourteen — measured, `5901234123457` and `4901234567894` are real GTIN-13s that pass Luhn); `AADHAAR` only when the match carries no separator (twelve digits is also UPC-A — `712345678904` is a valid one, while a real Aadhaar written `2345 6789 0123` keeps its spacing and stays refused); `BANK_ACCT` never excluded, because sixteen bare digits is no GTIN. Two further classes are excluded as a CHOICE rather than a measurement, and the difference is stated because it matters: `PAN` (`[A-Z]{5}\d{4}[A-Z]`) and `IFSC` (`[A-Z]{4}0[A-Z0-9]{6}`) compete with supplier-chosen `sku`/`mpn` values that have no length bound, no vocabulary and no check digit, so no boundary is derivable. Measured only that the collision is ordinary — `CHAIR2024B` was refused as a tax ID, `ACME012345X` as a bank branch code. Excluded on the asymmetry: a supplier cannot renumber their catalog, while a genuine PAN in a `sku` column is a deliberate act rather than the ERP-export accident this scan is for. Knowingly given up: a Diners Club number is fourteen digits, so a 13- or 14-digit card in a product-number field is not scanned. Free text keeps the FULL scan and will refuse a description quoting a product code, which is the accepted cost — the alternative is a gate that refuses honest catalogs, and a gate that refuses honest catalogs gets switched off. +14 tests, mutation-checked |
| 5.4 | AppView ingest: repo proof, snapshot digest/root, caps | §10 FR-A1 FR-A2 | **DONE** | 1.8 | Three pure modules under `appview/src/shared/commerce/`: `catalog-verify` (the §10.2 chain — pointer advance, snapshot commitments, the pointer↔snapshot binding, every page, and the caps checked on METADATA before a fetcher starts), `catalog-projection` (FR-A3/A5/A7), and `catalog-ingest-decision` (what an arriving record means). **An INDEPENDENT implementation, not a copy.** AppView deploys standalone and cannot depend on `@dina/commerce-protocol`; a verbatim copy is not available either, because commerce-protocol uses extensionless relative imports and AppView runs Node ESM — those pass under vitest's bundler resolution and fail in production, which is worse than either alternative. The FROZEN VECTORS are the gate instead: the unit test reads `conformance/vectors/catalog.json` and pins the digests plus every chain refusal STRING. First real port; the vectors held. Digest math and chain strings are shared; COMPOSITION refusals deliberately are not (Core says `catalog feed: …` about a fetch that lied, AppView says `pointer: …` about a record that lied — one wording would be inaccurate to one operator). The ordering problem is solved once: a snapshot no pointer names is stored as evidence and never indexed (that is how a withdrawn catalog comes back to life), a pointer whose snapshot has not arrived is held, and both Jetstream delivery orders reach the index through ONE decision function. 44 tests, 10 mutations — 1 SURVIVED and was fixed: the product key was separator-joined and the collision test used a pair that only looked like a collision. **WIRED:** three tables (`commerce_catalog_pointers` = authority / `_snapshots` = evidence, keyed by content digest / `_products` = the projection), two zod record schemas, and two Jetstream handlers. The table split is what lets both delivery orders reach the same index without either record making a snapshot current on its own. A withdrawal deletes the products and KEEPS the pointer row — that row is what refuses a silent relaunch under the same `catalog_id`. A PENDING pointer is deliberately NOT the predecessor: it indexed nothing, so counting it would refuse the supplier's next publication for a gap they did not create. A wiring test asserts the collection list, the schema map and the handler map all agree, because a record silently ignored looks exactly like a supplier who published nothing. 18 more tests. **CLOSED:** `tests/integration/commerce_catalog_ingest.test.ts` drives both handlers against REAL Postgres (`dina_commerce_test`), in real transactions, and reads every assertion back out of the tables rather than off a recorded call. 8 scenarios: both Jetstream delivery orders reaching the same index; a snapshot no pointer names staying evidence; a pointer whose snapshot has not arrived waiting rather than indexing an empty catalog; full-state replacement actually deleting the discontinued product; a replayed OLDER pointer leaving the live catalog alone; withdrawal deleting products and keeping the pointer tombstone; a withdrawn catalog not reviving when its snapshot is re-delivered; and both records being re-deliverable without duplicating a row. Mutation: removing the delete that makes replacement full-state kills two cases — the exact claim a stub cannot make, since it records `delete:products` either way. Writing it found a fixture error of mine rather than a code defect: the snapshot record NESTS the snapshot under `snapshot`, and a fixture that spread it at the top level is refused before anything is stored |
| 5.5 | Exact-variant index without name-based merging | §10 FR-A3 | **DONE** | 5.4 | `catalog-projection.ts`. Identity comes from the IDENTIFIER, never the name: the row key is a canonical rendering of the `ProductRef` and names are searchable text hanging off it. An index that merged on name would answer a buyer's question about product A with product B's reviews, and nothing downstream could tell that it had. `variant_digest` is part of the key, so exact variants sharing an identifier stay apart. Two items claiming ONE identity refuse the whole snapshot rather than dedupe (§9.4) — picking either one for the supplier is the merge FR-A3 forbids, and one product would silently inherit the other's standing. Every secondary identifier is indexed too, so a buyer holding the GTIN and a buyer holding the supplier SKU both find it. The key is LENGTH-PREFIXED, not separator-joined: a `custom` value is an arbitrary bounded string, so any separator can appear inside a field and splice two refs into one key — a surviving mutation proved the first collision test was wrong before the encoding was. The separate relationship projection is 5.8 |
| 5.6 | Search by identifier, category, text, region + bounded evidence | FR-A4 FR-A5 | **DONE** | 5.5 | `shared/commerce/catalog-search.ts` (pure) + the `com.dinakernel.commerce.searchCatalog` xRPC. TWO-STAGE: SQL narrows, the pure matcher scores, and every row SQL returns is re-checked — the narrowing is an optimisation and the scoring is the contract, so a WHERE clause that drifted from the matcher cannot silently change results. The narrowing is deliberately a SUPERSET; narrower would lose a candidate before the contract saw it. `retrieval_score_bp` is RECALL confidence, not permission and not the buyer's ranking (that is `offer_ranking` against signed quotes) — conflating them would let an index operator move commercial outcomes by tuning recall, the failure §10.6 exists to survive. Identifier weight (6000bp) exceeds category+text+region combined, because a GTIN match IS the product. Every candidate NAMES why it matched; a result nobody can explain is indistinguishable from a paid placement, and the validator refuses an empty `matched_fields`. Ties break on the row key, which carries no commercial meaning. A query with no signal returns nothing rather than the whole index. 8 mutations killed. **The §10.5 candidate is a FROZEN VECTOR** (`search_candidate.json`) — the first written for a consumer rather than a publisher: the protocol side asserts its validator accepts it, the AppView side asserts its projection produces it, and that pair is the only thing keeping a standalone deployment and a package it cannot import agreeing. It found a real bug on its first run: the projection was emitting `{scheme, code}` with an invented `iso3166-2` scheme where §9.0's `RegionRef` is `{scheme, value}` over a CLOSED vocabulary, and was dropping `issuer_did` on a `custom` region. Regions are now carried verbatim and only flattened for comparison. **DR-5 correction.** Every candidate answered `service_uri`/`service_rkey` as `self`, which is where a buyer sends the quote request. §10.2 gave a supplier no way to say which listing serves a catalog, so `CatalogPointer.service_rkey` was added (optional, additive — validated in both the protocol type and AppView's own copy of it), carried through ingest onto the product row the way `snapshotDigest` already is, and read by `toCandidate`. `self` remains the documented convention for a primary listing and is used only where the supplier has not said. A listing rkey carrying a URI separator is REFUSED at ingest, because the buyer's `parseAtUri` would otherwise drop the supplier's products with no explanation to anyone. +9 tests (5 protocol, 4 against real Postgres), mutation-checked |
| 5.7 | Withdrawal, expiry, freshness, incremental refresh | §10.4 FR-A6 | **DONE** | 5.4 | Three predictable rules. WITHDRAWAL deletes every product row and KEEPS the pointer row as a tombstone — the row is what refuses a silent relaunch under the same `catalog_id`, which is the signal a buyer needs to see. EXPIRY drops a row at search time rather than returning it flagged: a discovery result is what a buyer requests a quote against, and offering a row the supplier has already called stale invites the round trip §10.4 exists to avoid. The boundary is `>` not `>=` — the supplier said valid UNTIL — and a row with no `valid_until` never expires, which is the supplier's own claim rather than an omission to guess at. INCREMENTAL REFRESH is §10.2's full-state replacement: a pointer advance deletes then inserts inside one transaction, so no reader sees the new catalog half-applied over the old |
| 5.8 | Product relationship projection with provenance and disagreement | §10.7 FR-A8 | **DONE** | 5.5 | `shared/commerce/relationship-projection.ts` + two tables + a Jetstream handler for `com.dinakernel.commerce.relationshipClaim`. A PROJECTION OVER the exact-variant index, never a merge across it: an edge says "these two are related, here is who said so and how strongly", never "these two are the same". A merge is irreversible in practice — once two identities are one row, the reviews, order history and lineage of both are indistinguishable, and no later evidence separates them. THREE ORDERED THRESHOLDS, separate constants rather than one number with three call sites: show-as-related (3000bp) < inherit-standing (6000) < authorize-substitution (9000). An INFERENCE is arithmetically capped below the standing threshold whatever the model reports, and `mayInheritStanding` checks the source again so a hand-built edge cannot route around the cap. Substitution additionally requires a FIRST-PARTY `same_formulation_as`/`replaces` claim — being a variant of something is not permission to send it instead. DISAGREEMENT IS DATA: two claims naming different parents land on ONE edge, marked `disputed`, with both claims kept; a disputed edge never carries standing, whatever its confidence. Deleting the loser would be the silent merge §10.7 forbids. Edge confidence is the STRONGEST single claim, never a sum — three weak inferences agreeing is still three weak inferences. TWO TABLES because edges are DERIVED from stored claims rather than mutated: a withdrawal has to be able to un-dispute an edge, which an in-place update cannot do. `first_party` comes from the publishing repo, not the record — a self-declared trust level is not evidence. RECALL EXPANSION is wired into 5.6's search: a query by identifier also reaches products across edges at or above the SHOW threshold, capped per query so a heavily-claimed product cannot turn one lookup into a graph walk. A row reached that way scores 1200bp — below category+text, so an edge improves recall without winning on precision — names `related` in its matched fields, and carries the claim ids that put it there. A direct hit is never additionally paid for being related to itself, and expansion does not widen the freshness rule. 25 tests, 13 mutations killed | **REAL-POSTGRES INTEGRATION ADDED, and it found the worst defect in the lane.** The edge key joined its parts with a LITERAL NUL character — unambiguous in a JS Map and impossible in Postgres, whose `text` type rejects 0x00 outright (`invalid byte sequence for encoding "UTF8"`). Every relationship claim failed to index; §10.7 was entirely non-functional against a real database while its stub suite stayed green, because a Map stores a NUL happily. The NUL also made the source file read as BINARY to grep, which silently broke earlier audits of it. Now length-prefixed through the same `encodeParts` the product key uses, exported so there is ONE key encoding rather than two. `commerce_relationship_ingest.test.ts` (8) covers: one claim making one undisputed edge; re-delivery not duplicating; two claims naming different parents landing on ONE disputed edge with BOTH kept; a disputed edge carrying no standing at any confidence; withdrawal of the loser UN-disputing the edge (the property the two-table design exists for, and the one an in-place update could not provide); withdrawal of the last claim leaving no edge; confidence taking the strongest single claim rather than a sum; and an inference never reaching standing at a reported 10000bp. |
| 5.9 | Bounded top-N quote fan-out | §12.3 §20.17 | **DONE** | 3.2 | `commerce/quote_fanout.ts`. A HARD ceiling (`MAX_QUOTE_FANOUT`), not a default: without it one buyer tap becomes N outbound D2D messages to N independent nodes, and a peer repeating it has a flood with someone else's name on the envelopes. A caller asking for more is CLAMPED, not refused — refusing tempts a workaround that has no cap at all. Amplification test runs 10/100/1,000/10,000 candidates and asserts the outbound count never moves. ONE query per SUPPLIER, not per listing (six listings from one manufacturer would otherwise take six of eight slots and narrow the buyer's comparison). Trust ORDERS but never excludes: an unrated supplier sorts last and still gets asked, because excluding at fan-out makes the trust system self-fulfilling — no queries, no orders, no reviews, no trust. Deterministic down to the DID, so fan-out is not a function of someone else's pagination. A mutation showed the lower clamp was behaviourally redundant (`>=` already handles a negative cap); rather than delete it or leave it looking load-bearing, its real job — the REPORTED `capApplied` an owner sees — is now asserted and documented. 17 tests, 6 mutations killed |

---

# WS-6 — Ranking, privacy and disclosure

| ID | Deliverable | Spec | Status | Depends | DoD |
|----|-------------|------|--------|---------|-----|
| 6.1 | Hard filters before scoring | §13.2 | **DONE** | 1.5 | `commerce/offer_ranking.ts` — `applyHardFilters` runs BEFORE `rankOffers` scores anything, and the ordering is the rule: a weighted score can always be dragged across a hard requirement by making one factor extreme, so a requirement that participates in a score is not a requirement. Seven reasons (expired quote, currency mismatch, insufficient quantity, incomparable unit, region not served, too late, over budget), each REPORTED — a shortlist that cannot answer "why isn't ChairMaker here" is one the buyer cannot trust. Currency is never converted (that would be Core inventing a commercial term); an unreadable expiry reads as expired, not as no expiry. Pinned by a test giving a FREE offer that cannot meet the quantity and checking it never appears |
| 6.2 | Deterministic score + explanation | §13.3 §13.4 FR-B6 FR-B7 | **DONE** | 6.1 | Integer basis points throughout, ties broken on quote id not arrival order, score recomputable from the components shown. §13.4 is the load-bearing part: a missing PeerLens rating is NOT scored zero (which makes trust a barrier to entry for every new supplier) and NOT full marks (which rewards having no history) — the factor leaves that offer's score, `weightAppliedBp` travels with it, and the omission is named. Both failure directions are tested. DESIGN DECISION, got wrong first: price and lead time are RELATIVE (normalised across the shortlist) but trust is ABSOLUTE — normalising a rating would stretch the least-bad supplier in a weak field to full marks. My own test expected 10000 for the top offer, which only holds if trust normalises too. **Open question:** weights (60/25/15) are fixed in v1 rather than buyer-tunable, because tunable weights make two Dinas rank the same offers differently and there is nowhere yet to record the chosen weights with the decision. 24 tests, 6 mutations killed |
| 6.3 | Context projection per capability and task | §13.5 FR-P3 | **DONE** | 3.5 | `plugins/context_projection.ts`. `contextScopeViolation` already existed and describes itself as a fail-closed BACKSTOP for the case a producer skips scrubbing — but there was no producer, so the backstop was the only thing between the vault and a runner. Projection is built from ONE capability's declared scope; a capability declaring none receives NOTHING (silence in a manifest is an answer, not a request for defaults). Fields copied IN by name, never scrubbed out — a deny-list leaks every key nobody thought of, which are exactly the ones a new vault feature adds. Two distinctions kept apart deliberately: an ABSENT persona list (no restriction declared) vs an EMPTY one (a declaration of none); and an undeclared `max_context_items` read as ZERO not unlimited, matching the backstop so producer and checker cannot disagree. Exclusions are reported for the OWNER, who is entitled to know a capability asked for something its consent does not cover. 12 tests, 6 mutations killed |
| 6.4 | Hierarchical product evidence composition | §13.6 FR-B13 FR-B14 | **DONE** | 5.8 | `commerce/product_evidence.ts`. Exact and inherited evidence are never summed, never merged, always labelled — a single count collapses "this variant has 2 reviews" and "the family has 40" into a number describing no product anyone can buy, irreversibly. No function returns a bare rating: the headline type carries its own scope, because a bare number would be rendered beside the product name and lose the distinction at the last step. FR-B14 tested both ways. Null, never zero, when nothing is known. Writing it exposed TWO defects in 5.2 hidden by a cast — the importer built `{scheme:'sku'}` and cast to `ProductRef`, but `sku` is not a §9.3 scheme and `manufacturer_sku` REQUIRES `issuer_did` (two suppliers may both call something CHAIR-1), so every imported catalog carried refs the protocol validator rejects. Fixed by building + validating; `mpn` dropped as an importable scheme (it is issued by someone other than the supplier, so scoping it to their DID asserts an authority they lack). The leakage gate then caught a THIRD: `issuer_did` was missing from the public vocabulary, so no scoped ref could publish. 13 tests, 6 mutations killed |
| 6.5 | Progressive disclosure + price confidentiality | §14.1 §14.2 | **DONE** | 1.5 | Audience binding done (0.4 shipped, so the old open clause was stale — but a stale clause says the DEPENDENCY landed, not that the surface holds, so both halves of §14.2 are now driven). CLAIM ONE — customer-specific pricing is a private D2D record: six plausible per-customer price columns (`customer_price`, `contract_price`, `negotiated_rate`, `discount_tier`, `credit_terms`, a per-buyer map) are each refused by the publication gate for the SAME reason, which is the property worth having — the CLOSED public vocabulary does not need to have thought of the name. A published LIST price still goes out, because §14.2 protects customer-SPECIFIC pricing and a list price is the supplier's own advertising; refusing it would make the gate useless rather than careful. A leaking item refuses the WHOLE publication rather than dropping the offender, because a snapshot is content-addressed and published: shipping the rest would publish a catalog the supplier never reviewed. The verdict names the FIELD and never echoes its value. CLAIM TWO is the half that rots, because it is a claim about COPY: §14.2 says a private quote is "confidential, not anonymous — the supplier necessarily sees the authenticated buyer DID… the UI and consent copy must not imply that D2D signing hides the requester". Prose has no compiler, so there is one now: every string LITERAL in the commerce surface is scanned for a promise of anonymity, and the owner-facing order card is rendered and checked. Scanned rather than listed, because a test naming today's strings passes for ever while a new screen ships the forbidden sentence. Literals only, so a doc-comment DISCUSSING anonymity (as §14.2 itself must) is not a promise the product makes. 11 tests; 3 mutations killed, two of which inject the forbidden sentence into a real owner-facing headline — a copy scanner that cannot be made to fire is worthless. **Open:** nothing |
| 6.6 | Competitor and probing resistance | §14.3 §20.10 | **DONE** | 6.5 | `commerce/probing_resistance.ts`. The harm is in the AGGREGATE — a hundred legitimate quote replies draw the price curve — so no single-request check can see it. TWO defences, neither the other's fallback: a per-counterparty BUDGET over a window (the only thing that sees the aggregate) and a UNIFORM refusal (without which the refusal is the oracle, and a prober reads the shape of the "no" to learn the catalog before the budget runs out). The wire refusal is ONE constant, not a per-case message; the real reason lives in a field that never crosses the wire, and a test greps the constant for leaking words. Remaining budget is reported on SUCCESS only — a countdown on a refusal measures the policy and tells a prober how slowly to probe. Unknown budget is small but NOT zero (a stranger must be able to become a customer); counting is per counterparty, never global (a global counter lets one busy customer exhaust everyone's, converting a privacy defence into an outage a competitor triggers by being noisy). 12 tests, 6 mutations killed. **Open question:** default budgets (60/hr known, 5/hr unknown) are a guess at where a real customer stops and a prober starts |

---

# WS-7 — User experience

| ID | Deliverable | Spec | Status | Depends | DoD |
|----|-------------|------|--------|---------|-----|
| 7.1 | Commerce Pack install journey | §18.1 | **DONE** | 3.1 | `commerce/install_plan.ts` + `POST /v1/commerce/install/plan`. §18.1's one sentence IS the item — "Both" creates two installs/consent decisions, not one superset install — and it is a SAFETY rule rather than a UX preference: a superset install gives one consent record authority over both sides of a trade, so revoking selling would revoke buying and a compromised supplier runner would carry buyer authority. The plan REFUSES three ways it could blur: two roles sharing a manifest (the superset in disguise), "both" collapsing to one install, and a manifest carrying the other role's capability — the last CHECKED rather than assumed, because the manifests are data a pack author edits and an overlap is exactly the change that would silently give a buyer install a supplier's authority. The read side matches: `roleIsInstalled` answers PER ROLE, and the inbox route uses it — "is any commerce install present" is precisely the shortcut a superset would have made correct. It PLANS and does not install: the install machinery owns repo proofs, pending expiry and activation, and a route that did both would put a consent decision behind one tap. The reference packs stop being documentation-only and become the manifests this installs. 9 tests |
| 7.2 | Buyer settings | §18.2 | **DONE** | 7.1 | `commerce/commerce_settings.ts` + `settings_store.ts` + owner GET/PUT routes. SETTINGS ARE POLICY, and policy is an input to refusals — a fan-out ceiling stored on a phone stops applying when the server acts, and a blocked-supplier list only the UI knows is a list nothing enforces, so both live in Core beside the engines that read them. The ceiling is REFUSED above the protocol maximum rather than clamped (the planner clamps defensively at dispatch; this runs where the OWNER TYPES the number, and silently turning 50 into 8 leaves them believing something untrue). A supplier that is both preferred and blocked is refused rather than resolved by precedence — either answer guesses what the owner meant, and the guess that prefers a blocked supplier sends them business they said they did not want. Every finding is reported, not the first: an owner fixing a screen one refusal at a time gives up on the third round trip. WIRED into `/procurement/plan`, which now takes the ceiling and the block-list from SETTINGS and honours a smaller request but never a larger one |
| 7.3 | Supplier settings + inbox | §18.3 §18.6 | **DONE** | 7.1 3.3 | Supplier settings DONE, same store. Fails closed on a response policy it does not recognise — an unrecognised value means this build does not know what the owner asked for, and defaulting to `auto` answers customers on a guess. CREDENTIALS CANNOT REACH IT: §18.3 asks for credential STATUS, and a settings record is the most-read, most-exported, most-synced object a node has, so a connector entry carrying an api key/secret/token/password/private key is refused — and the finding names the FIELD without echoing the value, because reporting a leak by repeating it turns one leak into two. `quoteAdmissibility` is WIRED into the provider ingress BEFORE any probing budget is spent: a paused listing that still answered would be a supplier ignoring their customers rather than one who is closed, and spending a peer's budget on a question this node was never going to answer would penalise them for the owner's decision. It returns the SAME refusal code as a budget refusal (§14.3 — one refusal for every reason, or a prober reads which "no" they got). 23 tests. THE §18.6 INBOX is built too (`commerce/supplier_inbox.ts` + `GET /v1/commerce/inbox`): a projection over undecided orders, listing/connector settings and catalog freshness, ORDERED BY WHAT IT COSTS TO IGNORE rather than by recency — a stale catalog is a slow leak, an order about to time out is a customer who ordered and heard nothing, and sorting by arrival buries the second on the day it matters. An order whose `effect_phase` is `effect_started` is reported as UNRESOLVED rather than waiting: the boundary was crossed, so deciding now could double the work. Counterproposal controls are offered, not just accept/reject — a supplier who can only accept or reject will reject an order they would gladly have filled on other terms. A closed listing ranks BELOW a failing connector on purpose: it is usually a decision, and nagging an operator about their own choice is how an inbox stops being read. `clear` is reported rather than inferred from an empty list, because "nothing needs you" and "the inbox could not be built" render identically. Reading the list required adding `listReserved()` to the CommerceOrder AGGREGATE rather than reaching into the repository — ARCH-0's point, and a read costs nothing the aggregate was protecting. 12 more tests **DEEP REVIEW CLOSED TWO THINGS.** First, `clear`'s own comment overclaimed: it said the field was "reported rather than left to `items.length === 0`" while being exactly that. The distinction it wanted is real but lives one level up — the builder cannot fail, so the "could not be built" reading is answered by the ROUTE's 503. The comment now says what is true, because in this codebase a comment that overclaims is a claim someone will later rely on. Second, the route itself had NO test: the projection suite drove `buildSupplierInbox` directly and one journey walked past. That left all three of this endpoint's answers unchecked, and all three are DISTINCTIONS BETWEEN EMPTY LISTS — 200 `clear` (nothing needs you), 503 (this node has no commerce), 409 `supplier_not_installed` (you are not selling). Rendered as an empty list they read identically, and two of them are wrong in the direction that costs money: an operator reassured by a node that is not even asking. `__tests__/server/routes/commerce_inbox.test.ts`, 15 tests. My first fixture invented `{ role: 'supplier' }` for the install; `roleIsInstalled` reads `pluginId` against the reference manifest and ignores `role` entirely, so the 409 case passed for the wrong reason and its sibling failed. The fixture now uses `SUPPLIER_REFERENCE_MANIFEST.plugin_id`, which also means the test breaks if that id ever moves. |
| 7.4 | Comparison card | §18.4 | **DONE** | 6.2 | `commerce/comparison_card.ts`, built INSIDE the `/procurement/choose` route so the card travels with the decision — two clients deriving their own headline is how one of them ends up offering "Buy now". §18.4 asks for a result that stays useful on the GENERIC CardSpec fallback, so every baseline field is an ordered label/value line a renderer with no commerce knowledge can print, and an absent value reads "not stated" rather than leaving a blank where a number should be. It EXPLAINS rather than re-decides: the "why" lines are the ranking's own components (contribution, not raw factor score — an owner asking why this won is asking what moved the total), the incomparable list is read off `missing` rather than inferred, and a winner scored on less than the full weight says so. Every excluded offer is named with its reason, never counted. The empty case is a card with reasons, not an error. The verb is "Review order", never "Buy now" (Cart Handover). 11 tests |
| 7.5 | Approval card bound to one canonical order payload | §18.5 §15.2 FR-P5 | **DONE** | 0.2 | `commerce/approval_payload.ts`. Approval mints a payload covering every §15.2 field and a digest over it; execution REBUILDS the payload from what it is about to do and compares. **Why a payload and not `order_digest`:** the order digest covers the order and not WHICH INSTALL is about to send it, which capability, which manifest CID, which config revision, or which principal approved under what authority — a swap of any of those is a different act by a different actor with the same paperwork, and five bait-and-switch cases prove an order-digest comparison alone would pass every one. Its own digest domain, NOT a §9.12 wire domain: an approval never leaves the node, and widening the frozen list to hold a private record would break the vectors. The builder REFUSES a card that omitted a §15.2 field rather than binding a placeholder — the first version filled gaps with empty strings, and two bait-and-switch cases passed while proving nothing, because when the card supplies neither side of a comparison the field binds to a CONSTANT. `null` (the card showed no per-line price) and ABSENT (the card never said) stay distinct. **SUPPLIER HALF WIRED** into `settleInboundOrderDecision` BEFORE the decision is recorded — a runner answering `accepted` where the supplier approved `rejected` is refused and nothing moves; checking afterwards would leave the chain moved on something nobody approved. Fails closed when there is no order to bind against. 34 + 5 tests, 12 mutations. Two survived and both taught something: the payload kind in the digest prefix was redundant (it is already a canonical field) and was removed; the `(digest)` fallback is unreachable by construction and now says so instead of pretending a test covers it. **BUYER HALF NOW WIRED** through `commerce/buyer_executor.ts` (WS-3.7): the executor rebuilds the payload from the order about to be sent and refuses before anything is written or dispatched, so a re-planned order between the tap and the send never reaches the wire **THE TWO-HALF CHECK IS NOW ENFORCED.** The binding is only worth what the call sites are worth, and nothing stopped a THIRD path deciding or dispatching an order without it — which is precisely how the card and the order come apart. `__tests__/commerce/boundary.test.ts` now asserts the verifier is called in EXACTLY `buyer_executor.ts` and `order_decision.ts`: not at most two, because a cap alone would pass a build where one half stopped checking, and not at least two, because a third caller is a path nobody reviewed. Mutation-checked in both directions — removing the supplier's check and adding a third caller each turn the suite red.. **DR-2 correction.** The card was minted from a context taken VERBATIM out of the request body, so §15.2's last line — plugin install, capability, manifest CID, scope hash, config revision — bound whatever the caller claimed, and the binding check later compared that claim against itself. `commerce/acting_install.ts` now resolves those five from Core's own install registry at `prepare`: the body SELECTS an install and a capability, this node says what they are, and a body that states a different CID or config revision is REFUSED rather than silently corrected (the surface showed the owner something, and if it is not what would run the owner approved a description of a different act). `actingBusinessDid` must equal this node's DID; an unknown, inactive or capability-less install refuses 403; no registry refuses 503. +7 tests, three of them mutation-checked |
| 7.6 | Supplier-side approval payload | §15.2b | **DONE** | 7.3 | `buildSupplierApprovalPayload` (see 7.5), wired into `settleInboundOrderDecision` BEFORE the decision is recorded. Distinct from the buyer payload by construction: different kind, different bound fields, and a comparison across the two refuses on `kind` — the two authorize opposite sides of one trade and must never share a digest. Carries the §15.2b cancellation-resolution extension, which binds the status head the ruling was made against so a resolution approved at one chain position cannot be applied at another. Row was stale: the work landed with 7.5 |
| 7.7 | Reconciliation UX for `outcome_unknown` | §12.7 | **DONE** | 2.3 4.3 | `commerce/buyer_reconciliation.ts` (pure state machine + read model) + `commerce/buyer_orders.ts` (durable store, `commerce_buyer_orders`). THE RULE WITH TEETH: `never_received` is the only outcome authorizing resubmission, and it is legal only when the buyer presented NO supplier-signed evidence — a supplier that denies an order while holding its own signature must RE-ADOPT, so the buyer checks the LEGALITY of the answer rather than trusting it and stays parked when it is unsound. `received_unresolved` never authorizes resubmission however long it loops, and there is deliberately no buyer-side timeout converting it to terminal: a timeout would be the buyer deciding, on a clock, that an effect which may have fired did not. Retry intervals are clamped so a hostile supplier can neither park a buyer forever nor turn a re-poll into a spin. DURABLE BY CONSTRUCTION: transitions are values the caller persists, and `poll_count` is a column because §12.7 requires the loop to survive a restart — the crash that lost the acknowledgement is exactly when an in-memory counter resets. The stored acknowledgement is RE-VALIDATED on read (`rehydrateAcknowledgement`), never parsed-and-cast: it is a supplier's signed commitment and the row is editable by anything with the database open. READ MODEL: `resend` appears on exactly one state and only when it was earned — a mutation that offered it unconditionally SURVIVED until a test rendered a `never_received` row with no authorization, because every other path to that branch happened to be authorized. Nothing claims an ambiguous order failed; "it failed" is what invites the second order. 19 tests, 8 mutations. **WRITE HALF NOW WIRED** by the executor (WS-3.7), which creates the record and settles the first answer through this machine. A surviving mutation then exposed a real divergence: `received_processing` and `received_unresolved` had been collapsed into one state, where §12.7 puts the first in `submitted_unconfirmed` (the decision has not reached the external boundary) and the second in `outcome_unknown` (the effect MAY have fired). Split, with the reason recorded. **RE-POLL BUILT** — `commerce/reconcile_poller.ts`. A SWEEP over the unsettled list rather than a timer per order: a per-order timer is state to leak, to double-fire after a restart, and to forget to cancel when the order settles; the sweep re-reads the store each pass so a settled order simply stops appearing. It ASKS and does not DECIDE — every answer goes through `applyReconcileResult`, so the poller cannot drift from the submit path about what the same answer means, and it never authorizes a resubmission of its own accord however long it loops. A transport failure is NOT an answer: the record is left untouched and asked again. An order it can no longer describe is SKIPPED rather than asked, because asking without a digest and an idempotency key invites the one answer that must never arrive by accident. Bounded per pass. 10 tests. **RE-POLL NOW WIRED END TO END (both halves).** The ASK is `askReconcilePolls` — it fires the `order_reconcile` query on the same `service.query` egress every other outbound capability uses, advances `nextPollAtMs` through `applyReconcileResult` so the interval rules stay in one place, and counts `asked` / `unsent` / `undescribable`. `ReconcilePollSweeper` runs it on a tick both composition roots start through `startCommerceSweepers`, with no overlapping passes (the interval is not network-aware, and stacking passes would ask the same order twice against exactly the peer already struggling) and a `send` resolved per tick rather than captured (the outbound lane is torn down on an identity switch, and a tick holding the previous identity's sender would ask that identity's suppliers about this one's orders). THE CONTRACT CHANGE THE PREVIOUS NOTE ASKED FOR TURNED OUT TO BE TWO. First the split, as recorded. Then a second, smaller one that only showed up at the boot wiring: the record carried no way to REBUILD the question. `poll_count` was a column because §12.7 requires the loop to survive a restart, and after a restart the buyer knew it should ask and could not say what about — so `order_digest`, `idempotency_key`, `protocol_version` and `service_rkey` now live on `commerce_buyer_orders` beside it, written when the order is sent. The rkey is not a detail: a supplier may offer commerce on a non-default listing, and a query with no `service_uri` is checked against the default one, so omitting it would be refused by exactly the suppliers who run more than one. An order written before these existed reads as UNDESCRIBABLE and is skipped rather than asked, because a supplier's honest answer to a question with no digest is `never_received` — the one answer that must never arrive by accident. **AND THE APPLY HALF EXPOSED A BIGGER HOLE.** The buyer lane was a one-way street: `commerce/buyer_response.ts` is the first thing on this node that RECEIVES a supplier's answer. Until it existed a supplier could accept an order, sign the acknowledgement, answer the reconcile — and every one of those reached the D2D ingress and stopped, so the buyer asked for ever. Invisible for the usual reason: an order that never settles is indistinguishable from a supplier who has not answered yet, and the loop that hides it is the same loop that exists to survive a supplier who really has not. One seam serves BOTH `submit_order` and `order_reconcile`, because the two carry the same document and a second reading of an acknowledgement would eventually disagree with the first; `acknowledgementToResult` moved into the state machine for the same reason. It is called DIRECTLY from the receive pipeline rather than through an injectable both roots install — the injectable version of this exact seam on the supplier side (`ingressResultTransformer`) shipped wired on the server and unwired on the phone, and the phone is the product. The supplier is the TRANSPORT-AUTHENTICATED sender, never a field in the payload: the record is keyed on `(supplierDid, purchaseOrderId)`, so binding to the body would let any peer settle somebody else's order by naming it. A `status` other than `success` is `not_an_answer` — that is a fact about the supplier's runner, not about the order. Both payloads go through the commerce-protocol validators, so a tampered digest or a fourth outcome value is refused rather than believed. 16 + 10 + 13 + 5 + 8 tests, 22 mutations killed. The 5 drive the REAL receive pipeline from a sealed, signed envelope, because the half that keeps going missing on this workstream is not the rule but the reach; the 8 are a real-SQLCipher round trip, added because a mutation swapping two adjacent bind parameters survived the entire suite — every other test reached this lane through the in-memory double, which stores whole objects and cannot disagree with itself about columns. Two more survivors said the same thing about the describability check: the test built a record with NOTHING filled in, so any one of the four conditions caught it and three were invisible; it is now driven field by field. And one defect I caught by reading rather than mutating: the first ask-advance expressed every record as `received_unresolved`, which would move a `submitted_unconfirmed` order into `outcome_unknown` because the BUYER asked about it — reclassifying an order as possibly-committed on the strength of our own question. The advance now uses the outcome the record is already in. **Open:** no boot supplies a held-evidence reader (`installHeldEvidenceReader` is node-level and both the ask and the apply read it, so the two can no longer disagree) — not a wiring gap: an order parked in `outcome_unknown` holds no acknowledgement by construction (terminal states carry one and are never re-polled), so the evidence §16.2 wants needs a buyer-side STATUS RECEIPT store. **THAT STORE NOW EXISTS AND IS WIRED — gap corrected 2026-08-10, it had gone stale.** `SQLiteBuyerStatusRepository.evidenceChain` is the store, `createCommerceRuntime` builds it, and `makeHeldEvidenceReader` is installed at BOTH composition roots over the runtime's own repositories rather than a second set over the same tables — a second set is a second chance for one of them to disagree with the other. So the §12.7 legality rule is live end to end: this buyer presents what it holds, and a supplier's `never_received` over presented evidence is refused rather than believed |
| 7.8 | One command/projection contract shared by mobile and web | FR-P10 | **DONE** | 3.6 | The READ half exists for the order surface: `GET /v1/commerce/orders/unsettled` returns `describeOrderForOwner`'s output, so mobile and web render the same headline and the same action list rather than each deriving one from a state name. Two renderers would eventually disagree about whether `outcome_unknown` means "failed", and that reading is what puts a duplicate order on the wire. Owner-only: an order the node cannot account for is a list of exactly where its money might already be. **COMMAND HALF NOW SHIPPED** — `POST /v1/commerce/orders/command`, owner-only, with ONE design rule: the command is authorized by the SAME projection that offered it. `describeOrderForOwner(record).actions` decides what may be performed, so the card and the command cannot disagree — and the divergence that matters runs in one direction, a command allowing what the card never offered, on the single state where the wrong answer puts a duplicate order for real goods on the wire. Two of the four actions are commands at all: `wait` is the ABSENCE of one and `view_acknowledgement` is a read the projection already answers, so both are refused rather than given an invented side effect. `reconcile_now` asks about EXACTLY that order, now, through `askReconcilePolls`'s own `only` filter rather than a second ask path — and it deliberately ignores the backoff, because the backoff exists to stop the AUTOMATIC loop spinning against a slow supplier and is not a limit on what the owner may do on their own node; it still advances the clock, so one tap does not become two questions. `resend` runs through `submitApprovedOrder` in a MODE rather than a parallel function, so the authority check, the §15.2 binding rebuilt from the order actually about to go, and record-before-send are shared by construction; the only difference is what an existing record means. It REBUILDS the approval rather than reusing the first attempt's, because the order is unchanged but which install, capability, manifest CID and config revision are about to send it may not be. And it SPENDS the authorization before the send: the fresh record clears `resubmissionAuthorized`, so an owner tapping twice on a slow network gets a refusal rather than a duplicate. 25 tests + 3 real-SQLCipher executor tests, 11 mutations killed. Two survived first and both were the same lesson as WS-7.7's: a mutation swapping `create` for `put` was invisible because the settle at the END of the submit writes the record anyway AND because the in-memory double upserts where real SQL updates — so there is now a test that reads the store from INSIDE the send, and a real-database one proving a first submission INSERTS (a `put` would leave an order on the wire with nothing recording it). Also caught, in my own test rather than in the code: `it.each([undefined, …])` against a helper with a DEFAULT parameter silently tested the owner, so the one caller shape that matters most — Brain's in-process transport, which carries no callerType — was never exercised. The default is gone. **QUOTE SURFACE NOW HAS THE SAME TREATMENT.** `commerce/quote_read_model.ts` + `GET /v1/commerce/quotes`, owner-only. A quote's state is a JOIN — voided, validity window, spent capacity — and a client computing "live" from any ONE of those is wrong in a specific, expensive way: an owner told a voided quote is live will honour terms this node has already refused to stand behind, and a supplier who says a thing twice and means it once has a dispute rather than a sale. So the join happens once, in Core. THE PRECEDENCE IS THE SAME ORDER `QuoteFamily` USES WHEN IT REFUSES, and that is the property rather than a coincidence to be tidied: a card ranking expiry above voiding would tell an owner a quote merely lapsed when this node disowned it, and they would reasonably re-offer the same terms — which is exactly what §16.2's restore-voiding must not invite. `void` is offered on ONE state, because it is the only one where voiding changes anything; offering it on an expired quote invites an owner to cancel something already over and then wonder what they just did. An unreadable `max_uses` reads as ONE (the protocol default) rather than NaN or unlimited: the reading that offers the least is the safe one. The listing is newest-first with a STABLE tiebreak, because a list that reorders itself on refresh reads as if something changed. 16 + 11 tests, 9 mutations killed. TWO SURVIVED FIRST and both taught the same lesson this session keeps teaching: the tiebreak mutation survived every test of the projection (a projection cannot see the order it is given), and the capacity-join mutation survived because the route test STUBBED the aggregate — so the test now uses the real `QuoteFamilyStore`, the way the round-trip journey uses the real bridge sender and the catalog tests use the real publisher. **CATALOG SURFACE NOW CLOSED, and closing it found two defects rather than one.** `commerce/catalog_pointer_store.ts` + `GET /v1/commerce/catalog/published`, owner-only, rendering `describeCatalogForOwner`. The card itself is the small half: `withdraw` is offered on a live catalog and NOT on a tombstone, because withdrawing a tombstone republishes it at a new sequence and tells every consumer the catalog was retired twice. The two defects were underneath it. FIRST, the publish route took `expected_pointer_cid` — the compare-and-swap the repo enforces — OUT OF THE REQUEST BODY, which made the caller the authority on a fact only this node knows: where its own chain is. A caller that omitted it published a GENESIS over a live chain; one that got it wrong lost a race it had no way to understand. The node now records its own head on the repo's acceptance and supplies both the predecessor and the CAS from that record; a body `previous` that names a different point is REFUSED (409 `stale_predecessor`) rather than ignored, because a client that believes the chain is at sequence 4 while this node publishes 9 would show its user a catalog nobody published. `expected_pointer_cid` is no longer read at all. SECOND — and this is the one a card would never have surfaced — the withdrawal route BUILT a tombstone and handed it back, and nothing wrote it. The live catalog stayed live, consumers kept fetching the previous head, and the owner believed they had stopped selling; the gap between those two beliefs is filled with orders. Withdrawal now publishes through the same `publishCatalogRecords` path, with the same three outcomes as publication (`published: null` = built-not-written for an operator with no repo, 200 = written, 409 = the repo refused). The store reads rows back through `rehydrateCatalogPointer` — the SAME validator a consumer runs against a pointer fetched from the repo — so a row this build cannot read the way a buyer would is reported ABSENT rather than believed; `JSON.parse('null')` as a pointer would otherwise read as "nothing published yet" and invite that genesis. 18 + 23 tests, 16 mutations (15 killed, 1 replaced: the first attempt cast `null` to the record type, which changes no runtime value because both callers already handle null — a bad mutation, not a coverage gap; the honest version returns a record with an empty pointer and dies on five assertions). The boundary guard caught both of my own violations before the suite did: a `JSON.parse` outside `rehydrate.ts`, and the in-memory double missing from the orphan ledger. |

---

# WS-8 — Managed runtime (Phase 3)

| ID | Deliverable | Spec | Status | DoD |
|----|-------------|------|--------|-----|
| 8.1 | Multi-tenant control plane + tenant cells | §17.1 §17.3 | **PART** | `packages/managed-runtime/src/{tenant,control_plane}.ts`. DoD PROVEN at module level by `ws8_definition_of_done.test.ts`: a lease admits ONE tenant and ONE purpose and nothing else; an expired lease admits nothing even for its own tenant; two leases are never interchangeable; a SEALED cell refuses a lease because waking is the tenant's own act (registration alone does not make a tenant servable — a cell never unlocked holds no key material); an unknown tenant is refused rather than served a default. **PART not DONE:** the package has NO production importer. There is no hosting product for it to run inside, and §23 forbids claiming readiness from passing tests.. **WHY THIS IS NOT 'MISSING A PRODUCTION IMPORTER' (corrected 2026-08-10).** That wording read as an oversight and was the wrong frame. §17 opens: "Commerce depends on the generic managed Dina runtime but does not define it. The following requirements are vertical integration constraints." A hosting product is not a commerce deliverable, and wiring a `ControlPlane` seam into Core for a deployment that does not exist would be speculative generality — the orphan defect this review spent six rounds removing, wearing new clothes. What commerce DOES owe §17 is now asserted, by `__tests__/commerce/vertical_constraints.test.ts`: no LLM and no model client anywhere in the aggregate (§17.5 cost, and the same rule CLAUDE.md states from the security side), ranking and the comparison card free of randomness and of the clock (§17.5 determinism — a ranking that moved between runs makes §18.4's "why did this win" unanswerable), money as integer minor units and never a float, the clock read ONLY as a composition default, and no commerce module holding the runtime's own apparatus. That last one is isolation by ignorance: a module that could resolve a tenant is a module that could resolve the wrong one. It found one thing worth fixing: `transformInboundOrderResult` read `Date.now()` inside logic while every neighbouring module takes `nowMs` as an argument, which made the D2D ingress the one path where a test could not put the world at a chosen instant. Now injected, with the default at the seam. The package stays **PART** and the reason is now accurate: the generic hosting runtime it belongs to is out of commerce's scope by the spec's own first line, and §23 forbids claiming readiness from tests either way. |
| 8.2 | Hosted runner binding per install | §17.4 | **PART** | `hosted_runner.ts`. A runner bound to one install is refused another (`not_bound_to_install`) and refused under another tenant (`not_bound_to_tenant`) — the refusals are DISTINCT because they send an operator to different places, a misconfiguration versus an intrusion. An unbound runner is `unknown_runner`, an inactive binding is `binding_suspended` with the row surviving so it stays revocable, and a FLEET suspension stops every tenant a leaked vendor serves at once rather than requiring a walk. **WHY THIS IS NOT 'MISSING A PRODUCTION IMPORTER' (corrected 2026-08-10).** That wording read as an oversight and was the wrong frame. §17 opens: "Commerce depends on the generic managed Dina runtime but does not define it. The following requirements are vertical integration constraints." A hosting product is not a commerce deliverable, and wiring a `ControlPlane` seam into Core for a deployment that does not exist would be speculative generality — the orphan defect this review spent six rounds removing, wearing new clothes. What commerce DOES owe §17 is now asserted, by `__tests__/commerce/vertical_constraints.test.ts`: no LLM and no model client anywhere in the aggregate (§17.5 cost, and the same rule CLAUDE.md states from the security side), ranking and the comparison card free of randomness and of the clock (§17.5 determinism — a ranking that moved between runs makes §18.4's "why did this win" unanswerable), money as integer minor units and never a float, the clock read ONLY as a composition default, and no commerce module holding the runtime's own apparatus. That last one is isolation by ignorance: a module that could resolve a tenant is a module that could resolve the wrong one. It found one thing worth fixing: `transformInboundOrderResult` read `Date.now()` inside logic while every neighbouring module takes `nowMs` as an argument, which made the D2D ingress the one path where a test could not put the world at a chosen instant. Now injected, with the default at the seam. The package stays **PART** and the reason is now accurate: the generic hosting runtime it belongs to is out of commerce's scope by the spec's own first line, and §23 forbids claiming readiness from tests either way. |
| 8.3 | Encrypted managed catalog store + export | §17.2 | **PART** | `managed_store.ts`. One tenant cannot read another's blob; an export lists only the asking tenant's own objects (a listing that leaked neighbour object NAMES would be a directory of who hosts what); a delete by one tenant does not touch another's object of the same name. **WHY THIS IS NOT 'MISSING A PRODUCTION IMPORTER' (corrected 2026-08-10).** That wording read as an oversight and was the wrong frame. §17 opens: "Commerce depends on the generic managed Dina runtime but does not define it. The following requirements are vertical integration constraints." A hosting product is not a commerce deliverable, and wiring a `ControlPlane` seam into Core for a deployment that does not exist would be speculative generality — the orphan defect this review spent six rounds removing, wearing new clothes. What commerce DOES owe §17 is now asserted, by `__tests__/commerce/vertical_constraints.test.ts`: no LLM and no model client anywhere in the aggregate (§17.5 cost, and the same rule CLAUDE.md states from the security side), ranking and the comparison card free of randomness and of the clock (§17.5 determinism — a ranking that moved between runs makes §18.4's "why did this win" unanswerable), money as integer minor units and never a float, the clock read ONLY as a composition default, and no commerce module holding the runtime's own apparatus. That last one is isolation by ignorance: a module that could resolve a tenant is a module that could resolve the wrong one. It found one thing worth fixing: `transformInboundOrderResult` read `Date.now()` inside logic while every neighbouring module takes `nowMs` as an argument, which made the D2D ingress the one path where a test could not put the world at a chosen instant. Now injected, with the default at the seam. The package stays **PART** and the reason is now accurate: the generic hosting runtime it belongs to is out of commerce's scope by the spec's own first line, and §23 forbids claiming readiness from tests either way. |
| 8.4 | Staff authority + acting-for chain | §7.2 §7.3 | **DONE** | `commerce/staff_authority.ts` + `commerce/buyer_authority.ts`. **This row read DONE for a long time while the whole section was dead, and that history is the point of the entry.** The rule was written, tested and reachable only through an OPTIONAL `authority` argument on `submitApprovedOrder` that neither order route supplied — so spend ceilings, category and branch authority, quorum and time-bounded delegation never ran on a real order. The orphan guard could not see it: it matches symbol names, and the executor names the symbol. A rule can have a caller and still never run. Closed (DR-1) by three changes that only work together: a `BuyerAuthorityProvider` seam Core resolves at submit and resend; `authority` made a REQUIRED argument so no future caller can quietly omit it; and both composition roots installing a provider, with a null node DID treated as a refusal rather than an empty principal. A node with no provider returns 503 and a provider with no record returns 403 — a node that cannot say who may commit its business does not commit it. Also fixed here: the executor built its `AuthorityRequest` with `regionValue: null` hardcoded, which made §7.2 BRANCH authority unsatisfiable on every real order (`covers` refuses a location grant outright when the request names no region). The region now comes off the order's own delivery projection as `scheme:value` — from the order, never the caller-supplied context, so nobody names the region that grants them authority. Unchanged and still correct: the chain is a VALUE assembled from authenticated sources and a gap in it is a REFUSAL; there is no owner branch that skips evaluation (the pilot's single approver is a configuration with one grant, so an owner with no grant record is not an owner); QUORUM takes the SET of approvers, not a count; a ceiling in another currency does not convert; every category must be covered rather than any; an unreadable amount exceeds every ceiling. 24 + 4 + 8 tests, including a mutation-checked pair proving the ceiling and the branch region are load-bearing on the route |
| 8.5 | Metering, quotas, backup, hot/cold lifecycle | §17.5 | **PART** | `control_plane.ts`. Read and quote work keeps its lease after eight hours with no owner interaction — §17.5's actual promise, since a buyer's question answered at 2am is what "available" means for a business. Metering counts leases and wakes so a sleeping owner's node is billable; quota exhaustion is REFUSED and COUNTED rather than silently served, because a throttled tenant an operator cannot see is a support ticket with no evidence; and an idle cell is swept to `sealed`, after which serving it again needs a wake — staying available must not mean staying resident. **WHY THIS IS NOT 'MISSING A PRODUCTION IMPORTER' (corrected 2026-08-10).** That wording read as an oversight and was the wrong frame. §17 opens: "Commerce depends on the generic managed Dina runtime but does not define it. The following requirements are vertical integration constraints." A hosting product is not a commerce deliverable, and wiring a `ControlPlane` seam into Core for a deployment that does not exist would be speculative generality — the orphan defect this review spent six rounds removing, wearing new clothes. What commerce DOES owe §17 is now asserted, by `__tests__/commerce/vertical_constraints.test.ts`: no LLM and no model client anywhere in the aggregate (§17.5 cost, and the same rule CLAUDE.md states from the security side), ranking and the comparison card free of randomness and of the clock (§17.5 determinism — a ranking that moved between runs makes §18.4's "why did this win" unanswerable), money as integer minor units and never a float, the clock read ONLY as a composition default, and no commerce module holding the runtime's own apparatus. That last one is isolation by ignorance: a module that could resolve a tenant is a module that could resolve the wrong one. It found one thing worth fixing: `transformInboundOrderResult` read `Date.now()` inside logic while every neighbouring module takes `nowMs` as an argument, which made the D2D ingress the one path where a test could not put the world at a chosen instant. Now injected, with the default at the seam. The package stays **PART** and the reason is now accurate: the generic hosting runtime it belongs to is out of commerce's scope by the spec's own first line, and §23 forbids claiming readiness from tests either way. |

---

# WS-9 — External systems (Phase 4)

| ID | Deliverable | Spec | Status | DoD |
|----|-------------|------|--------|-----|
| 9.1 | Spreadsheet + generic REST connectors | §24 | **DONE** | `connectors.ts` — three kinds (uploaded CSV, hosted CSV, REST JSON) all reaching ONE normalizer (`importCatalogRows`), so replacement cannot change semantics by construction. `connector_executors.ts` performs the fetch under the §10.3 policy and withholds the credential on a cross-origin redirect. Owner route `POST /v1/commerce/catalog/load`. §6.5 re-consent classifier (`classifyConnectorChange`) + route. Server transport is `node:https` (reports the connected address); mobile has none, so networked connectors refuse `no_executor` there and the CSV upload still works. |
| 9.2 | One real ERP/inventory connector | §24 | **PART** | Live round trip | The connector lane can now READ a real ERP. The request half already spoke JSON-RPC (owner-configured body, credential that never crosses an origin); the answer half was missing. Two owner-DECLARED fields close it, neither of them a guess: `rowsAt` names the response field the rows sit under (Odoo answers `{jsonrpc, id, result: [...]}` and the row reader refuses any wrapper on purpose — a JSON-RPC ERROR reply carries no `result` and now REFUSES rather than importing an empty catalog, which would publish a withdrawal of every product the supplier sells), and `fieldMap` + `price` rename an ERP's vocabulary onto catalog columns and turn a major-unit decimal into integer minor units (§9.1). Rounds rather than truncates. A test caught a real bug in the first implementation: `Number(false)` is 0, and Odoo answers `false` for an unset field, so an unpriced product would have been published free. **PROBED 2026-08-10 — both live-Odoo routes are down, and neither is a Dina problem.** `demo.odoo.com/start` (Odoo's own demo provisioner) answers **500** to the documented POST, twice, minutes apart; `demo.odoo.com` JSON-RPC 301-redirects rather than serving; `runbot.odoo.com` is up but hands out no stable JSON-RPC target. The local fallback — real Odoo in Docker, which exercises the identical JSON-RPC — is unavailable because the **Docker daemon is not running on this machine** (`/Users/rajmohan/.docker/run/docker.sock` absent). **Open:** a live run, unblocked by EITHER starting Docker locally (then real Odoo + Postgres containers close this immediately) OR Odoo's demo service recovering. What a live run adds over the existing unit coverage is narrow and real: whether the connector survives Odoo's ACTUAL response shapes. One such shape has already been caught without a live instance — Odoo answers `false` for an unset field and `Number(false)` is 0, so an unpriced product would have published free — which is the class of defect the live run exists to find more of.
| 9.3 | Credential broker + rotation UX | §6.5 §8.3 FR-P4 | **DONE** | `credential_broker.ts` + `credential_store.ts` + `commerce_credentials` (Tier 0). NO read API: a caller performs a typed operation and never receives material. Grants read from the store per call, so `forget()` stops the next one. `paramsCarryCredential` refuses a secret in a workflow payload. §8.3's lease fallback is built with all five bindings and stays on the orphan ledger (no connector needs one). Owner routes: list status / rotate / forget. §18.3 status is DERIVED from brokered calls and overrides the settings row in the inbox. |
| 9.4 | End-to-end idempotency evidence at the order boundary | §15.5 | **DONE** | `idempotency_evidence.ts` + `idempotency_store.ts` + `commerce_idempotency_evidence` (per resource:operation). A DECLARATION is not evidence: proof is an observed probe — same key twice, one external reference, no second order, a gap wider than an in-flight lock, and a declared window ≥ quote validity + reconciliation + commercial retention. `effect_executor.ts` records `effect_started` BEFORE crossing, sends ONE key on every attempt, retries only on proven evidence (cap 2), and reports a throw or an error answer as AMBIGUOUS — never as never-sent. Owner routes: read verdicts, record a probe, cross the boundary. |
| 9.5 | Fulfilment updates + reconciliation | §12.7 | **DONE** | `fulfilment_reconciler.ts` PROPOSES against the §9.11 graph and signs nothing: backwards reports, illegal jumps, terminal chains, crossed references and line-less shipments all become `needs_attention` rather than a signed claim. `sweepFulfilment` polls through the broker (no inbound webhook lane) and drops an unreachable read rather than calling it 'unchanged'. `listWithExternalRef` (both stores, parity-tested) supplies the list; owner routes reconcile one report or sweep every open order. |

---

# WS-10 — Trust and interoperability (Phase 5)

| ID | Deliverable | Spec | Status |
|----|-------------|------|--------|
| 10.1 | Commerce outcome prompts + Ranked Reviews integration | §14.4 | **DONE** | `appview/src/shared/commerce/review-dimensions.ts`. §14.4's projection rules made checkable: a derived dimension RETAINS extractor, version, confidence, source review, target node and relationship path, or it is refused `untraceable` — a dimension nobody can trace back is an opinion the index publishes in its own name, and the requirement holds for a reviewer-confirmed scope too. The projection never mutates its input and copies the relationship path, so a caller cannot rewrite what it was derived from. The §14.4 dimension vocabulary is CLOSED, so a projection cannot invent one. **CONSUMER BUILT 2026-08-10, closing the gap.** `appview/src/api/xrpc/commerce-review-dimensions.ts` — `com.dinakernel.commerce.getSupplierDimensions`, registered in the route table and driven against real Postgres by `commerce_review_dimensions_route.test.ts` (10 scenarios). A buyer asks what reviewers said and gets it PER DIMENSION, each carrying its own provenance, source and `mayAffectStandingAlone`, plus every refused claim NAMED — an index that silently dropped an out-of-vocabulary category would look identical to a supplier nobody has reviewed. The endpoint computes NO score, and a test asserts the response keys to keep it that way: §10.6's argument is that an extractor's confidence and a trust weight tunable in one place is the coupling to avoid, so the projection reports and the ranking stays elsewhere. The query JOINS through `subjects` because an attestation names a subject ROW and the DID lives there — reading `subject_id` as a DID would answer about whoever shared the string, the §9.4 identity mistake one table over, and there is a test for exactly that. Mutation-checked: removing the projection kills the three §14.4 rule cases. |
| 10.2 | Reviewer-confirmed evidence scope + dimension UX | §14.4 FR-A9 | **DONE** | Same module. §14.4's ranking, enforced arithmetically rather than advised: an unconfirmed model extraction is CAPPED at `MODEL_EXTRACTION_CAP_BP`, which sits below the standing floor, so no amount of model confidence outranks a person who said the same thing — "opaque AI classification alone must not create a large public standing penalty". `mayAffectStandingAlone` re-checks the SOURCE as well as the number, so an edge built by hand cannot route around the cap, and a dimension reached through a DISPUTED relationship qualifies at no confidence (same rule as §10.7's disputed edge). Ranking is by SOURCE first: a reviewer at 3000bp beats a model at 10000. Strongest per dimension, never summed. The five §14.4 provenance classes stay distinguishable so a UI can separate a review about this exact variant from one inherited across a family. LEAKAGE GATE: evidence text carrying an exact commercial term is refused rather than trimmed — trimming leaves the shape of the number behind — and the finding names the FIELD, never the value. Commentary on whether terms HELD is permitted, because §14.4 names it as a publishable dimension; my first test had that backwards and the code was right. 33 tests. |
| 10.3 | Plural relationship resolvers + disagreement display | §10.7 FR-A8 | **DONE** | `relationship_resolver.ts` — §10.7 forbids "whichever merge produces the highest score" by name, so consensus is the FLOOR (`Math.min`): a threshold is crossed only when every consulted view agrees it is. An edge only some views report is kept for recall but capped below the standing bar. A CONTESTED edge authorises nothing whatever its number. Four disagreement kinds, each with an owner-readable headline; conflicts are detected across views AND within one (a directory listing two parents is the one that noticed). Thresholds mirrored from the AppView and statically checked against its source. |
| 10.4 | Additional AppViews + cross-index evidence | §10.6 | **PART** | `commerce-protocol/src/cross_index.ts`. §10.6 is three sentences and the third has the consequences: every result carries enough SOURCE and SNAPSHOT evidence to identify where it came from and to verify the supplier live before commitment. A combiner that merged two indexes into one anonymous list would satisfy the first half and destroy the second. So merging keeps every index's sighting with its own snapshot reference; two indexes projecting the same product from DIFFERENT snapshots mark the candidate `divergentSnapshots` and it is NEVER resolved here — picking the "newest" would resolve on the buyer's behalf using a claim made by the index that wants to win. Scores take the strongest sighting, never a sum, so two mirroring indexes cannot manufacture rank. Candidates from a stranger's index are VALIDATED, and a refusal names which index and which position rather than being dropped, because an index quietly returning nonsense is a thing a buyer should notice. `needsLiveVerification` is true on divergence AND on a single sighting — one index agreeing with itself is one opinion, and the same index listed twice is still one. Placed in the protocol package rather than Core: a pure function over the §10.5 candidate this package already freezes, usable by a buyer's node and a third-party pack alike, and it does not grow the kernel. 19 tests. **Open:** the second live AppView. `relationship_resolver.ts` (10.3) and this both take N answers; what is missing is a second deployment to ask, which is infrastructure rather than code. |
| 10.5 | Third-party pack via the conformance kit | §25.1 | **PART** | `conformance/runner.ts` + `__tests__/conformance_runner.test.ts`. The gap was precise: ten frozen vector files and a generator, and NOTHING that could execute them — our own AppView port had to hand-write assertions against `catalog.json`, and a third party writing a pack in Go or Rust had only the JSON. The runner takes the implementation as an ARGUMENT, which is the design constraint: a conforming pack cannot import `@dina/commerce-protocol` and may not be TypeScript, so the kit describes the functions it needs and judges whatever it is given. FOUR REFUSALS make it worth running: a skip is not a pass (an implementation supplying nothing reports `ok: false`); unwired families are NAMED rather than implied; every case is named rather than counted; and the runner is proven not to be a rubber stamp by four negative tests — off-by-one arithmetic, ignored domain separation, accepting `EACH`/`each ` where the closed vocabulary rejects them, and ordering quantities it cannot compare. Its first run reported 5 failures against our own implementation and ALL FIVE were the runner: the vectors encode refusal cases in the same arrays as positive ones (`expected_error_contains` beside `expected_minor_units`, `error` beside `compare`) and only the positive shape was handled — a kit that mishandles the refusal half tells every correct port it is broken. **TWELVE families wired as of 2026-08-10** (arithmetic, digests, units, quantity, product, relationship, search_candidate, schema_evolution, catalog, malformed, held_signed, schema_evolution.parse_round_trip). §9.3/§9.4 identity is the family that decides whether two suppliers are selling the same thing, and a port that gets the arithmetic right and this wrong prices the correct total for the WRONG PRODUCT, which is worse than a refusal because nothing downstream notices. 23 cases: equality, variant distinction, scoped-reference acceptance AND refusal, and the frozen refusal STRINGS — two ports that describe one fault differently make one incident look like two in an operator's log. Mutation-checked by removing the variant-digest comparison, which is exactly the §9.4 substitute-for-the-ordered-thing failure: the kit fails `same_identifier_different_variant_is_NOT_a_substitute`. The three added last carry rules a port can get wrong SILENTLY: §10.3 decides what an edge in the product graph means, so a port admitting `manufactured_by` pointed at a product rather than an operator builds a graph whose edges mean two different things and every traversal after that is arithmetic over a category error; §10.5 is the one vector written for a CONSUMER rather than a publisher, and its refusals are the whole boundary between a stranger's projection and a buyer's screen; §9.13's forward-compatibility law is the most consequential of the three, because a port that drops unknown fields before canonicalising digests a document nobody sent and then verifies a signature over something the signer never signed — it looks like tidiness and it is a forgery seam. Four new NEGATIVE tests, one per rule, each pinning the specific case that must fail rather than 'something failed'. Also added: a per-family EXECUTED-CASE COUNT, because `missingVectorFamilies` catches a family that ran zero cases and nothing caught a family that ran one of nine — a wiring bug that iterated the wrong key would report the family as executed and certify a rule it never checked. And `REQUIRED_VECTOR_FAMILIES` is now exported and pinned in ONE test: it had been repeated in three separate expectations, so wiring a family meant editing three assertions that had nothing to do with it, and an expectation updated mechanically to make a suite green is an expectation nobody is reading. **`catalog`** adds the two-level digest and the publication chain: a page digest covers items, a snapshot digest covers page digests, and a port that collapsed them computes a stable-looking value no other node reproduces. Its chain refusals are the load-bearing half — §10.2 makes a gap and a rollback publication FAULTS rather than warnings, because an index that applied sequence 3 after sequence 1 serves a catalog no supplier ever published, assembled out of two that they did, and every record in it is genuine. **`malformed` needed a fix to the VECTOR, not just wiring.** Its quote, status and held_evidence cases are mutations — "set `total.minor_units` to 1 and expect a refusal" — and the file carried no record to set it on: our suite built one with `makeSignedQuote()`, so the family was executable by exactly one implementation, ours. A frozen vector only its author can run is a fixture, not a contract, and the unreachable half was the half deciding whether a TAMPERED QUOTE is caught. Base records are now emitted into `malformed.json` additively — one new `base` key, every pre-existing case byte-identical — and the dotted-path mutation is applied by the KIT rather than by each port, because path semantics are exactly what two implementations would read differently and then disagree about a case neither got wrong. +4 negative tests, incl. the two that matter most: a port that verifies a quote's SIGNATURE and trusts its arithmetic (a tampered total travelling under a valid signature, so every downstream party holds cryptographic proof of a number nobody computed), and one that accepts held evidence with no signature (a record plus its content digest proves nothing — the digest is a hash of the record, so anyone holding or inventing it computes one, and the recovery path becomes forger-drivable). **CORRECTED 2026-08-10 (Codex round 6):** that claim read stronger than the family was. `malformed.held_evidence` judged STRUCTURE only — its own "valid" base carried a 64-character signature, half an Ed25519 one, over an envelope binding no record — so a port that verified nothing could still pass it. `held_signed.json` now carries genuinely signed vectors from a fixed seed, and `verifyHeldEvidence` must check BOTH a real signature over the canonical envelope AND that the envelope commits to the record digest; a case defeats each half alone. `schema_evolution.parse_round_trip` closes the second overclaim: the unknown-field family tested the CANONICALIZER, so a port with byte-perfect canonicalization and a schema parser that strips additive fields passed — the exact defect AppView shipped. The new family composes parse-then-canonicalize, and a negative test proves such a port now fails while the canonicalizer-only family stays green. `UNWIRED_FAMILIES` is now empty and KEPT rather than deleted, because the next family added to `vectors/` arrives unwired; the test that asserted `NOT EXECUTED` appears was inverted to assert it does not, and a separate test keeps the reporting mechanism honest by formatting a report that does have a gap. **Open:** no actual third party has run it, which is the part no amount of wiring settles. |
| 10.6 | Optional standard-capability promotion | §11.3 | **DONE** | `capability_promotion.ts` — §11.3's seven proofs as checkable evidence (2+ independent provider DIDs, one pinned schema, ≥20 answers each, ≥30 days, declared action/privacy class, declared exposure/subject-auth, card fallback, both interoperable failure codes, observed rate limit). The ADDITIVE rule is checked FIRST and refuses any promotion that would change an existing schema, action or privacy class — §11.1's `price_check` is what happens otherwise. The custom id survives as an alias, and `resolveCapabilityId` reports `via_alias` so a translation is never hidden. |

---

# WS-11 — Conformance, testing and acceptance

| ID | Deliverable | Spec | Status | DoD |
|----|-------------|------|--------|-----|
| 11.1 | Protocol vectors complete | §25.1 | **DONE** | See 1.9 | Every §25.1 category is frozen. `packages/commerce-protocol/docs/conformance.md`'s **Known gaps** list is empty, and a doc test enforces that each vector file is listed there — so a category added without documentation fails CI rather than sitting unnoticed. |
| 11.2 | Plugin security tests | §25.2 | **PART** | The commerce lane is now covered where the rules will actually be relied on: `commerce_security.test.ts` installs BOTH reference packs side by side — §18.1's "two installs, not one superset" — and drives §25.2's own claims against them. Lane isolation both ways, the device binding (a leaked install id is not enough to drain a competitor's queue), two installs of the SAME pack kept apart by nothing but the install id, pause-holds-then-resumes, deny-before-validation with an EMPTY registry (so the refusal cannot depend on the operation existing), and the scope hash moving when an update widens `host_operations`. Writing it found a REAL wiring gap: both boots built the `ExtensionOperationRegistry` EMPTY and populated only the DISPATCHER, so every §3.4 operation was refused `operation_unregistered` and the three executors WS-3.5 added were unreachable — the lane was open on the doing side and closed on the declaring one. `registerCommerceHostOperations` now ships the three registrations, code-shipped per §3.4, wired at both roots; action classes are the fail-closed reading (`publication_candidate` is `read` because it validates and does not publish; `d2d_send` and `connector_broker` are `write` because the same name can submit a purchase order). Two further findings: a PROVIDER-only capability cannot ride the tool lane (terminalized `stale_authority`, now pinned), and I briefly "fixed" the reference Buyer to declare `d2d_send` before noticing `reference_manifests.test.ts` pins the opposite as a decision — the buyer's fan-out goes through Core's own service-query egress, so the pack needs no brokered authority and §25.2's requirement is met more strongly by declaring none. The manifest was reverted and the test moved. **"SECRETS NEVER ENTER APPVIEW" NOW DRIVEN, 2026-08-10 — and it found a real leak.** `appview/tests/integration/commerce_no_secrets.test.ts`, 9 scenarios against real Postgres. The claim needed interpreting before it could be tested, because AppView ingests from a FIREHOSE, not from Core: a guarantee that holds only when every publisher is honest is a guarantee about publishers, not about AppView. And AppView cannot classify a stranger's string as a secret without inventing a requirement nothing can meet. So the sentence splits — (1) Dina never SENDS secrets, which is Core's §12.1 gate and the only half protecting the OWNER's data; (2) AppView creates no NEW exposure, meaning it must not take an unnamed field and PROJECT it into the queryable surface discovery answers from. The second is structural and checkable. **THE LEAK:** `fulfilmentRegions: [...item.fulfilment_regions]` and `indicativePrice: item.indicative_price` copied inbound OBJECTS verbatim into jsonb columns, so a hostile supplier nesting a field inside a region or a price put it straight into the searchable product row — the allow-list held at the top level and stopped one level above the data, because a shallow copy of an array of objects copies the REFERENCES. Both now rebuilt field by field. Found by scanning EVERY text and jsonb column of every commerce table, with the column list read from `information_schema` rather than from a list I maintain — a test naming the columns it expected to be clean would have checked the ones I thought of, and these were the two I would have missed. Mutation-verified by reverting the region half alone (anchor asserted first, so a mutation that silently did nothing could not be mistaken for a passing test). Two further properties pinned while here: the raw `pages_json` column DOES retain the publisher's bytes verbatim and that is the design rather than a leak (digests are computed over them, and they were already public on the publisher's own PDS) — asserted out loud so nobody "fixes" it and breaks re-verification; and a page that does not hash to the digest it was published under projects NOTHING, which was found by accident when a naive taint made the whole catalog vanish from search, and which matters because indexing it would let anyone reaching the firehose edit somebody else's prices. **Open:** only the hosted-identity claim (§25.2 line 4), a STATED BOUNDARY on WS-8 rather than missing work — §17 makes hosting a constraint commerce must not make expensive, not a product commerce defines |
| 11.3 | Commerce workflow tests | §25.3 | **PART** | Engine-level covered; supplier-side journey covered end to end by `procurement_scenario.test.ts` (retailer↔manufacturer, both backends, mutation-verified). NEW: `procurement_lane_scenario.test.ts` runs the same journey THROUGH THE PLUGIN LANE — provider ingress → compiled-Core admission → the install's private lane → runner claim via the SDK → `transformInboundOrderResult` → the buyer receives Core's SIGNED acknowledgement. Installs the WS-3.9 reference manifest rather than a stub. 7 scenarios: happy path, decline, replay, spent capacity, `outcome_unknown`, schema-rejected runner answer, stranger probe. It found a real defect within an hour of WS-3.9 landing: the manifest declared the decision field as `decision` while Core's seam reads `kind`, so a CONFORMING supplier pack could never complete a sale — every local check passed and only the end-to-end run noticed. Fixed structurally: `readSupplierDecision` is now exported (the shapes it accepts ARE the plugin contract) and a test drives the real reader against the manifest's enum. NEW: `procurement_journey.test.ts` runs the WHOLE in-process journey in one sequence — spreadsheet → import → publication (leakage gate inside) → bounded fan-out → probing admission → signed quote → hard filters + ranking → product evidence → order through the plugin lane → Core-signed acknowledgement → dispatched → delivered, with each status record chained to its predecessor. It exists because import/publication/fan-out/probing/filters/ranking/evidence were each built and gated ALONE, which is the defect shape this codebase keeps producing; the previous two times these pieces met, the meeting found real mismatches. Three supporting cases keep the happy path from being the only claim (expired-but-otherwise-perfect offer ranks nowhere; private column refuses to publish; reference manifest still installs). REVOCATION is now covered, and driving the REAL `uninstall` rather than the registry's `remove` is the whole value: `remove` is one step of the teardown, and reaching for it directly skips the two rules that matter. Five scenarios on the retailer↔manufacturer lane: the uninstall is REFUSED while Sancho's order is open (§16.4 — every lifecycle capability a buyer needs answers through this install's binding, so tearing it down leaves the records intact and unreachable); it is STILL refused after delivery, because `delivered` is deliberately non-terminal and a buyer inside the dispute window can still dispute — the orders an uninstall would damage most; it succeeds once nothing is open and FENCES the runner by revoking its paired device, which is what stops it acting rather than the row's absence; a new order afterwards is refused `install_unavailable` (not `no_plugin_binding` — the listing still names the install, and the two send an operator to different places) with no reservation left holding the buyer's capacity; and a PAUSED install HOLDS its queued order rather than losing it, because §16.3's pause is a lift-able hold. Two of my own expectations were wrong and the code was right both times: `markRevoked` is a PENDING-only tombstone that silently no-ops on an active install (a test using it would report a revoked pack that kept trading), and the post-teardown refusal code is the more informative one. NEW: `buyer_round_trip_journey.test.ts` runs TWO NODES — two identity databases, two commerce runtimes — and carries ChairMaker's answer back to Sancho over the real D2D ingress. Every other journey stops where the response bridge hands bytes to a socket, and every buyer-side test STARTS by handing the executor an answer; between those two points sat the defect that nothing on a buyer's node received a supplier's answer at all. 6 scenarios: accepted, rejected (never offering to resend — a rejection is a decision, not a failure to deliver), the answer lost (Sancho parked, ChairMaker still holding a real commitment), the re-poll asking and settling, §16.2's denial-against-evidence, and a peer with a legitimate window of its own relaying ChairMaker's acknowledgement (ingress accepts, the commerce seam still refuses because it binds to who SENT it). IT FOUND TWO REAL DEFECTS. The first was mine, in the test: pushing `ctx.resultJSON` onto the wire instead of the `service.response` envelope the bridge builds — every delivery silently did nothing, which is what testing a wire body production never emits looks like; it now uses the real `makeServiceResponseBridgeSender`. The second was in the CODE, and it is the one worth having: held evidence was a PER-CALL argument, so the ask could be given a reader while the apply — which runs in the D2D ingress and had no way to obtain one — judged the answer against an empty question. §12.7's legality rule is about what the buyer PRESENTED, so that asymmetry accepts a `never_received` the buyer was entitled to refuse and authorizes exactly the duplicate order the section exists to prevent. `installHeldEvidenceReader` makes it node-level and both halves read it; null is the honest default and both halves agree on it. **APPVIEW DISCOVERY IS NOW CLOSED** by `appview/tests/integration/commerce_discovery_interop.test.ts` + `packages/core/__tests__/commerce/catalog_interop_fixture.test.ts`: Core's REAL publisher emits ChairMaker's catalog into a fixture (regenerate-and-compare, so it cannot drift), AppView ingests those exact bytes through its real handlers into real Postgres, and Sancho finds a manufacturer he was never told about — by category, by free text, by a GTIN read off the product, and by a scoped secondary SKU. 13 scenarios. AppView takes no `@dina/*` dependency by design, so the two halves are joined by the BYTES rather than an import, which is what the contract is; the existing discovery suite built its records with AppView's own digest functions and asserted AppView accepted them, so both sides of that check were AppView's. **It found a real defect on its first run.** Core's §12.1 leakage vocabulary was written from the spec's prose (`category`, `regions`, `list_price`, `pack_size`) while `@dina/commerce-protocol` defines the published item as `category_ids`, `fulfilment_regions`, `indicative_price`, `pack` — two self-consistent vocabularies that had never met, so **Core's own publication gate refused every item shape AppView's ingest requires** and a supplier publishing a real `CatalogItem` could not publish at all. Neither side could see it: Core's publisher tests used a flat `{sku, name}` CSV shape, AppView's hand-built `CatalogItem`s and skipped Core. Fixed at the root — the vocabulary now covers the wire type AND the flat CSV lane, `attributes` keys are supplier-chosen per §9.5 with their values still scanned, and `catalog_leakage_vocabulary.test.ts` asserts every field a valid `CatalogItem` can carry is in the list, in both directions. That test is the fix; the list is data it checks. **SELF-CONTRADICTION CORRECTED 2026-08-10.** This clause said discovery was unreachable because it "needs a running catalog AppView (WS-5.4) and a PDS" and that "every journey here starts from a supplier the buyer already names" — while the SAME CELL, a few thousand characters earlier, records that APPVIEW DISCOVERY IS NOW CLOSED. The closing clause was written before the discovery work and never revised when the earlier text was appended, so the row argued with itself and a reader taking either half at face value drew the opposite conclusion. Sixth drift found in this document, and the first INSIDE a single row: appending to a cell is not updating it. What is actually true: a buyer finding a supplier they were never told about IS covered, by `commerce_discovery_interop.test.ts` against a real catalog AppView on real Postgres, ingesting Core's own published bytes — by category, free text, a GTIN read off the product, and a scoped secondary SKU. **LIVE-PDS PROBE RUN 2026-08-10, and the infrastructure is NOT the blocker.** `scripts/commerce/live_pds_publication.mjs` publishes Core's frozen catalog bytes to `test-pds.dinakernel.com` as ChairMaker, reads them back as a stranger holding only the DID, and re-derives every page digest over what came back — the check that catches a transport reordering a key, coercing a number or dropping an optional field, which is the damage class a network hop actually causes. It refuses to run against any host but a `test-*.dinakernel.com` one, because it publishes with fixture credentials and pointing it elsewhere would put a fictional manufacturer's catalog under a real identity. **Measured state:** the PDS is LIVE (`did:web:test-pds.dinakernel.com`, no invite code); all three commerce actors exist with resolvable `did:plc` identities; step 1 (supplier identity resolves) PASSES against the real server. **The remaining five steps need one credential.** The live ChairMaker account is `chairmaker.test-pds.dinakernel.com`, but the repo's committed fixture (`scripts/register_test_actors.py`) describes `chairmaker-test.test-pds.dinakernel.com` with password `dina-test-chairmaker-2026`, and that password is refused — the live accounts were provisioned by something other than the committed script. **Every actor's repo has `collections: []`**, so no commerce record has ever been published to a real PDS. **Unblock:** the ChairMaker account password, or permission to re-provision the handles the committed script describes. This is a named secret, NOT missing infrastructure — an earlier version of this row called it "needs a provisioned PDS", which was wrong and cost a round. |
| 11.4 | AppView tests | §25.4 | **PART** | Blocked on WS-5 | The INGEST and DISCOVERY halves now run against real Postgres (`dina_commerce_test`): `commerce_catalog_ingest.test.ts` (8) and `commerce_catalog_discovery.test.ts` (13). Discovery is the claim 11.3 named as unreachable in-process — a buyer with no prior supplier reference finding one — driven by publishing two manufacturers through the real Jetstream handlers and then searching with the real xRPC. IT FOUND TWO PRODUCTION DEFECTS, both invisible to the stub suite because a stub records that a select happened rather than whether it runs. (1) CATEGORY search was dead: drizzle spreads a JS array into separate placeholders, so `= ANY($1)` received a bare string and Postgres answered `malformed array literal`. Now `IN (...)` with one placeholder per value. (2) REGION search was dead: `->>` and `||` share a precedence class in Postgres and associate LEFT, so `r->>'scheme' || ':' || r->>'value'` parsed as `(((r->>'scheme') || ':') || r) ->> 'value'` — `operator does not exist: text ->> unknown`. Now parenthesised. Also removed a `sql.raw` array literal built with hand-rolled quote doubling over a stranger-published `custom` identifier value, replaced with real placeholders and a `::text[]` cast. **ROUTE LAYER NOW DRIVEN, 2026-08-10.** `commerce_xrpc_route_layer.test.ts` calls through the real `dispatchXrpc` over the real `XRPC_ROUTES` with a method id and a query STRING — the two things a caller actually meets and every previous commerce test skipped by handing the handler a parsed object. 10 scenarios: registration of both commerce methods, an unknown method as a 400 rather than a 500, exact method-id matching, `limit` coerced from a string, the schema default applied when absent, a bound REFUSED rather than clamped (a cap a caller can talk past is not a cap), a repeated parameter read as a list, and a retailer's whole question end to end — discovery answers WHO, dimensions answer WHAT IS SAID. Mutation-checked twice: unregistering the endpoint and unbounding the limit each fail their own cases. This matters because the repository has the scar — a catalog-search test once pinned the parse HELPER rather than the server, and reverting the server would have left it green. **STALE GAP CORRECTED 2026-08-10.** This row listed "relationship-claim ingest against Postgres" as open. It was already closed: `tests/integration/commerce_relationship_ingest.test.ts` drives `commerceRelationshipClaimHandler.handleCreate`/`handleDelete` against the real database, 8 scenarios, including the rule with teeth — two claims naming different parents. **CORRECTED 2026-08-10 (Codex round 6):** this row previously described ONE edge marked disputed carrying both claims, and that was the IMPLEMENTATION rather than the rule. §10.3 says plainly "Conflicting edges coexist; an AppView does not rewrite the underlying exact product records to make its interpretation look authoritative." Collapsing to one edge hid an alternative, and which one survived depended on row order from a query with no `ORDER BY`, so two rebuilds of identical data could disagree. Edges are now keyed by `(subject, relationship, object)` throughout, every alternative in a conflicting group is marked disputed, and an order-independence test pins the projection against read order. Verified by running it, not by reading the filename. Ingest also now runs the 30-scenario no-secrets battery and the AppView half of the wire-rule parity vectors against the same real database. **Open:** only §25.4's hosted-identity claims, which are a STATED BOUNDARY rather than missing work — §17 makes hosting a constraint commerce must not make expensive, not a product commerce defines, so there is no commerce-side hosting to test. Same residual as 8.1/8.2/8.3/8.5 |
| 11.5 | Managed-runtime tests | §25.5 | **PART** | Blocked on WS-8 | 42 tests across the package: 24 unit (`control_plane.test.ts`) plus 18 that state WS-8's four DoDs as PROPERTIES and attack them — a lease used against the wrong tenant, a runner reaching for a second install or a second tenant, an export asking after a neighbour, work continuing while the owner sleeps, a sealed cell refusing to be quietly re-opened. **Open:** §25.5's hosted-identity and operational claims, which need a deployed control plane. The phrase "no production importer" was removed from 8.2/8.3/8.5 and from here on 2026-08-10: it sat immediately BEFORE the paragraph explaining that this exact wording was the wrong frame, so each row asserted a gap and refuted it a sentence later. §17's first line makes the managed runtime something commerce DEPENDS ON and does not define, so a missing hosting product is a boundary, not an oversight — and a row that states both readings lets a reader pick whichever one they arrived expecting. |
| 11.6 | **Manual acceptance journey — 15 steps** | §25.6 | **PART** | Two real Dinas: install → catalog → publish → discover → quote → approve → accept → **restart both** → matching receipts → revoke and prove no further work | In-process half DONE: `external_fulfilment_journey.test.ts` walks credential → catalog-through-connector → idempotency probe → staff authority → ambiguous effect → earned retry (same key twice) → fulfilment reconciliation → inbox, all through the owner routes. RESTART IS NOW COVERED: `acceptance_restart_journey.test.ts` runs steps 13-15 with two real nodes — two SQLCipher files, two commerce runtimes, a SQLite workflow store so the supplier's plugin lane survives too — and CLOSES both databases mid-trade, reopening them with the same passphrases and rebuilding both nodes from disk. Nothing crosses in a variable. 5 scenarios: the buyer still holds the acknowledgement; both sides still name the same order digest and idempotency key; a resubmission is refused `already_submitted` WITHOUT reaching the wire; a replayed order is answered INLINE by Core from its receipt (the ingress returns the `coreAnswerJson` variant, never a task — so the runner is not asked and a supplier pack can neither un-accept nor accept twice); and a paused install refuses new work with a lane that goes quiet. Mutation-verified the only way that matters: pointing the reopen at EMPTY databases kills all five, so the suite is genuinely reading from disk. The MANUAL half stays open: a real ERP and a real PDS.

§25.6 is the production claim. Nothing below it substitutes.

---

# Critical path to a demonstrable Phase 1

```
0.2 CommerceOrder ─┐
0.3 StatusChain ───┼─→ 0.5 boundary ─→ 3.6 composition root ─┐
0.4 audience ──────┘                                          │
                                                              ├─→ 11.6 acceptance
3.1 provider install ─→ 3.2 ingress ─→ 3.3 listing binding ───┤
                                    └─→ 3.9 manifests + SDK ──┘
                                                              │
4.2 validated restore ─→ 4.3 reconciliation ─→ 4.4 genesis ───┘
```

Everything else is parallelisable. The three chains above are serial and none of
them can be skipped for a credible §25.6 run — step 13 is "restart both sides",
which is precisely what WS-4 governs.

## Sequencing note

WS-0 before WS-3, not after. Three of the six round-2 review findings were
round-1 fixes leaking at a second call site, and the round-3 finding was the
same rule leaking at a third. Wiring more call sites onto a substrate whose
rules live in call sites multiplies that failure mode rather than containing it.

## Honest scale

WS-1 and WS-2 are roughly a third of the vertical and carry the best test
coverage in the codebase, which is exactly why the green suite reads more
reassuring than the state warrants. WS-3 through WS-5 are the bulk of the
remaining engineering; WS-8 through WS-10 are post-pilot.
