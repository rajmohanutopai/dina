[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "dina-dht"
version = "0.3.0"
description = "did:dht support for Dina — wraps pkarr via PyO3"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
