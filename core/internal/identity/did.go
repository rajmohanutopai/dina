// Package identity manages DID documents, personas, and key lifecycle.
// did.go — DID document creation, resolution, and rotation.
// Supports did:key (local) and did:plc (via PLC Directory).
package identity
