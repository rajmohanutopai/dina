// Package config defines the configuration struct for dina-core.
// config.go — Typed configuration: listen addresses, vault paths,
// brain URL, security mode, spool limits, rate limits.
package config
